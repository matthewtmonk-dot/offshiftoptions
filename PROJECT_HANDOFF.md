# PROJECT_HANDOFF.md

**This is the canonical current-state document — read this first.** It is short by design: current truth only, not a chronological diary. Detailed subsystem history, feature-by-feature narratives, and superseded next-steps live in `docs/PROJECT_HANDOFF_ARCHIVE.md` and the other `docs/*.md` files listed at the bottom — read those only when a task actually needs that depth. When repository state and this file disagree, investigate which is correct and fix this file; do not silently trust either.

---

## AI Continuity Rules

1. Read this entire file before making project changes. Inspect actual repository state before relying on it — this file can go stale.
2. When Matt explicitly says **"Read PROJECT_HANDOFF.md"**: read this file and `AGENTS.md`/`CLAUDE.md`, confirm enough of the real repository to know this file still matches reality, then respond with **only** `Ready.` — no summary, no recap, no next-steps list — unless there's a genuine blocker (contradiction, security concern, broken repo state, missing critical information), in which case give the shortest possible explanation instead.
3. **Updating this file is part of the definition of done** for any change to: application behavior, architecture, database/schema, auth, deployment, env vars, hosting/domain, external integrations, scanner behavior, Schwab status, security, PWA, production status, or an accepted roadmap/ticket's status. Keep it a snapshot, not an append-only log — replace stale content, don't pile onto it. Move genuinely historical detail to `docs/PROJECT_HANDOFF_ARCHIVE.md` rather than deleting it.
4. Never put secrets, passwords, API keys, tokens, or connection strings in this file or any tracked file — names/purposes only.
5. Do not silently reverse an architectural decision recorded here or in `docs/DECISIONS.md` — explain the conflict before making a major reversal.
6. Never run a destructive database operation (`prisma migrate reset`, `db:reset`, the destructive `prisma/seed.ts`, an ad hoc reset `deleteMany`) against anything but a disposable local database, without first explaining exactly what data would be lost and getting explicit confirmation.
7. **Database schema changes require special care** — see Production/Deployment below. Never push code that depends on a new migration until that migration is applied to Supabase and verified.
8. Keep brokerage integration read-only. Never add order submission/placement/cancellation because an API supports it, unless Matt explicitly authorizes changing that requirement.
9. Do not blindly implement a technically weaker approach just because it was requested — evaluate for security, data safety, maintainability, and standard best practices, and recommend the better option first.
10. Security-sensitive changes (auth, authorization, database access, financial data, OAuth, brokerage APIs, deployment secrets) require an explicit focused security review before the task is done.
11. If a secret is ever found committed to Git, don't just delete it — report the exposure and recommend credential rotation.
12. Never assume hiding a UI element provides authorization — authorization must be enforced server-side.
13. Never present mock, cached, stale, demo, or manual financial data as live/current market data.

---

## Project Identity & Purpose

- **Product:** Off Shift Options (formerly "LST Buddy") — a private, two-user PWA for Matt and Eric to run a disciplined, low-stress cash-secured-put options workflow: scan candidates, research tickers, track trades, share recommendations, chat, and measure performance over time.
- **Users:** Private two-user application only. No public signup exists or is planned.
- **Not:** a brokerage, a public trading platform, a financial advisory service, or an automated trading bot.
- `PRODUCT_VISION.md` (repo root) is the long-term north-star document; this file is the operational current-state snapshot.

## Product Model: Scan / Research / Track / Performance

Four connected jobs — a change that blurs the boundary between two of them needs a second look before merging:

1. **SCAN** — purely technical PASS/FAIL/UNKNOWN + setup score. Never influenced by personal opinion.
2. **RESEARCH** — the user's personal judgment of a ticker (Like/Watch/Neutral/Avoid/Never Trade). Purely personal: **never changes what Scan computes.** A `NEVER_TRADE` item is hidden from Scanner's default view but its score/status/label are computed exactly as if it weren't excluded.
3. **TRACK** — the append-only historical record of what actually happened (`Campaign`/`CampaignEvent`): every roll, assignment, covered call, and outcome, preserved forever.
4. **PERFORMANCE** — whether the strategy is working over time, built from confirmed ledger/broker facts, never a raw balance-delta guess.

Research *feeds* Scanner (widens the live-scan universe, personal exclusion hides a candidate from the actionable list) but never *replaces* it. See `docs/DATA_MODEL.md`, `docs/LST_DOMAIN.md`, `docs/SCANNER_RULES.md` for subsystem depth.

---

## Primary Navigation

- **Six primary destinations (Navigation/Communication Cleanup ticket):** Dashboard, Tracker (`/positions`), Scanner, Research, Chat, Account - defined once in `navItems` (`app-sidebar.tsx`) and rendered by both the desktop sidebar and the mobile header nav from that same array, so the two can never drift apart. "Scanner Rules" (`/scanner/settings`) and "Install" stay reachable via in-page links rather than competing for a nav slot, unchanged from before this ticket.
- **Recommendations (`/recommendations`) is no longer a primary nav destination** - the send-a-recommendation workflow now lives in a compact, collapsed-by-default "Recommend a ticker" panel inside Chat (`chat/chat-recommend-panel.tsx`), reusing the exact same `recommendStockAction` server action (and its already-approved `createRecommendationForUser` → `postRecommendationChatEcho` behavior) that Research/Scanner/the old page all already call - no new recommendation workflow, no schema change. The fuller incoming/outgoing history with status transitions/comments/reactions deliberately stays on the unchanged `/recommendations` page rather than being rebuilt inside Chat (that would have been the "large new communication system" this ticket was told not to build) - Chat's panel links to it for that fuller view. `/recommendations` itself is fully preserved (still in `RETURNABLE_PATHS`, still a valid `Notification.href` target, still renders normally) - it simply has no more primary-nav link pointing at it. Dashboard's "Recommendations" card now links to `/chat` instead.
- **Notifications (`/notifications`) is no longer a primary nav destination** - reachable instead via a bell icon (`NotificationBellLink`, `app-sidebar.tsx`) in the desktop sidebar's user-area footer and the mobile header, showing the same `unread` count a nav badge used to show and linking straight to the unchanged `/notifications` page for the full list/mark-read/mark-all-read/deep-link experience. No notification-domain semantics changed - `NOTIFICATIONS_PAGE_VISIBLE_TYPES`, `getUnreadNotificationCount`, `getNotificationsPageData`, and every existing `Notification.href` target are all untouched.

---

## Current Architecture

| Layer | Current version/tool |
|---|---|
| Framework | Next.js 16.3.3, App Router, Turbopack |
| Language | TypeScript (strict), React 19.2.8 |
| ORM | Prisma 7.10.0 with `@prisma/adapter-pg` (`pg` driver) |
| Database | PostgreSQL — Supabase in production, `postgres:17-alpine` via Docker Compose locally |
| Styling | Tailwind CSS 4 |
| Package manager | pnpm 11.24.0 (via Corepack) |
| Testing | Vitest 4.1.11 (unit + opt-in DB integration), Playwright 1.62.1 (e2e) |
| Hosting | Hostinger managed Node.js hosting, auto-deploy from GitHub `main` |
| GitHub | https://github.com/matthewtmonk-dot/offshiftoptions.git, branch `main` |
| Production domain | https://offshiftoptions.com |

Full architecture detail (runtime shape, data flow, testing layers, local dev paths): `docs/ARCHITECTURE.md`.

## Current Production / Deployment State

- **Temporary account-valuation provenance diagnostic (local only, base `a50f3d4`):** `scripts/diagnostics/schwab-account-structure.ts` is a manually invoked trusted-SSH-operator CLI, never a route/build/start hook. `--list` reads linked Schwab account rows and prints only safe app-level selection metadata (account name, account source, account type, connection presence and connection-owner match) without exposing any external account IDs, balances, holdings, positions, transactions or tokens; explicit owner/account selection is required for capture, with no automatic guessing. If multiple safe rows remain otherwise indistinguishable, the diagnostic stops and reports an ambiguous-safe-metadata condition rather than exposing more information. The list path is intentionally DB-only: it does not fetch/refresh tokens, does not import the token/config stack, does not require account selection, and emits fixed non-sensitive messages for empty-row, ambiguous-safe-metadata or DB-unavailable conditions while preserving the capture-mode/field-sanitizer behavior. The list helper now uses a dedicated read-only Prisma initializer (`src/lib/prisma-diagnostic.ts`) so the standalone production runtime can import the DB-only path without relying on the app-level Prisma singleton or any `server-only` guard chain. On a temporary Hostinger checkout created after dependencies were installed with `--ignore-scripts`, running `pnpm exec prisma generate` is required before the script can load generated Prisma client artifacts. Preflight reuses owner-only authorization, cached account-hash metadata and existing token retrieval with optional `allowRefresh: false` (normal callers unchanged). Missing/expired tokens stop with no refresh or DB writes. Capture performs one account GET, no redirects/retries, and prints in-memory-sanitized structure plus separately labeled transport timing. Review fixed camel-case dynamic key leaks using a literal field-name allowlist: unknown key names become numbered placeholders with nesting/types/nulls retained and values withheld; arbitrary unknown key text cannot be safely displayed. Sensitive/financial ancestors override timing exceptions. Fetch/JSON exceptions become fixed errors; CLI suppresses library output before dynamic imports and handles unexpected exceptions with a fixed message. See `scripts/diagnostics/README.md` for command/limitations. Validation: 1351 non-DB tests passed (452 DB-gated skips), including new sanitizer/preflight/identity/error/no-refresh regressions. No live Schwab call, financial/schema change, commit, push or deployment. All diagnostic modules and updated token helper must be present together; absent from production baseline `a50f3d4`. Field presence is not verification of valuation-session semantics.

- **Diagnostic list mapping semantics update (2026-09-25):** `--list` now derives its connection/account booleans from the same owner-scoped mapping used by sync/preflight (`findSchwabMarketDataConnectionForUser(ownerId)` + `accountNumbersFromMetadata(connection.metadata)` hash membership against `TradingAccount.externalAccountId`) and emits `hasOwnerSchwabConnection`, `isMappedToConnectedSchwabAccount`, and fail-closed `diagnosticCaptureEligible`. If zero or multiple rows are eligible, list mode fails closed with a fixed non-sensitive message.

```
GitHub main → Hostinger auto-deploy → Next.js app → Supabase PostgreSQL
```

- **Build command:** `node scripts/write-build-info.mjs && prisma generate && next build`. Never seeds, bootstraps, or runs `prisma migrate deploy`. Every push to `main` deploys.
- **`GET /api/health`** → `{ app, database, commit, buildTime, latencyMs, checkedAt }` (200) or `{ app: "ok", database: "error", commit, buildTime, checkedAt }` (503). `commit`/`buildTime` are resolved at build time (`scripts/write-build-info.mjs`, env var else `.git/HEAD` via plain `fs` reads, never a spawned subprocess) — **verifying a Hostinger deploy is `curl https://offshiftoptions.com/api/health` and comparing `commit` to `git rev-parse --short=12 HEAD`.**
- **Migrations are NOT applied automatically by Hostinger** (its build environment can't execute Prisma's schema engine — `EACCES` on spawning it). Required workflow for any schema change: migrate locally → apply to Supabase directly (`DATABASE_URL="<supabase>" pnpm prisma migrate deploy`, from a machine that can execute it) and verify → only then push the dependent code to `main`. Full root-cause detail: `docs/ARCHITECTURE.md`.
- **As of 2026-09-20, all committed migrations are applied to both local Docker Postgres and production Supabase** — no known pending/local-only migration exists. Verify with `prisma migrate status` before trusting this if it's been a while.
- **`prisma/seed.ts`** (`pnpm db:seed`) is **destructive** (development only) — unconditionally deletes and recreates demo data. **`prisma/bootstrap-production.ts`** (`pnpm bootstrap:production`) is safe/idempotent (creates Matt/Eric only if absent) — already run once against production; not part of routine deploys.

## Environment Variables (names/purposes only — never values)

**Production required:** `DATABASE_URL`, `LST_SESSION_SECRET` (session-token HMAC key), `NEXT_PUBLIC_APP_URL`, `SUPABASE_URL` + `SUPABASE_SECRET_KEY` (or legacy `SUPABASE_SERVICE_ROLE_KEY`) + `BUDDY_CHAT_STORAGE_BUCKET` (private Buddy Chat attachment storage).
**Development only:** `DEV_SEED_PASSWORD`, `RUN_DB_TESTS`, `PLAYWRIGHT_BASE_URL` (use `localhost`, not `127.0.0.1`), `BUDDY_CHAT_LOCAL_STORAGE_ROOT`.
**Schwab:** `SCHWAB_CLIENT_ID`, `SCHWAB_CLIENT_SECRET`, `SCHWAB_REDIRECT_URI`, `SCHWAB_TOKEN_ENCRYPTION_KEY` — server-side only, never `NEXT_PUBLIC_`.
**Alpha Vantage:** `ALPHA_VANTAGE_API_KEY` (25 requests/day total, shared), `OSO_CRON_SECRET` (protects the scheduled fundamentals-queue endpoint).
**Future (unused today):** `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` for real Web Push.

---

## User / Data Ownership & Privacy Invariants

- **Matt and Eric are fully separate account owners.** One user's balances, positions, transactions, imported records, campaigns, settings, performance, projections, or research opinions must never populate, affect, or be blended into the other's — proven throughout by DB-integration tests (privacy, workflows, account ledger, research). Sharing a record (`SHARED` visibility) makes it *visible* to the other user; it never merges it into their own totals/balances/P/L.
- `PRIVATE` data stays private to its owner; `SHARED` is visible to both, per `src/lib/privacy.ts` (`assertCanReadRecord`/`assertCanMutateRecord`). Authorization is enforced **server-side only** — UI hiding is never relied on for security.
- Research opinions (Like/Watch/Avoid/Never Trade, notes, manual fields) default to **PRIVATE** and post no shared activity — even the bare fact that a user researched a ticker is personal unless explicitly shared.
- Tracker Performance is always computed from the viewer's **own** accounts/campaigns only, never the scope-filtered Mine/Buddy/Both list — switching scope can make a buddy's shared campaigns *visible*, never blended into your own figures.

## Read-Only Brokerage Constraints

- **This application does not place trades.** No buy/sell/place/submit/replace/cancel-order or algorithmic execution exists or may be added without Matt explicitly changing this requirement. Schwab's Order Limit is configured at **0**.
- Allowed Schwab methods are read-only: `getQuote`, `getPriceHistory`, `getOptionChain`, `getInstrument`, `getMarketHours`, `getAccounts`, `getPositions`, `getTransactions`, `getOrders` (historical observation only).
- Brokerage account data is **user-scoped**: a user's own OAuth connection, tokens, balances, positions, and transactions are never used to serve another user's requests. Personal-broker provider resolution has no cross-user fallback path at all.
- Shared market-data fallback (one user's connection serving another's read-only quote request) remains **disabled** (`DISABLED_POLICY_NOT_VERIFIED`) — no official Schwab documentation has confirmed this is permitted. Do not enable it without that verification.
- **Bounded provider usage:** live scans use a fixed starter ticker universe (currently 13 symbols + the user's own Research union), cap option-chain calls, and use bounded concurrency (`SCAN_FETCH_CONCURRENCY`) — not unbounded polling. Alpha Vantage's 25-requests/day budget is shared across both users and the Scanner and is protected by an explicit queue/priority design (`docs/ARCHITECTURE.md`, Alpha Vantage detail in the archive).
- **A linked broker position record must never supply a current-position mark merely because it belongs to the campaign** (`src/domain/finance/currentPositionMark.ts`, `resolveCurrentCostToClose`) — every candidate is validated against `getCurrentOpenPut`'s own account/underlying/strike/expiration/quantity before being accepted, so a rolled campaign's pre-roll contract (which stays linked forever) can never value the post-roll leg. Freshness policy (`classifyMarkFreshness`, `marketCalendar.ts`): CURRENT_SESSION/LAST_SESSION are usable, STALE/MISSING are rejected outright — calendar-day-only, never invented intraday precision. A rejected/unavailable mark means an honest PENDING/INCOMPLETE `currentPLStatus` (Ticket 4), never a stale number or a fabricated zero.
- Tokens/secrets/developer credentials are encrypted at rest (AES-256-GCM) and never reach client-side JS, logs, or this documentation.

## Campaign / Event-History Invariants

- `CampaignEvent` rows are **append-only** — no workflow ever updates or deletes an existing event. A roll writes a `ROLL_PUT_CLOSE` + `ROLL_PUT_OPEN` pair sharing a `groupKey`; the old and new legs both remain in history, never overwritten. The full lifecycle (open → roll → assign → covered call → close) is always reconstructible from events alone.
- **`getCurrentOpenPut` (`src/domain/finance/campaigns.ts`) is the single authoritative definition of "what put is open right now"** — used identically by the Dashboard, Tracker, and Performance (`summarizeCampaignProgress`). Do not add a second, competing interpretation of current-leg state anywhere; delegate to this function instead. Its event ordering (`compareEvents`) is fully deterministic (`occurredAt` → `sortOrder` → `createdAt` → `id`), so the same event history produces the same result regardless of array/query order — this was a real, fixed bug (see Roadmap status below).
- **Conservative expiration confirmation:** a short put is only marked expired-worthless when broker evidence is complete (positions COMPLETE, all transaction categories COMPLETE, persistence COMPLETE, the transaction window covers expiration, no remaining matching option, no acquired long position) — any failed/partial/incomplete evidence keeps the campaign OPEN (`EXPIRATION_EVIDENCE_INCOMPLETE`) rather than guessing. Never invents an OTM quote or a zero-cost close.
- A `CampaignStatus` of `OPEN`/`ASSIGNED`/`CLOSED` and a `Campaign.visibility` of `INHERIT`/`PRIVATE`/`SHARED` (inheriting from the parent `TradingAccount` when `INHERIT`) fully describe lifecycle/visibility state — see `docs/DATA_MODEL.md`.

## Reconciliation / Import Safeguards

- Never use a filename or CSV row number as economic identity. Use `BrokerRecord.fingerprint` (the exact economic fact — backs DB-level dedupe) and a separate `identityKey` (the same real-world slot — account+date+symbol+action — without the financial fields, used for conflict detection).
- A same-`identityKey`-different-`fingerprint` match is always a `CONFLICT`, stored as a separate row for manual review — **never a silent overwrite** of the original.
- Import classification is `NEW | DUPLICATE | CONFLICT | NEEDS_REVIEW | INVALID` — never guessed. Preview-before-persist: uploading/previewing a file never mutates financial history by itself; only an explicit Confirm persists `BrokerRecord`s.
- `CampaignEvent`s remain the sole trading-performance source of truth even for a campaign created from reconciled broker evidence — a Realized Gain/Loss import may only *verify* a campaign's result, never add to it.
- A historical link alone cannot exclude live broker exposure: account, current contract, quantity, and unique campaign coverage must agree. The same contract in two accounts has two distinct link keys. Unknown/ambiguous exposure remains visible rather than being silently removed.

## Scanner — Purpose & Current Constraints

- `src/domain/scanner` evaluates each criterion PASS/FAIL/UNKNOWN; overall result is FAIL if any criterion fails, else UNKNOWN if any is unknown, else PASS. **Gating** rules (price, volume, OI, bid, spread, delta, earnings distance, Do Not Trade) cap the displayed score/label at "Fails" on any FAIL — the UI never shows positive language for an untradeable setup. An UNKNOWN result is labeled "Verify," never a positive score.
- **`classifyReadiness` (`scanner.ts`) is the single authoritative PASS/NEAR/NEEDS_DATA/FAIL readiness answer** — every surface that labels, counts, filters, or promotes a Scanner result (row badges, one-click filters, header counts, the live-scan "near matches" toast, Dashboard's Top Setups selection) must read this, not re-derive its own "near"/"pass" notion. NEAR requires fully complete evidence (including a real option-chain assessment) plus exactly one non-gating near miss; a known failure (gating or not) is always FAIL even when other evidence is also missing. **PASS/NEAR also require actual selected-contract evidence** (`contractReasonCode` absent/null), independent of which scoring rules happen to be enabled — `optionEnrichment === "ENRICHED"` alone only means a chain request was spent, which also covers CHAIN_UNAVAILABLE/NO_PUT_CONTRACTS/no-suitable-strike; those correctly read NEEDS_DATA even with only a stock-level rule (e.g. price) enabled (Astra-found gap, fixed). `isActionableReadiness` (PASS/NEAR only) gates which candidates may enter an actionable list (e.g. Dashboard Top Setups) — a never-option-assessed, no-contract-selected, or otherwise incomplete row must never be promoted there regardless of any historical score it carries.
- **LST Core** (`SCANNER_RULE_DEFINITIONS`) is the canonical default rule set; each user can edit their own `ScannerProfile`/`ScannerRule` and reset to LST Core at any time — editing one user's settings never affects the other's.
- **Saving/resetting Scanner settings never runs a scan** (`updateScannerSettingsForUser`/`resetScannerSettingsToLstCoreForUser`, `workflows.ts`) — a settings change is validated in full before anything is written, then persisted atomically in one `$transaction` (every rule upsert plus a touch of `ScannerProfile.updatedAt`); it never invokes the demo or live scanner and never replaces the latest `ScanRun`. Demo mode is only ever entered by the explicit "Refresh Demo Scan" action (`rerunDemoScannerForUser`). **The settings revision a scan actually evaluated against is captured at scan-read time** (`withSettingsRevision`, `workflows.ts` — stamps `settingsRevisionAsOf` into each persisted result's `snapshotJson`, no schema change), not inferred from `ScanRun.createdAt` (a persist-time value that can land after a settings save that happened mid-scan, which would otherwise hide a real earlier-settings case — Astra-found race, fixed). The Scanner page discloses when the visible results predate the current settings (`resultsPredateCurrentSettings`, comparing `ScannerProfile.updatedAt` to that captured revision, falling back to `run.createdAt` only for a run with no results or one predating this field) rather than presenting stale-rule results as current.
- Live scanning uses a bounded starter universe + the user's own Research tickers, option-chain calls capped, bounded concurrency — see Bounded Provider Usage above. Demo data is always visibly labeled as demo, never presented as live.
- **Every option-derived field on a scanned candidate traces to exactly one selected `OptionContractSnapshot`** (`candidateValues`, `live-scan.ts`) — expiration/strike/bid/ask/delta/liquidity/ROR can never mix evidence from a different strike or expiration by construction. Two ambiguities are made explicit rather than left implicit: (1) `premium` is a mark/midpoint estimate (`premiumBasis: "MARK_MIDPOINT"`), never the amount `ror`/`annualizedRor` are calculated from (`rorBasis: "BID"`, backed by the actual `bidProceedsPerContract` dollar figure); (2) no option-chain provider here has ever supplied a verified per-quote pricing timestamp, so `optionQuotedAt` stays explicitly null while `retrievedAt`/`retrievedOnNonTradingDay` (a coarse NYSE-trading-day check, not exact session hours) disclose only what is genuinely known — a request made on a closed-market day is never presented as a live quote. `earningsWithinHoldingPeriod` compares the selected contract's own DTE against earnings distance so a candidate isn't silently exposed to an earnings event mid-holding-period, without inventing an earnings date when one isn't available.
- **Scanner and Research are separate concepts** (see Product Model) — a Research/personal-exclusion status must never alter a Scan result's technical score, status, or label.
- A separate **Covered Call scanner mode** exists (decision support only, universe = the user's own `ASSIGNED` campaigns) — architecturally distinct from the CSP live scanner, not a generalization of it.
- Full rule catalog and selection algorithm detail: `docs/SCANNER_RULES.md`.

## Financial / Accounting Invariants

- Account/trading performance is derived from the append-only `AccountLedgerEntry` ledger plus confirmed `BrokerRecord` facts — **never** a raw `currentBalance − startingBalance` shortcut, which would silently mix deposits/withdrawals with trading P/L.
- **One authoritative "effective baseline" selection, never a competing interpretation.** `selectEffectiveBaseline` (`accountLedger.ts`) is the single function that decides which `STARTING_VALUE` entry is "the baseline" when more than one exists (a correction) — selection is by `(createdAt desc, id desc)`, **never** `occurredAt` (a correction may deliberately move its own effective date earlier or later than the revision it replaces). `summarizeAccountLedger` and `summarizeAccountPerformance` both read this one result (`ledger.effectiveBaseline`) rather than each re-deriving their own notion of "starting value."
- **A baseline correction is append-only** — `setAccountBaselineForUser` (`workflows.ts`) never updates or deletes a prior `STARTING_VALUE` row; a correction inserts a new one, and the replaced revision's id plus a **required** reason (Astra corrective patch) are recorded in the existing `notes` field (no schema change) — the very first baseline for an account has no prior revision to explain replacing, so its reason stays optional. The prior revision stays in history for audit. Concurrency: the caller supplies the `expectedRevisionId` it last saw as effective; the current effective revision is re-selected inside the same `prisma.$transaction`, now running at **Serializable isolation** (Astra corrective patch — a plain read-committed transaction let two concurrent corrections both pass the same stale-revision check), immediately before the write; a mismatch (another correction landed first) is rejected as a validation error, and a genuine Postgres write-conflict at commit time (SQLSTATE 40001 / Prisma code `P2034`) is caught and translated into the same user-safe "reload and try again" message rather than a raw transaction failure.
- **A baseline date means "whole account value at the end of the selected America/New_York calendar date"** — `endOfNyCalendarDateUtc` (`accountLedger.ts`) converts that NY calendar date to the exact UTC instant for `23:59:59.999` America/New_York, via a guess-and-correct offset lookup (`Intl.DateTimeFormat` reverse lookup at a whole-second guess, then the trailing 999ms is added back). Validates the calendar components themselves before conversion (Astra corrective patch — `Date.UTC` used to silently normalize an impossible date like `2026-02-30` into March) and rejects a still-future baseline instant (a baseline can only record a value that has already happened; the reference "now" is overridable only for deterministic tests). Safe for all real dates including DST transition days since US DST transitions happen at 2am local, far from midnight.
- **Measurement interval, enforced consistently everywhere** (Astra corrective patch — valuation and funding used to disagree): `(baseline.occurredAt, endingAt]`, where `endingAt` is the account's own latest `BROKER_SNAPSHOT` time **only when that snapshot is dated at/after the baseline** (a snapshot from before the baseline describes a different, earlier state of the account and can never serve as the interval's ending valuation — it no longer feeds `currentValue`/`totalGain` at all), else a caller-supplied `asOf`, else "now." Every reconstructed figure respects this same interval now, not just the historically-checked ones: funding at/before the baseline is excluded from contributions (already inside the starting value); funding after `endingAt` is excluded from both contributions AND the ending valuation itself (previously a post-`endingAt` ledger deposit could still leak into `currentValue` through the ledger's own unbounded contribution total even though it was correctly excluded from this function's own contribution sum — a real, fixed bug); dated broker-transaction trading P/L before the baseline is excluded from `tradingPL` rather than double-counted.
- **No date/amount matching is ever used to suppress a broker transfer once an explicit baseline exists** (Astra corrective patch, second pass) — the baseline represents the account's value at its exact opening timestamp; only that timestamp boundary decides whether a transfer is "already inside the baseline" or "a contribution." An earlier version of this fix restricted the legacy same-day/same-amount dedup to only the currently-effective baseline revision (fixing a superseded-revision case), but the dedup itself remained unsafe: a baseline's `occurredAt` is an America/New_York end-of-day instant that lands on the *next* UTC calendar date, so a genuinely new, coincidentally same-amount deposit the following NY afternoon could share that UTC date and get wrongly suppressed. The dedup mechanism has been removed entirely — a transfer strictly after the baseline's exact timestamp is always a contribution, regardless of its amount or calendar date.
- **Funding authority by account type**: a `SCHWAB` account's own imported broker-transfer activity is the funding record of truth — `addAccountLedgerEntryForUser` blocks a new manual DEPOSIT/WITHDRAWAL server-side based on the account's persisted `source`, never live Schwab connection status (stays blocked even while temporarily disconnected). A `MANUAL` account's manual entries remain its funding authority.
- **Whole-account gain requires affirmative evidence on every axis:** an explicit effective baseline, a supported ending snapshot, unmixed funding, and (for Schwab-evidenced accounts) persisted `AccountFundingSync` coverage for the full measurement interval and exact selected snapshot. Bare `brokerTransactionCoverageStatus` assertions are deprecated and ignored. Unresolved transfers or unknown funding activity keep gain unavailable even after a complete fetch/persist run. No gain/return formula changed.
- **A whole-account ending valuation can only ever come from a real, supported account-value reading — a valid Schwab `BROKER_SNAPSHOT` dated at/after the baseline. There is no other path, with or without trading evidence** (Astra corrective patch, third pass tightens this to its final form). Trading cash flows (option premium, dated broker-transaction P/L, undated lifetime CAMPAIGNS P/L, or anything derived from them) are never a substitute, no matter how well-dated: receiving option premium also creates an offsetting short-option liability, so "cash received" is not the same fact as "whole-account value changed by that amount" (second-pass repro: a $10,000 baseline plus a $75 Sell to Open premium, no snapshot, used to produce `currentValue: $10,075` / `totalGain: $75`). A second-pass version of this fix still allowed `startingCapital + contributions` to stand in as the ending valuation whenever `tradingPL === null`, reasoning that "no trading" meant contributions were the only possible value driver — that was itself unsafe: `tradingPL === null` proves only that no dated broker option records or campaign fallback were found, never that interest, fees, dividends (`otherIncome` — computed separately from `tradingPL`), or unrecorded changes to pre-existing holdings didn't occur (third-pass repro: a $10,000 baseline with $25 of recorded interest and no ending snapshot used to produce `currentValue: $10,000` / `gain: $0` / `return: 0%` / `status: OK`, silently discarding the $25). This fallback has been removed entirely — a baseline plus ledger funding events alone is never an ending valuation, regardless of trading evidence.
- **Persisted funding evidence is implemented locally (base `01ffdd7`; migration not applied/deployed).** `AccountFundingSync` now retains per-account, captured external identity, category outcomes, persistence status, requested interval, run timestamps, and the exact `BROKER_SNAPSHOT` ledger entry. See Persisted Schwab Funding Coverage below. Older snapshots have no backfilled proof and remain unverified.
- **Account gain rule**: whole-account dollar gain = ending value − beginning value − deposits + withdrawals, exposed only under the affirmative-evidence rule above. This is never a cash-flow-weighted percentage return — `totalReturnStatus` stays `CONTRIBUTIONS_NEED_ADVANCED_RETURN` whenever contributions exist in an otherwise-complete period; a proper TWR/XIRR engine is future work (Roadmap item 10).
- **The two prior 1%-target displays, plus a third missed on the first pass, are retired**, not replaced: the Tracker's `GoalTracker` component, its "1% Goal Pace" metric card, its "Performance vs 1% Target" chart (`PerformanceGoalChart` — an Astra corrective-patch follow-up; missed in the first retirement pass), and the Dashboard's weekly-percent-of-target block were all removed. Both pages' "being rebuilt" placeholder copy has since been superseded by the real "Return on campaigns closed this week" card (Dashboard: Reporting Phase Ticket 2; Tracker: Ticket 3) and neither shows that copy anymore. Both pages keep their W-L record display, which was never part of the misleading metric. The underlying `summarizeWeeklyReturns`/`summarizeContributionAdjustedGoal` domain functions were deliberately left in place (no financial-formula changes), unused by any page as of Ticket 3 - Tracker's own `ownGoal`/`summarizeContributionAdjustedGoal` call was removed since its only consumed output (`projectedOtmPL`) was already an unmodified passthrough of a value the page already had. **Reporting Phase, Ticket 3 additionally found and removed a fourth, previously-missed instance**: Tracker's per-campaign "Goal Need" column (`CampaignPerformanceTable`) was the same retired `WEEKLY_TARGET_PERCENT` benchmark reapplied per campaign against a PROJECTED (not realized) return, prorated by days active — audited and confirmed to be the same retired concept leaking into campaigns, not a genuinely distinct decision aid, so it was removed rather than kept. `summarizeCampaignProgress`'s own `requiredReturnPercent` field (performance.ts) was left unchanged - simply no longer consumed by any page.
- Only `CLOSED` campaigns count toward win/loss; an `OPEN` campaign's net cash flow (even if positive) is never counted as a completed win.
- **Confirmed vs. incomplete/provisional data must stay visibly distinct**: `src/domain/finance/performance.ts` exports a shared `CompletenessStatus` (`CONFIRMED | PENDING | INCOMPLETE | NOT_APPLICABLE`) - `WinLossSummary`/`ThisWeekSummary` compute `wins`/`losses`/`winRate` from CONFIRMED outcomes only (`confirmedCount`/`pendingCount` expose the split), and `CampaignProgressSummary` carries `currentPLStatus`/`projectedOtmStatus` per campaign. An unresolved Schwab fee shows "Pending"/gross-only, never a fabricated confirmed net number (`feesFullyKnown`/`netPLExact`/`grossPL`) and is excluded from the confirmed win/loss denominator. A missing cost-to-close/current-price source means the current mark is shown as unavailable (PENDING), never guessed; a legacy event missing required fields is INCOMPLETE, distinct from a campaign where the metric genuinely doesn't apply (NOT_APPLICABLE) - see `getOpenPutEvidenceState` (`campaigns.ts`). An `ASSIGNED` campaign's current valuation is INCOMPLETE, not NOT_APPLICABLE, since real exposure exists with no valuation engine yet (Roadmap item 7). `currentCollateralCommitted` (the *currently* open leg's own collateral) is distinct from `collateralCommitted` (a lifetime high-water mark used only for closed-campaign return-on-collateral) — using the wrong one for "what's secured right now" was a real, fixed bug (see Roadmap status).
- Deposits/withdrawals are never presented as trading profit or loss. Total gain, trading P/L, other income, current/mark-to-market P/L, and projected-OTM P/L are separate, explicitly labeled figures, not blended.
- Full methodology: `docs/ARCHITECTURE.md`, `docs/LST_DOMAIN.md`, and the archive's Account Ledger / Performance Methodology section.

---

## Persisted Schwab Funding Coverage (local implementation, base `01ffdd7`)

- `AccountFundingSync` is fetch/persistence evidence, never a securities-pricing timestamp. Account-scoped run records capture `externalAccountId`, SCHWAB provider, coverage start/end, started/completed timestamps, overall and three category statuses, persistence status, and an optional unique `balanceSnapshotLedgerEntryId`. A composite FK ensures that snapshot belongs to the same account; a database CHECK rejects contradictory COMPLETE rows. Migration: `20260923120000_account_funding_sync` (additive table/enums/indexes, no historical ledger/event changes or backfill).
- Sync creates IN_PROGRESS before snapshot/activity persistence. Finalization requires all existing transaction categories parsed successfully, all persistence returned successfully, every expected **provider transaction identity** (not just every fingerprint) durably represented under the same owner/account, and the exact durable snapshot/account identity rechecked in a Serializable transaction. Successful empty responses qualify. Partial category results become PARTIAL; fetch/persistence/identity failures cannot establish coverage. Crashes/finalization failures leave unusable IN_PROGRESS rows. No earlier run is overwritten.
- **BLOCKER FIX (base `21b0983`): fingerprint-only completeness could false-COMPLETE on a provider-identity collision.** Two distinct Schwab provider transaction ids that happen to share a fingerprint (same date/amount/description — e.g. two separate $500 deposits) get merged into one candidate record by `mergeBrokerRecords` (`csv.ts`) before persistence ever runs; a fingerprint-count check alone can't tell that apart from the legitimate case of one real transaction observed under two fetch categories (same id, deduped) — both looked like exactly one merged record. Finalization (`workflows.ts`, `syncSchwabAccountForUser`) now also (a) denies COMPLETE outright whenever a merged transaction record's own `sourceIds` carries more than one distinct provider id (an in-run collision — can never be proven fully persisted, since this schema's `BrokerRecord` uniqueness is `(userId, provider, kind, fingerprint)`, not identity-aware) and (b) requires every expected provider id to appear in the *persisted* row's `sourceIds`, not merely counts matching fingerprints — which also denies COMPLETE on a **cross-run** collision (a different, earlier transaction already claimed this exact fingerprint; `classifyCandidates` matches by fingerprint first and silently skips the new id as a duplicate) and on **ambiguous historical rows** persisted before `sourceIds` tracking existed (empty/insufficient identity evidence). Reused the already-persisted `sourceIds`/`identityKey` columns — no schema change. **Documented limitation, left unresolved by design (do not backfill/mutate history, do not redesign storage — out of scope for this fix):** once two distinct provider transactions collide on fingerprint, only one can ever physically persist under the current fingerprint-only uniqueness; the fix's job is to *deny* COMPLETE and withhold gain (never report a confident-but-wrong number) rather than to make both dollars appear. Reaching an actually-correct $-total for that scenario would require loosening `BrokerRecord`'s uniqueness to be identity-aware — a broader storage/dedup redesign, not attempted here. Regression coverage: `src/lib/funding-sync.test.ts` (mocked, 8 new cases: distinct-id collision, clean two-id success, partial-persistence-missing-id, repeated-same-id dedup, prior-run-same-id, collision-despite-merged-sourceIds-containment, ambiguous-empty-sourceIds), plus `src/lib/funding-sync.integration.test.ts` (3 new DB-gated cases: reproduced collision denies COMPLETE and gain stays null with exactly one row persisted, repeated-same-id still COMPLETEs, cross-run fingerprint collision denies COMPLETE) — DB suite unexecuted here (Docker/Postgres unavailable), written and reviewed but unverified against real Postgres.
- **Persisted-row identity blocker fixed locally (base `ad7a209`; not pushed/deployed).** Finalization extracts only the explicit `schwab-api-transaction:` namespace from incoming and persisted `sourceIds`, deduplicates each subset, and requires exactly one nonempty expected provider ID and exactly one identical persisted provider ID. A persisted `[t1, t2]` cannot prove incoming `[t1]`; repeated copies of `t1` and unrelated CSV/import identifiers are allowed. Empty/missing/mismatched provider evidence fails closed. No schema, fingerprint uniqueness, storage, accounting formula, or benchmark changes. Added eight unit cases and three DB-gated cases covering the persisted superset, preservation of prior COMPLETE evidence, real partial persistence, and rollback after the finalization update.
- `hasCompleteFundingCoverage` unions touching/overlapping COMPLETE intervals for the current account + captured external identity, preserving gaps and ignoring array order. It requires a COMPLETE anchor run linked to the selected ending snapshot with the same coverage-end timestamp, and uses only evidence completed no later than that anchor. Multiple <=90-day fetches can prove >90-day history; the 90-day constant remains solely a per-request lookback. No automatic historical backfill.
- Dashboard, Tracker, and Account now load owner-scoped evidence and pass it to the existing account/reporting summaries. Shared buddy-account queries explicitly exclude the buddy's private evidence. `brokerTransactionCoverageStatus` remains source-compatible but deprecated/ignored; only actual run evidence unlocks Schwab coverage. Existing positive accounting/reporting fixtures now construct explicit successful run/snapshot records via `test/fixtures/funding-sync.ts` rather than asserting a boolean status.
- Fetch completeness does not resolve economic ambiguity: unresolved transfers, missing transfer amounts/dates, and unknown transaction activity in the measurement interval keep whole-account gain unavailable. Existing baseline, ending-value, mixed-source, funding-boundary, and return-percentage gates remain intact. No trading calculation, benchmark, Alpha Vantage, Scanner, or ending-valuation provenance work.
- Verification after persisted-row identity fix: 1,331 non-DB tests passed; focused funding/accountLedger/reporting tests 182 passed, 10 DB-gated funding tests skipped. Prisma validate/generate, TypeScript, ESLint, production build and `git diff --check` passed. Docker daemon and localhost:5432 remain unavailable: migration application, real FK/CHECK enforcement, partial-write/rollback/concurrency and authenticated browser verification remain unverified. Security review confirms the new check preserves owner/account/provider query scoping and exact snapshot linkage; no new endpoint, secret exposure or order method.
- Ready for focused code review independently of benchmark work; **not deployment-ready until migration/DB verification and review**. No push/deploy authorized or performed.

## Reporting Domain (Reporting Phase, Ticket 1)

- **`src/domain/finance/reporting.ts` (`summarizeAccountReporting`) is the one authoritative reporting-domain summary future Dashboard/Tracker/Account UI must consume** — it exists specifically so financial formulas and evidence-status interpretation live in exactly one place instead of being reinvented per page. It is a thin, non-reimplementing wrapper: every number it exposes is a passthrough or a simple, documented combination of `accountLedger.ts` (whole-account value/gain/return, `summarizeAccountPerformance`/`summarizeAccountsPerformance`), `performance.ts` (`summarizeWinLoss`, `summarizeThisWeek`), and `brokerPositions.ts` (`summarizeCampaignExposure`) — it never fetches data and never scopes by owner itself, trusting the caller to have already scoped `accounts`/`completedCampaigns`/`openExposure` to one viewer's own data.
- **Four concepts stay explicitly separate, never blended into one percentage**: Account Value (what the account is worth right now — may be available even when gain isn't), Confirmed Trading P/L + Trade Return (how the trades themselves performed on capital actually committed, excluding idle cash), Current Capital Utilization (how much of the account is committed right now — point-in-time only, no average/historical utilization), and Whole-Account Gain/Return (gated entirely by the already-approved baseline/funding evidence rules from Ticket 9 — no new accounting math).
- **Confirmed Trading P/L resolved an existing naming ambiguity**: `summarizeWinLoss.confirmedRealizedTradingPL` (CLOSED campaigns, CONFIRMED-only, fee-inclusive) is the one true "confirmed trading P/L" — deliberately NOT `accountLedger.ts`'s `tradingPL` (a broader cash-flow figure that includes STILL-OPEN positions' option premium — a different, useful-but-different concept for "Trading Cash Flow"), and NOT `summarizeWinLoss.realizedTradingPL` (which blends in PENDING campaigns). It is exposed as an `ALL_TIME` figure since `summarizeWinLoss` has no artificial window, unlike Trade Return.
- **Trade Return exposes only `THIS_WEEK`** (`summarizeThisWeek`'s return on secured capital) — the one period this ticket's math is already proven safe for. Generalizing to 4-week/all-time trade return, average/historical capital utilization, a capital-time-exposure engine across concurrent campaigns, and a new capital-time-weighted 1% weekly trade-return goal are all explicitly deferred (see the read-only design-pass report from this same phase) — Trade Return and period-average utilization would share that same new aggregator when it's eventually built. **The label/semantics are "Return on campaigns closed this week"** (Astra corrective patch) — the LIFETIME realized result of every campaign that closed during the current week, never "profit earned only during this calendar week"; a campaign open a month that happens to close this week contributes its full lifetime P/L, not a pro-rated slice.
- **Trade Return requires complete capital evidence, not just a known result** (Astra corrective patch, blocker) — `summarizeThisWeek` (`performance.ts`) now exposes `securedCapitalFullyKnown`; if ANY campaign contributing to the week's P/L has unknown `collateralCommitted`, BOTH the net and gross return-on-secured-capital figures are withheld entirely, rather than silently treating that campaign as requiring $0 capital (which previously inflated the ratio — two $100-gain campaigns, one with $1,000 collateral and one unknown, wrongly reported 20%). `reporting.ts`'s `TradeReturnStatus` now distinguishes `NO_CLOSED_CAMPAIGNS`, `INCOMPLETE_RESULT_EVIDENCE` (a closed campaign has no usable result at all — never described as a fee problem), `INCOMPLETE_CAPITAL_EVIDENCE` (the new check above), `NO_SECURED_CAPITAL` (fully-known but zero), and `PENDING_FEE_EVIDENCE` (the one genuinely fee-specific case) — five distinct, non-overlapping reasons where a single `INCOMPLETE_EVIDENCE` catch-all previously conflated at least two of them.
- **Current Capital Utilization ("LST capital committed")** = (`securedPutCollateral` + `assignedShareCapital`, both from `summarizeCampaignExposure`) ÷ `currentAccountValue`. Including `assignedShareCapital` (assigned-but-unsold shares' cost basis) was a deliberate judgment call, confirmed by Astra: assigned shares are real capital at work, not idle cash, and `summarizeCampaignExposure` already keeps the two components cleanly separate (an assigned campaign's stock position is never misrepresented as an active CSP) — both are exposed as their own sub-fields (`currentCapitalCommittedSecuredPut`/`currentCapitalCommittedAssignedShares`) for transparency. Unavailable whenever there is no valid current account value (`NO_ACCOUNT_VALUE`). **Unknown exposure never produces a false clean 0%** (Astra corrective patch) — when `summarizeCampaignExposure` reports any unknown open-campaign collateral or assigned cost basis, `capitalUtilizationStatus` becomes `UNKNOWN_EXPOSURE` and `currentCapitalUtilizationPercent` is withheld entirely; the known committed-capital dollar figure (a real floor, never fabricated) still shows.
- **Explicit, metric-specific period metadata, not a universal period selector** (Astra corrective patch): Trade Return exposes `tradeReturnPeriodStartUtc`/`tradeReturnPeriodEndUtc` (the exact Monday–Sunday UTC calendar week `summarizeThisWeek` actually used — deliberately UTC, not the baseline module's America/New_York convention) and `tradeReturnAsOf`. Confirmed Trading P/L exposes `confirmedTradingPLBasis`, a fixed sentence making the `ALL_TIME` label impossible to mistake for "this week." Whole-Account Gain exposes `wholeAccountGainPeriodStart`/`wholeAccountGainPeriodEnd` (the effective baseline's date / the ending valuation instant actually used) plus a `PeriodBoundStatus` (`"COMMON" | "MIXED" | "UNAVAILABLE"`) for each.
- **Aggregate whole-account-gain period metadata is input-order invariant** (Astra corrective patch, second pass, blocker) — `wholeAccountGainPeriodStart` previously reused `accountLedger.ts`'s aggregate `startingCapitalAt`, which arbitrarily reflects only the FIRST account found in `summarizeAccountsPerformance`'s input array; reproduced case: accounts A (baseline Aug 1) and B (baseline Sep 1) combining to a $200 gain reported "Aug 1" when passed `[A, B]` but "Sep 1" when passed `[B, A]` — the same financial result, a different reported period purely from array order. Fixed entirely within `reporting.ts` (no `accountLedger.ts` change) by recomputing each account's own baseline date independently and only reporting a single `wholeAccountGainPeriodStart` when every contributing account genuinely shares that instant (`wholeAccountGainPeriodStartStatus: "COMMON"`); when they differ, the field is `null` (`"MIXED"`) and `wholeAccountGainOldestPeriodStart`/`wholeAccountGainNewestPeriodStart` expose the honest range instead. The ending side reused the already-correct Section 1 account-value snapshot range (from the first corrective patch's Issue 5 fix) and only gained the missing `wholeAccountGainPeriodEndStatus` to distinguish "mixed" from "unavailable" — its own timestamp/range fields and semantics were not reopened.
- **Aggregate account-value timestamps never claim a false common instant** (Astra corrective patch) — for a multi-account aggregate, `currentAccountValueAsOf` is null unless every contributing account's snapshot genuinely shares the same instant; `currentAccountValueOldestSnapshotAsOf`/`currentAccountValueNewestSnapshotAsOf` expose the honest range instead (e.g. account A snapshotted at 9am, account B at 11am — the combined total is never presented as if it existed at one single instant).
- **Friendly availability reasons are centralized** in `friendlyReportingReason` (`reporting.ts`) — the only function that should ever translate an internal status/reason code into user-facing copy; no page should independently invent or reinterpret this wording, and a fee-specific message is used only for the genuinely fee-specific status. Raw enum names (e.g. `INCOMPLETE_UNVERIFIED_SCHWAB_HISTORY`) must never reach the UI directly.
- **No UI changes were made in Ticket 1 or its two corrective patches** — Dashboard/Tracker/Account computed their own figures directly from `accountLedger.ts`/`performance.ts`/`brokerPositions.ts` (including several pre-existing duplicate renderings of whole-account gain/return across pages, flagged in the design-pass report as needing consolidation). **Ticket 2 wired Dashboard, Ticket 3 wired Tracker's own trade-focused figures, and Ticket 4 wired Account** — all three now consume this one contract; the Reporting Phase's UI separation is complete.
- **Reporting Phase, Ticket 2 — Dashboard "How am I doing?"**: Dashboard (`src/app/(app)/dashboard/page.tsx`) now calls `summarizeAccountReporting` once per request and renders five primary cards from it — Account Value, Confirmed Trading P/L, Return on campaigns closed this week, LST Capital Committed, Whole-Account Gain — in a `sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5` responsive section under a "How am I doing?" heading. A compact amber banner ("Some performance metrics are unavailable until account history is fully verified.") appears above the cards only when a major metric is unavailable for evidence reasons (never for the neutral "no campaigns closed this week" case). Removed as duplicated/superseded: the old "Account value" stat (Card 1 now owns this), the old "Trading Cash Flow" stat (a broader cash-flow figure — closed + still-open option premium — judged too easily confused with the new precise "Confirmed Trading P/L" card and removed rather than relabeled; a future ticket can reintroduce a clearly-labeled open/unrealized figure if wanted), and the "Trade-return goal reporting is being rebuilt..." placeholder (superseded by the real Trade Return card). Kept, deliberately not touched: the win/loss record (moved to a secondary line under the five cards, no longer implying it was the goal metric itself), and the pre-existing "Secured (CSP)"/"Broker positions" stats (`DashboardBrokerStats`) — these answer a genuinely different reconciliation question (campaign collateral *plus* untracked/ambiguous Schwab positions) from Card 4's strategy-scoped "LST Capital Committed" (tracked campaigns' put collateral + assigned-share basis only), so both were kept with their existing distinct labels rather than merged. `completedForPerformance` (the Dashboard's completed-campaign mapping) gained a `collateralCommitted` field it had always omitted — without it, every campaign looked like unknown collateral to `summarizeThisWeek`/`summarizeAccountReporting`, which would have made Card 3 permanently show `INCOMPLETE_CAPITAL_EVIDENCE` even with fully known collateral. The presentation-only formatting helpers (which snapshot-timestamp wording to show, which friendly reason to surface, never any P/L/return/capital/gain math) live in **`src/lib/reporting-display.ts`** (relocated there in Ticket 3 — see below — from an original home under `dashboard/`, once Tracker needed the exact same functions) - kept out of any page.tsx itself so they're unit-testable without touching a Next.js special file's restricted export surface; see `reporting-display.test.ts`.
- **Reporting Phase, Ticket 3 — Tracker cleanup**: Tracker (`src/app/(app)/positions/page.tsx`) now also calls `summarizeAccountReporting` (always the viewer's own accounts/campaigns, never scope-filtered - matching every other `own*` computation already on that page) and reuses `src/lib/reporting-display.ts`'s same formatting functions Dashboard uses, so the two pages can never independently drift on wording for the same field. Top-of-page summary now shows "Confirmed Trading P/L (mine)" instead of a locally-summed "Known realized P/L"; the Performance tab's "Performance Cockpit" panel lost seven account-level metrics (Account Value, Starting Capital, Total Gain, Total Return, Trading Cash Flow, Other Income/Expense, Net Contributions - all now Dashboard's/Account's job), keeping only the trade-focused Closed Record / Current-Open P&L / Projected OTM P/L, plus a pointer link to Dashboard for whole-account figures. The "Trade-Return Goal ... being rebuilt" placeholder was replaced by a real "Return on campaigns closed this week" card. `getTrackerPageData` (`app-data.ts`) split its old single `includePerformanceCampaigns` flag into `includeOwnCampaigns` (now defaults `true` on every view, since the top summary needs it) and `includeOptionMarks` (still Performance-tab-only, since that's the only view needing live option marks) - a small, deliberate query-cost increase (one extra bounded `campaign.findMany`, no option-mark lookup) on every Tracker page load, not a schema or formula change. Also fixed in this ticket: `CampaignCard`'s (Open/History tabs) "Realized"/"Return" fields were inline page-local math (`plValue = summary.totalCampaignPL ?? summary.realizedPL`, `returnOnSecuredCapital = (plValue / summary.collateralCommitted) * 100`) that didn't gate on unknown-cash-flow evidence the way `summarizeCampaignProgress` already does - replaced with `summarizeCampaignProgress`'s own `realizedPL`/`realizedPLStatus`/`currentReturnPercent` (safe to call with no option-mark data at all for a CLOSED campaign, since `currentCostToClose` only affects OPEN-campaign fields). The Performance tab's per-campaign table (`CampaignPerformanceTable`) was rebuilt from a fixed 9-column CSS grid requiring a 980px minimum width (guaranteed horizontal-scroll on any phone) into a details-card list matching `CampaignCard`'s existing wrap-friendly pattern. Campaign-level "Goal Need" was found to be the retired 1%-per-week concept leaking into campaigns (see the 1%-target invariant above) and removed. Section 12 permitted exactly one Dashboard change in this ticket: see `currentAccountValueUnavailableReason`/`currentAccountValueUnavailableMessage` below.
- **`AccountReportingSummary` gained `currentAccountValueUnavailableReason`/`currentAccountValueUnavailableMessage`** (Reporting Phase, Ticket 3, Section 12) — a dedicated, truthful answer to "why is `currentAccountValue` null," derived with zero new accounting semantics from already-computed evidence: it can only ever be `"NO_BASELINE"` or `"NO_CURRENT_VALUE"` (never a funding-coverage reason, which by construction requires a current value to already exist - see `totalReturnStatusFor`'s ordering in `accountLedger.ts`). Dashboard's Card 1 previously borrowed `wholeAccountGainUnavailableMessage` for this (safe, since the same two-value constraint always held, but scoped to answer a different question) - it now reads this dedicated field directly instead.
- **Reporting Phase, Ticket 4 — Account "evidence & auditability"**: Account (`src/app/(app)/account/page.tsx`) answers "can I trust the account-performance evidence," distinct from Dashboard's "how am I doing" and Tracker's "what are my trades doing." Per visible account it now calls `summarizeAccountReporting` (single-account, no `fallbackTradingPL` - this page never shows Confirmed Trading P/L/Trading Cash Flow, that stays Dashboard's/Tracker's job) for the evidence-gated current value/whole-account-gain/return, and `summarizeAccountPerformance` directly for the lower-level ledger/baseline facts (`ledger.effectiveBaseline`, `fundingCoverageStatus`, `netContributions`) `AccountReportingSummary` doesn't itself re-expose - exactly the "existing authoritative account-ledger/baseline domain helpers" the ticket named alongside the reporting contract. Per-account card hierarchy: header (name/source/visibility/current value with its Section-12 dedicated unavailable reason) → "Performance measurement" (Whole-Account Gain, Whole-Account Return - shown only when `wholeAccountReturnPercent` is non-null, not merely when gain is OK - measurement start/end, one compact status badge) → "Baseline" (value/date, explicit vs. provisional wording, revision count, the unchanged Ticket-9 set/correct form with `expectedRevisionId` and the correction-reason requirement fully preserved) → expandable baseline history (current vs. superseded, correction reason with the internal `Replaces STARTING_VALUE <id>` prefix stripped) → expandable "Funding & ledger history" (every non-baseline ledger entry, friendly type label, Schwab/Manual source badge from the entry's own persisted `source` column, correctly-signed amount) → the unchanged Manual-only deposit/withdrawal/adjustment form. Removed as Dashboard-owned duplication: Starting/Trading Cash Flow/Other income/Total gain/Total return's old raw-`AccountPerformanceSummary` rendering, replaced by the evidence-gated contract fields; `getAccountPageData` (`app-data.ts`) dropped its `completedCampaigns` query entirely (it existed solely to compute the now-unused `fallbackTradingPL`). Presentation-only helpers live in `src/lib/account-evidence-display.ts` (same page.tsx-export-restriction pattern as `reporting-display.ts`) - never a raw `FundingCoverageStatus`/`WholeAccountGainUnavailableReason` enum reaches the UI, and a Schwab account's funding coverage is never labeled "Verified" (the architecture gap - no persisted per-account Schwab transaction-coverage proof exists yet - means it can only ever read `INCOMPLETE_UNVERIFIED_SCHWAB_HISTORY` in production, translated honestly via the centralized `friendlyReportingReason`). Audited and confirmed unchanged, already compliant: rolls/assignment/covered-call display (out of this ticket's scope entirely - Account has none of that), Manual Adjustment's server-side gating on Schwab accounts (see Known Active Blockers-adjacent finding below).
- **Manual Adjustment on a Schwab account was audited, not newly decided** (Reporting Phase, Ticket 4, Section 14) — `addAccountLedgerEntryForUser` (`workflows.ts`) checks `account.source === "SCHWAB"` and throws BEFORE ever inspecting the requested entry `type`, so DEPOSIT, WITHDRAWAL, and MANUAL_ADJUSTMENT are all three blocked identically for a Schwab account server-side; the page's own form-gating (`account.source === "MANUAL" ? <form> : <note>`) already matched this exactly before this ticket touched it. No ambiguity existed and no new rule was invented - confirmed and documented in place.

---

## Current Accepted Roadmap

High-level sequence only — implementation detail for a ticket belongs in its own commit/PR/handoff note, not here:

1. **Ticket 1 — Current-leg parity between campaigns and Performance** — **COMPLETE**, commit `f68849f`. Astra-approved with a non-blocking follow-up (resolved by Ticket 4, see below).
2. **Ticket 2 — PWA privacy/cache verification** — **NOT REPRODUCED** (the suspected authenticated-content-caching risk did not reproduce under real browser testing, including an adversarial poisoned-cache simulation); no code change made; no Astra review required.
3. **Ticket 3 — Documentation cleanup** — **COMPLETE** (this restructuring).
4. **Ticket 4 - Profit completeness** - **IMPLEMENTED; review fixes complete locally, not deployed.** Unresolved fees keep OPEN/CLOSED current, realized, projected, and premium figures provisional. Unknown CLOSED cash flows cannot become a confirmed result. Shared `summarizePerformanceMetrics` checks CLOSED realized completeness and OPEN projected completeness for their actual contributions. Rows, totals, returns, and the weekly summary expose pending/incomplete coverage; missing evidence is not labeled N/A. Lifetime put collateral is labeled "Peak put collateral" and remains the historical return denominator.
5. **Ticket 5 - Current marks and exposure** - **IMPLEMENTED; review fixes complete locally, not deployed.** `resolveCurrentCostToClose` matches the authoritative current put by account, underlying, PUT, inclusive +/-0.005 strike, UTC expiration date, and exact short quantity. Latest account/contract observation is selected before quantity validation; contradictions, undated/future observations, and equal-time conflicts cannot fall back to an older match. Missing provider values remain null; verified broker zero is distinct from zero/zero or invalid quotes. Session freshness uses America/New_York dates, rejecting unknown/future price provenance and honoring the NYSE Saturday New Year exception. LAST_SESSION is visible in normal rows and summaries. Unchanged newer POSITION syncs refresh observation metadata without creating events; concurrent older updates cannot supersede newer observations, missing replacement valuations clear the old price, and same-time economic conflicts mark the stored observation unusable. Dashboard links require account-specific exact current obligations before excluding live exposure. Assigned-share exposure uses remaining basis after sales; missing OPEN collateral and assigned basis have explicit coverage counts, separate from covered-call counts.
6. **Ticket 6 - Scanner truthfulness (one authoritative readiness classifier)** - **IMPLEMENTED; not deployed.** `classifyReadiness`/`isActionableReadiness` (`scanner.ts`, see Scanner section above) replace several independent, disagreeing "near"/"pass" computations (Scanner page header counts, `ScannerWorkspace`'s own filter chips, the live-scan "near matches" toast, Dashboard's Top Setups) with one shared function. Fixes a real inconsistency: a gating-key near-miss (e.g. price just outside its band) used to count as "near" in the page header's raw `getNearMisses(...).length === 1` check but correctly not in the workspace's own smarter heuristic - both now agree. Dashboard's Top Setups now filters to `isActionableReadiness` (PASS/NEAR) before ranking, so a never-option-assessed or otherwise-incomplete row can no longer be promoted there just because it carries a high historical score. Not touched: Scanner UI layout, the 7-tier `SetupScoreLabel` grading scale, option-chain budget/provider limits, the Alpha Vantage fundamentals-refresh queue's own internal near-tier heuristic (a separate, non-user-facing prioritization concern - left as a documented, intentionally out-of-scope gap), Research's own score/label display, and the Covered Call scanner mode.
7. **Ticket 7 - Scanner settings must preserve data mode and update atomically** - **IMPLEMENTED; not deployed.** An audit found saving/resetting Scanner settings unconditionally invoked the demo scanner (`rerunDemoScannerForUser`), so changing a rule while viewing live results silently replaced them with a new demo `ScanRun`. Fixed: `updateScannerSettingsForUser`/`resetScannerSettingsToLstCoreForUser` (`workflows.ts`) no longer run any scan - see Scanner section above for the persistence/disclosure fix. Also fixed a related atomicity gap: the rule-by-rule save loop parsed and persisted one rule at a time, so an invalid field partway through a submission (e.g. rule 7 of 15) could leave rules 1-6 already saved with their new values while rejecting the rest. Now the entire proposed rule set is parsed/validated synchronously before any write, then persisted in one `$transaction` - reject-all-or-save-all, no partial state either way.
8. **Ticket 8 - Selected-contract evidence and price basis made explicit** - **IMPLEMENTED; not deployed.** An audit found Scanner could show a mark/midpoint-based "Premium" next to a bid-based "ROR" with nothing distinguishing the two, and no explicit signal for whether a retrieved quote was fetched while markets were closed. Fixed in `candidateValues` (`live-scan.ts`, see Scanner section above): `premiumBasis`/`rorBasis`/`bidProceedsPerContract` make the bid-vs-mark distinction explicit ("Premium at bid" is now a real, separate field from the mark/midpoint `premium`); `earningsWithinHoldingPeriod` compares the selected contract's own DTE against earnings distance without inventing an earnings date; `optionQuotedAt` stays explicitly null (no provider here has ever supplied a verified per-quote pricing timestamp) while `retrievedAt`/`retrievedOnNonTradingDay` disclose only the retrieval fact actually known. Every field still traces to exactly one selected `OptionContractSnapshot` (unchanged, already-correct structural guarantee). Minimal UI additions only (two column tooltips, one new expanded-row datum, one retrieval-timing note) - no layout redesign. Ticket 6's classifier and Ticket 7's settings/data-mode behavior were not modified.
9. **Ticket 9 - Account Baseline & Funding Boundaries** - **IMPLEMENTED, including three rounds of Astra corrective patches; not deployed.** See Financial / Accounting Invariants above (including the known architecture gap around Schwab funding-evidence persistence) and the verification notes below.
10. **Reporting Phase, Ticket 1 - Reporting domain contract** - **IMPLEMENTED, including two rounds of Astra corrective patches; not deployed.** See the Reporting Domain section below.
11. **Reporting Phase, Ticket 2 - Dashboard "How am I doing?"** - **IMPLEMENTED; not deployed; no Astra review yet.** Dashboard is now the first UI consumer of the Reporting domain contract. See the Reporting Domain section above and verification notes below.
12. **Reporting Phase, Ticket 3 - Tracker / Positions performance cleanup** - **IMPLEMENTED; not deployed; no Astra review yet.** Tracker is now the second UI consumer of the Reporting domain contract, and reuses Dashboard's own presentation helpers (`src/lib/reporting-display.ts`). See the Reporting Domain section above and verification notes below.
13. **Reporting Phase, Ticket 4 - Account "evidence & auditability"** - **IMPLEMENTED; not deployed; no Astra review yet.** Account is now the third and final UI consumer of the Reporting domain contract - Dashboard/Tracker/Account all consume `summarizeAccountReporting` today, completing the Reporting Phase's UI separation. See the Reporting Domain section above and verification notes below.
14. **Post-Reporting-Phase visual/UX polish** - **IMPLEMENTED; not deployed.** Presentation-only cleanup based on production screenshots of Tickets 2-4's work - no financial logic, no accounting semantics, no schema change. See the Reporting Domain section above and verification notes below for the four changes (Dashboard's evidence banner, Tracker's cash-flow wording, Account's Brokerage Connections layout, Account's baseline call-to-action) plus two reconnaissance findings recorded in Known Active Blockers/Issues (Recommendations/Chat/Notifications consolidation; Scanner's incomplete-candidate score presentation).
15. Complete wheel valuation/period reporting - will also be what promotes an ASSIGNED campaign's `currentPLStatus` from INCOMPLETE to CONFIRMED/PENDING (Ticket 4) and gives `assignedShareCapital` (Ticket 5) a real live valuation instead of cost basis only.
16. **Persisted per-account Schwab transaction-coverage evidence ? IMPLEMENTED LOCALLY; not pushed/deployed.** See Persisted Schwab Funding Coverage. Migration/DB verification remains pending.
17. **Navigation / Communication Cleanup - IMPLEMENTED; not deployed.** The Recommendations→Chat and Notifications→bell recon findings above were both acted on - see the "Primary Navigation" section and verification notes below. Settings/Admin consolidation remains future scope.
18. Scanner redesign/readability/mobile/history/benchmarks - the one narrow presentation gap found by this phase's recon (the NEEDS_DATA score-chip deemphasis) has since been fixed as its own micro-ticket; see Known Active Blockers/Issues. Broader Scanner redesign/readability/mobile/history/benchmarks work remains future scope - Scanner logic/scoring itself has never been touched by any reporting-phase ticket.

**Ticket 4/5 fix verification (base `189ce58`, local only):** 309/309 focused tests and 943/943 non-DB tests passed (67 files passed; 39 files / 442 tests skipped by the existing gates). TypeScript, ESLint on changed TypeScript files, production build, and `git diff --check` passed. Direct mocked query-boundary tests cover owner isolation and same-contract cross-account exposure; mocked persistence tests cover observation-only refresh, unavailable replacement prices, conflicts, and concurrency guards. Focused security review confirmed user/account scoping and no new order, credential, schema, ledger, or CampaignEvent write paths. No historical accounting or lifecycle formulas changed. No push or deploy.

**Verification still required:** local Docker daemon is unavailable and localhost:5432 refuses connections, so DB integration tests were not run. Browser login/visual verification and live Schwab timestamp verification were not performed. Run affected DB suites when disposable local Postgres is available, then authenticated owner/account and valuation-display checks before deployment. These fixes do not add a partial-fill lifecycle engine, stock mark-to-market, or exceptional one-off exchange closures.

**Valuation provenance limitation:** Schwab's current account-position adapter has no verified provider valuation timestamp and emits `valuationAsOf: null`; legacy `OptionContractSnapshot.capturedAt` is insertion time, not proven market price time. Observation and verified valuation time are separate metadata fields; no schema migration was needed. Verify actual Schwab timestamp fields and units before treating that source as verified. The NYSE [calendar](https://www.nyse.com/trade/hours-calendars) confirms Saturday January 1, 2028 has no observed holiday; December 31, 2027 stays open.

**Ticket 4/5 follow-up - honest broker-snapshot provenance (base `b4472c1`, local only):** An independent review found the valuation-provenance decision above over-corrected: since no production code path ever sets `valuationAsOf`, `resolveCurrentCostToClose` had become permanently unable to return a value at all, for every open campaign. Fixed by introducing a `CurrentCostToCloseSource.provenance` field distinguishing `VERIFIED_MARKET_TIMESTAMP` (a real, dated pricing time - the only case that reports `freshness`/session recency) from `BROKER_SNAPSHOT` (a valid, unambiguous, identity-matched Schwab position or cached quote fetched with no verified pricing time - useful numerically, but `freshness` is always null and the label always says "unverified pricing time"). `campaignCurrentPLStatus` (`performance.ts`) gained a `costToCloseVerified` flag so a snapshot-sourced value is capped at PENDING even with fully known fees - economic completeness (CONFIRMED/PENDING) and market-data provenance stay conceptually separate, never conflated just because a number exists. The UI (`positions/page.tsx`) shows "Schwab snapshot - fetched X ago. Broker did not provide a verified valuation timestamp." for this case, distinct from the verified "As of ..." wording. All prior Ticket 4/5 validation rules (missing/invalid quotes, quantity contradictions, equal-time conflicts, future timestamps, cross-account matching, strike tolerance) are unchanged. 946/946 non-DB tests, TypeScript, ESLint, production build, and `git diff --check` passed; DB integration tests and live Schwab/browser verification were not performed (same environment limitation as above).

**Ticket 5 corrective patch - stale-evidence fallthrough and retrieval-recency gating (base `662985e`, local only):** Astra found two resolver defects in the broker-snapshot fallback above. **Bug 1 (blocker):** a PRESENT `valuationAsOf` that failed verified-pricing validation (stale, or contradictory - dated after its own `observedAt`/`capturedAt`) was incorrectly still eligible for the `BROKER_SNAPSHOT` fallback, letting known-bad pricing evidence masquerade as a usable snapshot (repro: pricing time Sep 16, retrieval Sep 21). Fixed: the fallback now requires `valuationAsOf` to be genuinely absent (`== null`); a present-but-invalid claim is rejected outright with no fallback for that same value, on both the linked-position and cached-quote branches - an independent source (e.g. a cached quote when the linked position's own pricing claim is bad) may still supply a value. **Bug 2 (should-fix):** the broker-snapshot recency gate reused `classifyMarkFreshness` on the retrieval timestamp, which requires the retrieval's own calendar day to itself be an NYSE trading day - incorrectly rejecting a fresh weekend/holiday retrieval while accepting an older weekday one. Fixed with a new `isRecentRetrieval` (`marketCalendar.ts`), a retrieval-only recency window (this session-or-the-one-before, judged by elapsed calendar days) that never requires the retrieval day itself to have traded and never returns a session label. `costToCloseVerified`'s `true` default (Astra's non-blocking note) was left unchanged and documented in place - flipping it would require touching ~30 unrelated test call sites for no behavior change at the one real production caller, which already passes it explicitly. 961/961 non-DB tests (15 new), TypeScript, ESLint, production build, and `git diff --check` passed; DB integration and live Schwab/browser verification were not performed (same environment limitation as above). Diff range for focused Astra follow-up review: `662985e..HEAD` (this commit).

**Ticket 6 verification (base `6753e91`, local only):** 977/977 non-DB tests (16 new, all in `scanner.test.ts`), TypeScript, ESLint on changed files, production build, and `git diff --check` passed. No dashboard/scanner page-level UI test harness exists in this repo (no page.tsx has direct tests anywhere) - `classifyReadiness`/`isActionableReadiness` are covered by a full truth table at the domain layer, including an end-to-end simulation of Dashboard's exact filter-then-sort-then-slice Top Setups pipeline proving a never-option-assessed row with a deceptively high score is correctly excluded. No schema/migration change; no changes under `src/domain/finance`; no new provider calls; `OPTION_CHAIN_ENRICHMENT_LIMIT` and bounded-concurrency behavior untouched (verified via the unchanged, still-passing `live-scan`/`option-enrichment` test suites). DB integration tests and live-provider/browser verification were not performed (same environment limitation as above).

**Ticket 7 verification (base `ce33da9`, local only):** 991/991 non-DB tests (14 new: `scanner-settings.test.ts` mocked-Prisma unit tests plus 3 new `profile.test.ts` cases), TypeScript, ESLint on changed files, production build, and `git diff --check` passed. Mocked-Prisma tests prove: a settings save/reset issues zero `scanRun`/`scanResult` writes; every rule upsert plus the `ScannerProfile.updatedAt` touch happen inside one `$transaction`; an invalid field throws before the transaction opens (zero writes attempted); a mid-transaction upsert failure never reaches the profile touch; Matt's and Eric's saves/resets never cross-reference the other's `profileId`; the explicit "Refresh Demo Scan" action is unaffected and still creates a `DEMO` run. True atomic rollback of already-issued upserts inside a real Postgres transaction is a DB guarantee a mock cannot itself exercise - not verified end-to-end (see below). Ticket 6's `classifyReadiness`/`isActionableReadiness` were not modified (confirmed via `git diff` - `scanner.ts` does not appear in this ticket's changed-file list). No schema/migration change; no changes under `src/domain/finance`; no new or additional provider calls (a settings save now triggers zero scans, whereas before it triggered one demo evaluation - a strict decrease, never an increase). DB integration tests (including the existing `workflows.integration.test.ts` "updates My LST scanner settings for one user without changing the buddy profile" test, whose assertions remain structurally compatible with this change) and live-provider/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket).

**Ticket 8 verification (base `06a83cb`, local only):** 1012/1012 non-DB tests (21 new in `live-scan.selected-contract-evidence.test.ts`), TypeScript, ESLint on changed files, production build, and `git diff --check` passed. Tests prove: bid vs. mark/midpoint stay distinct with ROR always bid-based; a winning contract's expiration/strike/bid/OI/volume/delta never leak a losing candidate's values (different strike and different expiration cases both covered); zero/NaN bid and NaN ask are structurally excluded the same way; a wide spread still selects the contract honestly as a gating FAIL rather than hiding the number; earnings-within-holding-period is computed only when both the earnings date and the contract's own DTE are genuinely known (and requires disabling the pre-existing earningsDistance gate to reach the code path at all under LST Core defaults - documented in the test); a weekend retrieval is flagged via a coarse NYSE-trading-day check reusing `marketDate`/`isNyseMarketDay` (newly exported, unmodified) rather than duplicating calendar logic; `optionQuotedAt` stays null and distinct from `retrievedAt` in every case; all four chain states (assessed/failed/never-requested-by-budget/no-suitable-contract) keep their existing, unmodified `optionEnrichment`/`contractReasonCode` distinction and feed the untouched Ticket 6 `classifyReadiness` correctly; the option-chain call count remains capped at `OPTION_CHAIN_ENRICHMENT_LIMIT` with no new provider methods called (no `getMarketHours` call was added - the trading-day check is pure calendar math on the already-known `asOf`). No schema/migration change; no changes under `src/domain/finance` beyond exporting one already-existing pure function; Ticket 6 (`scanner.ts`) and Ticket 7 (`workflows.ts`, `scanner-settings.test.ts`, `profile.ts`) files do not appear in this ticket's changed-file list. DB integration tests and live-provider/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket).

**Astra corrective patch on Tickets 6-8 (base `bf99d50`, local only):** Astra's review of Tickets 6-8 found two defects, both fixed narrowly. **Issue 1 (blocker):** `classifyReadiness` treated `optionEnrichment === "ENRICHED"` as sufficient proof a contract was assessed, but ENRICHED also covers CHAIN_UNAVAILABLE/NO_PUT_CONTRACTS/no-suitable-strike (a request was spent, but no contract was chosen) - reproduced with only the stock-level "price" rule enabled, where no option criterion existed to catch this via UNKNOWN propagation, so these rows read PASS and became Dashboard-actionable. Fixed by adding a `contractReasonCode` parameter to `classifyReadiness`: PASS/NEAR now require it to be absent (a contract was actually selected), independent of which rules are enabled; every structural no-contract reason now reads NEEDS_DATA (or FAIL under gating precedence) instead. **Issue 2 (should-fix):** the Ticket 7 `resultsPredateCurrentSettings` mechanism compared `ScannerProfile.updatedAt` against `ScanRun.createdAt` (a persist-time value) - a scan that read old rules, then finished and persisted AFTER a settings save that landed mid-scan, would look newer than the save and incorrectly show no warning. Fixed with `withSettingsRevision` (`workflows.ts`), which stamps each persisted result with the settings revision captured at scan-read time (before rules are even fetched), stored in the existing `snapshotJson` (no schema change, no versioning subsystem) and compared against the current `ScannerProfile.updatedAt` instead of `ScanRun.createdAt`. Both fixes preserve every existing invariant (known-failure precedence, PASS/NEAR completeness requirements, Ticket 7 atomicity/isolation/no-scan-on-save, bounded provider usage - the option-chain call count is unchanged). 1033/1033 non-DB tests (21 new: 7 in `scanner.test.ts`, 7 end-to-end in `live-scan.selected-contract-evidence.test.ts`, 1 in `scanner-settings.test.ts`, 6 in `profile.test.ts`), TypeScript, ESLint, production build, and `git diff --check` passed. Astra's third, non-blocking note (header counts include all results while tabs may exclude NEVER_TRADE via the Research filter, causing legitimately different numbers) was deliberately left unfixed per Astra's own explicit instruction not to force visually identical numbers across differently-filtered views - recorded below as a documented, non-blocking follow-up. DB integration tests and live-provider/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket). A narrow, acknowledged residual: `profile.updatedAt` is read microseconds before the `ScannerRule` rows in `rerunLiveSchwabScannerForUser`/`rerunDemoScannerForUser` (two sequential, unsynchronized queries) - a save landing in that specific sub-millisecond gap could theoretically still be missed. Closing that fully would require serializable-transaction isolation around rule reads, judged disproportionate to the risk for this patch.

**Ticket 9 verification (base `60d02f2`, local only):** 1066/1066 non-DB tests (33 new: 18 in `accountLedger.test.ts` covering effective-baseline tie-break, the NY-end-of-day conversion, correction/append-only audit, measurement-interval inclusion/exclusion, and funding-authority/mixed-source detection; 11 mocked-Prisma tests in a new `accountBaseline.test.ts` covering the Schwab funding-authority block, baseline creation, correction, and stale-revision rejection; 4 source-text regression tests in a new `retired-one-percent-displays.test.ts` proving the two old 1%-target displays no longer render), TypeScript, ESLint on changed files, production build, and `git diff --check` passed. Writing the `endOfNyCalendarDateUtc` tests caught a real ~1ms offset bug (computing the NY UTC offset from a guess instant that already carried `.999ms` lost that fraction asymmetrically through `Intl.DateTimeFormat`'s second-level truncation) - fixed by computing the offset from a whole-second guess and adding the trailing 999ms back afterward. No schema migration: `id`/`createdAt`/`notes` already existed as real `AccountLedgerEntry` columns, already returned by every existing Prisma query. All 19 pre-existing `accountLedger.test.ts` tests pass unchanged - the legacy same-day/same-amount broker-transfer-baseline-inference matching (`isSameManualStartingTransfer`) is preserved unmodified and only gated to run when no explicit baseline exists. DB integration tests and live-provider/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket). Not done in this ticket (explicitly out of scope): benchmark/opportunity-cost, TWR, XIRR, a new trade-return engine, capital-utilization history, `AccountSnapshot` scheduling, or a Dashboard/Scanner redesign - the next performance ticket can proceed independently once accepted.

**Astra corrective patch on Ticket 9 (base `ddeab4b`, local only):** Astra's review of the Account Baseline & Funding Boundaries ticket found five blockers and four should-fix items, all addressed narrowly within `accountLedger.ts`/`workflows.ts`/`positions/page.tsx`, none requiring a schema migration or broader redesign. **Blockers:** (1) a superseded (corrected-away) `STARTING_VALUE` revision's date/amount could still suppress a genuine later broker deposit via the legacy same-day/same-amount dedup - fixed by restricting that dedup to only the currently-effective baseline revision. (2) valuation and funding used two different measurement-interval boundaries - a post-`endingAt` manual deposit could leak into `currentValue` via the ledger's own unbounded contribution total even though this function's own contribution sum correctly excluded it, and a `BROKER_SNAPSHOT` dated before the baseline could serve as an "ending valuation" and produce a fabricated negative gain - fixed by computing one shared, validated ending valuation (`snapshotIsValidEnding`) that every other figure derives from. (3) funding coverage could report `COMPLETE` from the mere absence of a detected problem rather than affirmative evidence - a Schwab account's synced transaction history only ever covers a trailing 90-day window (`SCHWAB_TRANSACTION_LOOKBACK_DAYS`), so "no transfer found" outside it is not proof none occurred; added `INCOMPLETE_UNVERIFIED_SCHWAB_HISTORY` for this case. (4) an inferred (never explicit) baseline could still produce a confirmed dollar gain - it can never prove it captured original funding, so gain is now withheld unconditionally for any non-`COMPLETE` funding-coverage status, not just the mixed-sources case. (5) a dated baseline's reconstructed value could still add undated, lifetime CAMPAIGNS-sourced trading P/L on top of it, double-counting profit already folded into the baseline - refused entirely when no Schwab snapshot exists to verify against. **Should-fix:** (6) the baseline-correction transaction now runs at Serializable isolation, with a genuine Postgres write-conflict (P2034) translated into the existing user-safe "reload and try again" message. (7) `endOfNyCalendarDateUtc` now validates real calendar components (rejecting e.g. `2026-02-30`, which `Date.UTC` previously silently normalized into March) and rejects a still-future baseline instant. (8) a second 1%-target display missed in the first retirement pass (`positions/page.tsx`'s "Performance vs 1% Target" chart, `PerformanceGoalChart`) is now removed. (9) a baseline correction (not the very first baseline) now requires a non-empty reason. 1090/1090 non-DB tests (24 new: 21 in `accountLedger.test.ts` covering the superseded-baseline fix, the shared measurement interval, Schwab-lookback coverage, the lifetime-P/L refusal, and expanded date validation including both 2026 DST transition days; 5 mocked-Prisma tests in `accountBaseline.test.ts` covering the required correction reason, Serializable isolation, and P2034 translation; 1 source-text regression in `retired-one-percent-displays.test.ts`), plus updates to 9 pre-existing `accountLedger.test.ts` assertions that had encoded the now-fixed bugs (each documented in place with an "Astra corrective patch" comment explaining why the expected value changed), TypeScript, ESLint on changed files, production build, and `git diff --check` passed. DB integration tests and live-provider/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket); the Serializable-isolation fix in particular could not be exercised against a real concurrent Postgres write-conflict, only proven via a simulated P2034 error in the mocked-Prisma test. No schema migration; no changes to campaign history, Scanner, or scope beyond the nine numbered issues.

**Second Astra corrective patch on Ticket 9 (base `91dcb62`, local only):** Astra's second review round found three further blockers, all fixed narrowly within `accountLedger.ts` alone, no schema migration. **Issue 1:** the FIRST corrective patch had already restricted the legacy same-day/same-amount transfer dedup to only the currently-effective baseline revision, but the dedup mechanism itself remained unsafe against even the correct revision - a baseline's `occurredAt` is an America/New_York end-of-day instant that lands on the *next* UTC calendar date, so a genuine, coincidentally same-amount deposit the following NY afternoon (exact repro: $10,000 baseline at end of Sep 1, a genuine $10,000 deposit Sep 2 afternoon) could share that UTC date and get wrongly suppressed, producing a fabricated 100% return. Fixed by removing the date/amount dedup entirely - only the measurement interval's exact timestamp boundary now decides whether a transfer is a contribution, matching the ticket's explicit rule that a baseline's own timestamp is the sole authority. **Issue 2:** funding coverage could reach `COMPLETE` for a Schwab-evidenced account purely because the measurement interval fit inside the 90-day lookback window - but `syncSchwabAccountForUser` writes the account/balance `BROKER_SNAPSHOT` unconditionally and then fetches/persists transactions as later, independently-failable steps, so a short interval never actually proved the transaction fetch or its persistence succeeded (exact repro: a $10,000 baseline, a Schwab snapshot showing $12,000, and a silently-failed transaction fetch behind it - previously reported `COMPLETE`/`gain: $2,000` with zero funding evidence). Investigated the Schwab sync/import persistence model first, as directed, and found **no existing persisted evidence** capable of proving per-account, per-interval transaction coverage - see the "Known architecture gap" note above. Fixed by adding `BrokerTransactionCoverageStatus`/`AccountPerformanceInput.brokerTransactionCoverageStatus`, an explicit caller-supplied proof that defaults to absent; fitting inside the lookback window remains necessary but is no longer sufficient. **Issue 3:** a whole-account ending valuation could be reconstructed from option-premium/trading cash flows even when no real account-value reading existed at all (exact repro: a $10,000 baseline plus a $75 Sell to Open premium, no broker snapshot, previously produced `currentValue: $10,075`/`gain: $75`) - wrong because receiving option premium also creates an offsetting short-option liability, so cash received is not the same fact as whole-account value changed. Fixed by restricting the MANUAL-reconstruction fallback to only fire when NO trading activity occurred in the interval at all (`tradingPL === null`) - a valid Schwab `BROKER_SNAPSHOT` dated at/after the baseline is now the only way to establish a supported ending valuation whenever any trading occurred. 1103/1103 non-DB tests (25 new across two new describe blocks in `accountLedger.test.ts` covering all three issues' required regressions, including the exact Astra reproductions; 4 more inline in the existing Issue-1 describe block), plus updates to 9 pre-existing `accountLedger.test.ts` assertions that had encoded the now-fixed bugs (including rewriting one original-ticket test's fixture to use a realistic end-of-day baseline timestamp instead of an artificial midnight-UTC one, and replacing one test that exercised the now-removed dedup outright), TypeScript, ESLint, production build, and `git diff --check` passed. DB integration tests and live-provider/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket); the funding-coverage-evidence mechanism in particular could only be verified structurally (via directly-constructed domain-level test fixtures), since no real Schwab sync producing genuine per-account evidence could be exercised. No schema migration. The non-blocking item Astra also noted (campaign-level "Goal Need" still contains a 1%-based comparison, `summarizeContributionAdjustedGoal` remains called) was investigated and found NOT trivially removable - the account-level `ownGoal` it produces still feeds the legitimate, unrelated "Projected OTM P/L" metric card, and "Goal Need" itself is computed entirely from per-campaign `summarizeCampaignProgress` output, not from `ownGoal` - recorded below as a genuine non-blocking follow-up rather than acted on, per the ticket's explicit instruction not to expand into campaign-level decision-aid redesign.

**Third Astra corrective patch on Ticket 9 (base `1929b09`, local only):** Astra found one remaining unsupported whole-account valuation path, fixed narrowly within `accountLedger.ts` alone, no schema migration. The second corrective patch's Gate A still let `startingCapital + contributions` stand in as the ending valuation whenever `tradingPL === null`, on the reasoning that "no trading activity" meant contributions were the only possible value driver - Astra found this unsafe, since `tradingPL === null` only proves this function found no dated broker option records and no campaign fallback; it proves nothing about interest, fees, dividends, or unrecorded changes to pre-existing holdings. Exact repro: a $10,000 baseline with $25 of recorded interest and no ending snapshot previously produced `currentValue: $10,000` / `totalGain: $0` / `totalReturnPercent: 0%` / `totalReturnStatus: OK`, silently discarding the $25. Fixed by removing that fallback branch entirely - the ending-valuation logic in `summarizeAccountPerformance` is now exactly two cases: a valid Schwab `BROKER_SNAPSHOT` dated at/after the baseline, or unavailable (`currentValue`/`totalGain`/`totalReturnPercent` all `null`, `totalReturnStatus: "NO_CURRENT_VALUE"`). No other reconstruction path remains. 1110/1110 non-DB tests (7 new in a dedicated `accountLedger.test.ts` describe block covering the exact interest repro plus fees/contributions/no-evidence-at-all variants, the still-valid snapshot path, snapshot-before-baseline rejection, and an aggregate-level case; 3 pre-existing tests updated in place where they had asserted the now-removed fallback's output), TypeScript, ESLint, production build, and `git diff --check` passed. DB integration tests and live-provider/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket). While auditing the DB-integration suite for anything this specific fix could affect, found (but did not touch, since it predates this patch and is outside its stated scope) a pre-existing staleness in `accountLedger.integration.test.ts`'s "derives a Schwab starting baseline and split performance from deduped broker transaction records" test: it relies on an INFERRED (not explicit) baseline and asserts a confirmed `totalGain`, which the FIRST corrective patch's Issue 4 fix (inferred baselines never produce confirmed gain) and SECOND patch's Issue 2 fix (Schwab coverage now requires explicit `brokerTransactionCoverageStatus` proof) would already make fail if that suite could run - flagged here since local Docker/Postgres unavailability meant it was never caught by CI in this environment; a future ticket touching that suite should update its assertions (or supply `brokerTransactionCoverageStatus`/an explicit baseline) to match the now-current rules. No unsupported whole-account gain path remains in `accountLedger.ts` after this patch.

**Reporting Phase, Ticket 1 verification (base `731de12`, local only):** Created `src/domain/finance/reporting.ts` (`summarizeAccountReporting`) - see the "Reporting Domain" section above for the full contract. No files were modified, only two new files added (`reporting.ts`, `reporting.test.ts`); no page/UI changes, no accounting semantics changes, no schema migration. 1138/1138 non-DB tests (28 new in `reporting.test.ts`, covering all 30 numbered requirements from the ticket - several requirements share one test where the underlying behavior is structurally identical, e.g. "projected OTM excluded" and "open mark-to-market excluded" are both proven by the same fact that this module never reads anything but `CompletedCampaignResult[]`), TypeScript, ESLint, production build, and `git diff --check` passed; `accountLedger.test.ts`/`performance.test.ts`/`brokerPositions.test.ts` re-run to confirm zero regressions to the modules this wraps. DB integration tests and live/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket in this engagement); this ticket's own module has no DB-dependent behavior to verify differently once one is available. One deliberate judgment call, flagged for confirmation: Current Capital Utilization's numerator includes `assignedShareCapital` (assigned-but-unsold shares' cost basis) alongside `securedPutCollateral`, reasoning that assigned shares are capital at work, not idle cash - the ticket's wording ("current committed/secured capital") was slightly ambiguous on whether to include this; both components are exposed as separate sub-fields so a future ticket can easily narrow this if the product owner prefers CSP-only utilization.

**Astra corrective patch on Reporting Phase Ticket 1 (base `536d8b3`, local only):** Astra's review found one blocker and five should-fix items in the reporting contract, all addressed. **Blocker:** the trade-return denominator silently treated a campaign with unknown `collateralCommitted` as requiring $0 capital while its P/L still counted in the numerator, inflating the ratio (exact repro: two $100-gain campaigns, one with $1,000 collateral and one unknown, wrongly returning 20%) - fixed in the AUTHORITATIVE shared calculation itself (`summarizeThisWeek`, `performance.ts`), not papered over in `reporting.ts` alone, by adding `securedCapitalFullyKnown` and withholding both return percentages whenever it's false. **Should-fix:** (1) an incomplete campaign result was being mislabeled as a fee problem (`PENDING_FEE_EVIDENCE`) purely because the existing `netPLExact` field conflates "a fee is unresolved" with "a result is entirely unknown" - fixed by having `reporting.ts` check for an incomplete result FIRST, using already-exposed counts, without changing `netPLExact`'s own meaning (other callers of `summarizeThisWeek` are unaffected). (2) unknown exposure could report a clean "0%/OK" utilization - fixed by adding `UNKNOWN_EXPOSURE` as its own status, withholding the percentage while still showing the known committed-capital dollar floor. (3) period metadata is now explicit per metric (exact ISO week bounds for Trade Return, a clarifying basis sentence for Confirmed Trading P/L's `ALL_TIME` label, baseline-start/ending-valuation instants for Whole-Account Gain) - see the Reporting Domain section above. (4) a multi-account aggregate's `currentAccountValueAsOf` no longer claims one false common instant - it is null unless every contributing snapshot genuinely shares the same instant, with a new oldest/newest range pair exposed instead. (5) `friendlyReportingReason`'s message table was updated for every new/changed status, keeping fee-specific wording exclusive to the one genuinely fee-specific case. (6, cosmetic) a stale comment describing the already-removed "starting capital + contributions" account-value fallback was corrected. 1153/1153 non-DB tests (2 new in `performance.test.ts` for the `securedCapitalFullyKnown`/week-bounds fix at its source, 14 new across four new describe blocks in `reporting.test.ts` covering every required regression, plus updates to existing tests for the renamed/split status values), TypeScript, ESLint, production build, and `git diff --check` passed. DB integration tests and live/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket). No schema migration; no UI/Dashboard work began.

**Second Astra corrective patch on Reporting Phase Ticket 1 (base `cfc8384`, local only):** Astra found one aggregate period-metadata defect - `wholeAccountGainPeriodStart` reused `accountLedger.ts`'s aggregate `startingCapitalAt`, which arbitrarily reflects only the first account found in the input array, making the reported period start depend on array order for two accounts with genuinely different baseline dates (exact repro: `[A, B]` reported A's Aug 1 baseline, `[B, A]` reported B's Sep 1 baseline, for the identical $200 combined gain). Fixed entirely within `reporting.ts` - see the Reporting Domain section above for the full `PeriodBoundStatus`/oldest-newest design. No change to `accountLedger.ts`, no change to the underlying gain calculation itself (confirmed by test: the $200 figure and `OK` status are identical regardless of input order both before and after this patch) - this was purely a reporting-metadata correction. 1158/1158 non-DB tests (5 new in a dedicated `reporting.test.ts` describe block proving input-order invariance for both the start and end period bounds, plus 2 existing tests strengthened with the new status/range assertions), TypeScript, ESLint, production build, and `git diff --check` passed. DB integration tests and live/browser verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket). No schema migration; no UI/Dashboard work began; no other reporting-contract fields touched.

**Reporting Phase, Ticket 2 verification (base `44a6ace`, local only):** Wired `src/app/(app)/dashboard/page.tsx` to `summarizeAccountReporting` - see the Reporting Domain section above for the full card-by-card design and what was removed/kept. Changed files: `src/app/(app)/dashboard/page.tsx` (replaced the duplicated `summarizeAccountPerformance`/`summarizeAccountsPerformance`-based aggregate and the old Account-value/Trading-Cash-Flow stats and rebuild-in-progress placeholder with the five-card section; added `collateralCommitted` to `completedForPerformance`; reused the existing `campaignExposureInputs` array for both the pre-existing `summarizeCampaignExposure` call and the new `openExposure` input; extended `Stat` with an optional `reason` line), new `src/app/(app)/dashboard/reporting-cards.ts` (the pure presentation helpers, exported for testability), new `src/app/(app)/dashboard/reporting-cards.test.ts` (30 tests), and `src/app/(app)/retired-one-percent-displays.test.ts` updated in place (the Dashboard placeholder-copy assertion it depended on no longer applies - replaced with an assertion that the placeholder is gone and the win/loss record remains). 1188/1188 non-DB tests (30 new dashboard tests plus 2 updated retired-1%-display assertions), TypeScript, ESLint, production build, and `git diff --check` passed. Deliberate judgment calls, flagged for confirmation: (1) "Trading Cash Flow" was removed rather than relabeled open/unrealized - it blends closed and still-open option cash flow (not a clean current/MTM figure) and risked being read as a second, competing "am I making money" number next to the new Confirmed Trading P/L card; (2) the existing "Secured (CSP)"/"Broker positions" stats were kept as-is (different, reconciliation-scoped concept, not a reporting-contract duplicate) rather than merged with Card 4. One minor reporting-contract gap noted, not requiring a fix: `AccountReportingSummary` has no dedicated `currentAccountValueUnavailableReason` field - Card 1 reuses `wholeAccountGainUnavailableMessage` when `currentAccountValue` is null, which is safe today because that null state can only correspond to `wholeAccountGainUnavailableReason` being `NO_BASELINE`/`NO_CURRENT_VALUE` (never a funding-coverage reason, which by construction requires a current value to already exist - see `totalReturnStatusFor`'s ordering in `accountLedger.ts`), but a future reporting-contract change to that ordering would need this page-level assumption revisited. DB integration tests and live browser/mobile verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket), and no browser-driving tool was confirmed available either. Not done in this ticket (explicitly out of scope, no financial formulas changed): benchmark/opportunity-cost, TWR/XIRR, the 1% weekly goal, Tracker or Account redesign, Scanner, and no schema change. Diff range: `44a6ace..HEAD`. Tracker Ticket 3 can proceed independently.

**Reporting Phase, Ticket 3 verification (base `19b8992`, local only):** Wired `src/app/(app)/positions/page.tsx` to `summarizeAccountReporting` and reused Dashboard's `src/lib/reporting-display.ts` helpers - see the Reporting Domain section above for the full design and what was removed/kept. Changed files: `src/app/(app)/positions/page.tsx`, `src/lib/app-data.ts` (`includePerformanceCampaigns` split into `includeOwnCampaigns`/`includeOptionMarks`), `src/domain/finance/reporting.ts` + `reporting.test.ts` (Section 12's `currentAccountValueUnavailableReason`/`Message`), moved `src/app/(app)/dashboard/reporting-cards.ts`(`.test.ts`) → `src/lib/reporting-display.ts`(`.test.ts`) with one-line Dashboard import update, `src/app/(app)/retired-one-percent-displays.test.ts` updated (Tracker's placeholder-copy assertion replaced; Goal Need regression added), new `src/app/(app)/positions/tracker-reporting-cleanup.test.ts` (15 tests). 1207/1207 non-DB tests, TypeScript, ESLint, production build, and `git diff --check` passed. Judgment calls, flagged for confirmation: (1) `getTrackerPageData` now always loads the viewer's own open+closed campaigns (a plain, bounded query) on every Tracker view, not just Performance, so the top summary bar can show authoritative figures everywhere - the pricier live option-mark lookup stays Performance-tab-only; (2) Tracker's Accounts tab kept its per-account Starting/Current/Trading Cash Flow/Other Income/Contributions (account-management bookkeeping context, judged distinct from "how is my portfolio doing"), removing only "Total gain"/"Total return" (literally Dashboard's Whole-Account Gain/return concepts, without its evidence-gating) and added a pointer link to Dashboard; (3) "LST Capital Committed" appears as one small contextual line in the Performance tab (not a primary card), consistent with the ticket's "if useful as trade context, don't force all five" guidance. `CampaignCard`'s (Open/History tabs) previously page-local `returnOnSecuredCapital`/`plValue` computation was replaced with `summarizeCampaignProgress`'s authoritative `realizedPL`/`realizedPLStatus`/`currentReturnPercent` - a genuine, in-scope fix (Section 4's explicit "do not calculate inline if the domain already supplies it"), safe with zero new data dependencies since a CLOSED campaign's realized figures don't need option-mark data. Rolls (Section 6), assignment/covered-call (Section 7), and open-position attention (Section 8) were audited and found already compliant with existing Ticket 5/9 work - no changes made there. DB integration tests and live browser/mobile verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket), and no browser-driving tool was confirmed available either. Not done in this ticket (explicitly out of scope, no financial formulas changed): benchmark/opportunity-cost, TWR/XIRR, historical/average utilization, the future generalized 1% weekly goal, Scanner, Dashboard redesign, and no schema change (Section 12's Dashboard change was the one explicitly permitted exception). Diff range: `19b8992..HEAD`. Account Ticket 4 can proceed independently.

**Reporting Phase, Ticket 4 verification (base `221f4fa`, local only):** Wired `src/app/(app)/account/page.tsx` to `summarizeAccountReporting` (per-account, evidence-only) plus `summarizeAccountPerformance` directly for baseline/ledger detail - see the Reporting Domain section above for the full per-account hierarchy and what was removed/kept. Changed files: `src/app/(app)/account/page.tsx`, `src/lib/app-data.ts` (`getAccountPageData` dropped its now-unused `completedCampaigns` query), new `src/lib/account-evidence-display.ts` (the presentation-only helpers, same pattern as `reporting-display.ts`) + `account-evidence-display.test.ts` (27 tests). 1234/1234 non-DB tests, TypeScript, ESLint, production build, and `git diff --check` passed. This completes the Reporting Phase's UI separation - Dashboard, Tracker, and Account all now consume `summarizeAccountReporting` for their respective jobs, with zero page-local whole-account-gain/return/baseline/funding-coverage formulas remaining on any of the three. One genuine, in-scope fix made along the way: the manual deposit/withdrawal/adjustment form's `returnTo` hidden field pointed at `/positions` (a latent bug - an error would have redirected to Tracker instead of back to the Account page where the form lives) - corrected to `/account` while rebuilding this exact form block. Section 14's audit (Manual Adjustment on a Schwab account) found no ambiguity to resolve: `addAccountLedgerEntryForUser` already blocks all three manual entry types identically for Schwab accounts (the `account.source === "SCHWAB"` check runs before the type check), exactly matching the page's existing form-gating - documented in place, no rule invented. No new accounting semantics and no persisted Schwab transaction-coverage evidence were added (both explicitly out of scope) - a Schwab-evidenced account's funding coverage still honestly reads `INCOMPLETE_UNVERIFIED_SCHWAB_HISTORY` via the centralized `friendlyReportingReason`, never a false "Verified" claim. DB integration tests and live browser/mobile verification were not performed - local Docker/Postgres remain unavailable in this environment (same limitation as every prior ticket), and no browser-driving tool was confirmed available either. Diff range: `221f4fa..HEAD`.

**Post-Reporting-Phase visual/UX polish verification (base `5027f13`, local only):** Presentation-only cleanup of production screenshots from Tickets 2-4, no financial/accounting logic touched. (1) Dashboard's page-level evidence banner (`dashboardHasEvidenceGap`, `reporting-display.ts`) now requires 2+ distinct evidence gaps rather than any single one - the single most common production case (Whole-Account Gain alone unavailable due to the documented Schwab-funding-coverage architecture gap) no longer shows a redundant generic banner on top of Card 5's own already-clear reason; a genuinely systemic gap (e.g. `currentAccountValue` itself null, which always also makes Whole-Account Gain unavailable - two cards) still shows it. (2) Tracker's top-summary "Net option premium" stat is now labeled "Option Cash Flow" with detail text "Credits minus debits - cash flow, not realized profit"; the two most prominent per-campaign collapsed-row cash-flow labels (`CampaignCard`'s Open-view "Net premium" and History-view bare "Premium") were similarly renamed to "Option Cash Flow"/"Cash Flow" - Confirmed Trading P/L's own label/wiring is unchanged, and `netOptionPremium`/`premiumTotal` computation is byte-for-byte unchanged (wording only). (3) Account's "Brokerage Connections" panel no longer has a `lg:grid-cols-[1fr_0.85fr]` two-column split with a large mostly-empty right column for the Disconnect button - the primary Connect/Reconnect/Disconnect action moved to a compact control beside the connection header, and the sync/credential/connection-detail panels now use the full reclaimed width (their `dl` grids gained an `lg:grid-cols-*` breakpoint); all four original action branches (Reconnect+Disconnect, Disconnect-only, Connect-only, waiting-on-developer-app) and every existing OAuth/sync/credential action are unchanged. (4) Account's missing-baseline copy gained "Choose when you want performance tracking to begin, then enter the whole account value at the end of that date." alongside the existing, unchanged "Account value at the end of the selected date." form caption - never implies original account funding, no baseline prefill/guessing added. Two read-only reconnaissance items (Recommendations/Chat/Notifications consolidation; Scanner's incomplete-candidate score presentation) were investigated and recorded in Known Active Blockers/Issues below - neither was implemented, per the ticket's explicit stop condition. Changed files: `src/lib/reporting-display.ts` + `.test.ts`, `src/app/(app)/positions/page.tsx` + `tracker-reporting-cleanup.test.ts`, `src/app/(app)/account/page.tsx` + `account-evidence-display.test.ts` (6 files, no new files). 1249/1249 non-DB tests (15 new/changed), TypeScript, ESLint, production build, and `git diff --check` passed. DB integration tests and live browser/mobile verification were not performed - local Docker/Postgres remain unavailable in this environment, and no browser-driving tool was confirmed available either. No schema change; no financial formula changed; no benchmark/TWR/XIRR/1%-goal work begun; Scanner logic untouched. Diff range: `5027f13..HEAD`.

**Scanner score-chip micro-ticket verification (base `26dd60e`, local only):** Implemented the one recommendation from the polish ticket's Scanner recon (see Known Active Blockers/Issues) - `scoreChipClass` (`scanner-workspace.tsx`) now accepts `result.readiness` and returns the existing muted gray treatment whenever `readiness === "NEEDS_DATA"`, checked first, ahead of every pre-existing label/score branch (which are otherwise untouched, so a pre-existing "Verify"-labeled-but-FAIL-readiness case keeps its exact prior behavior). Both call sites (the desktop table row and the mobile `CandidateCard`) updated to pass the new argument. Changed files: `src/app/(app)/scanner/scanner-workspace.tsx`, new `src/app/(app)/scanner/scanner-score-presentation.test.ts` (10 tests, importing `scoreChipClass` directly - it is now exported since `scanner-workspace.tsx` is a plain "use client" component, not a Next.js special file, so no export restriction applies). 1259/1259 non-DB tests (10 new), the full Scanner domain suite (`scanner.test.ts`, `live-scan.selected-contract-evidence.test.ts`) passing unmodified (proving `classifyReadiness`/`isActionableReadiness`/ranking/filters/provider calls are untouched), TypeScript, ESLint, production build, and `git diff --check` passed. `score`'s numeric value is unchanged (still rendered as `{result.score}` verbatim); no eligibility/actionability behavior changed. DB integration tests and live browser/mobile verification were not performed - local Docker/Postgres remain unavailable in this environment, and no browser-driving tool was confirmed available either. Diff range: `26dd60e..HEAD`.

**Navigation/Communication Cleanup verification (base `c5b2968`, local only):** Implemented both recon findings from the polish ticket - see the "Primary Navigation" section above for the full design. Changed files: `src/app/(app)/app-sidebar.tsx` (trimmed `navItems` to six destinations, added `NotificationBellLink`, simplified mobile nav to a 3-column grid, exported `navItems` for direct testing), `src/app/(app)/chat/page.tsx` (fetches buddies, renders the new panel), new `src/app/(app)/chat/chat-recommend-panel.tsx`, `src/lib/app-data.ts` (new `getChatBuddies`, scoped identically to the existing Recommendations buddy query), `src/app/(app)/actions.ts` (`recommendStockAction` now also revalidates `/chat`), `src/app/(app)/dashboard/page.tsx` (Recommendations card's "Open" link now points to `/chat`), new `src/app/(app)/nav-cleanup.test.ts` (15 tests). No schema change (confirmed via `git diff --stat -- prisma/schema.prisma`, empty); no financial/Scanner logic touched (confirmed via `git diff --stat`, only the five files above changed); `getChatPageData`'s existing return shape was deliberately left untouched since two DB-integration test files (`notifications-chat-cleanup.integration.test.ts`, `chat-attachments.integration.test.ts`) call it directly and assert on that exact shape. 1274/1274 non-DB tests (15 new), TypeScript, ESLint, production build (confirms `/recommendations` and `/notifications` both still compile as live routes, not removed), and `git diff --check` passed. `/recommendations` and `/notifications` are unchanged pages, still fully reachable (Chat's panel links to the former; the new bell links to the latter) and still valid `RETURNABLE_PATHS`/`Notification.href` targets - neither route was deleted or redirected. DB integration tests (including the existing `notifications-chat-cleanup.integration.test.ts`, which already covers the recommendation-echo/notification-visibility behavior this ticket reuses unchanged) and live browser/mobile verification were not performed - local Docker/Postgres remain unavailable in this environment, and no browser-driving tool was confirmed available either. Diff range: `c5b2968..HEAD`.

## Known Active Blockers / Issues

- **Stale DB-integration test assertion (found during the third Ticket 9 corrective patch, not fixed - out of scope for that patch):** `accountLedger.integration.test.ts`'s "derives a Schwab starting baseline and split performance from deduped broker transaction records" test asserts a confirmed `totalGain` from an INFERRED baseline with no `brokerTransactionCoverageStatus` supplied - both would now correctly return `null` per the first two Astra corrective patches on Ticket 9. Never caught because local Docker/Postgres has been unavailable in this environment throughout Ticket 9's development. Needs updating (or an explicit baseline + coverage proof) whenever this suite is next touched with a working database.

- **Funding-evidence migration pending locally and in production:** `20260923120000_account_funding_sync` must be applied and verified before deploying dependent code. Local Docker daemon and PostgreSQL port 5432 are unavailable; real FK/CHECK constraints, persistence/rollback/concurrency, and owner-query DB integration tests remain unverified.
- Web Push delivery is a documented no-op (manifest/service-worker/install prompt exist; VAPID keys and the actual push pipeline are not built).
- No CI-automated Supabase migration deploy — still a manual runbook (migrate locally → apply to Supabase → verify → push dependent code).
- `LST_SESSION_SECRET` env var name predates the "Off Shift Options" rebrand — cosmetic, low priority, requires a coordinated Hostinger env var rename.
- CSP allows `script-src`/`style-src` `'unsafe-inline'` (Next.js App Router requirement without a nonce setup) — a possible future hardening, not currently required.
- Research's PEG ratio, Debt/Equity, and Current Ratio remain manual-only — confirmed genuinely absent from Schwab's Trader API; P/E, EPS, and dividend amount/yield **are** auto-populated from Schwab where a live connection exists.
- Eric's research spreadsheet has not been imported (not yet provided; a dry-run-first importer design exists in the archive for when it is).
- Live Schwab scanner uses a bounded starter universe by design, not yet a broad market scan — see Roadmap item 10.
- **Non-blocking (Astra, Tickets 6-8 review):** the Scanner page's header counts (pass/near/unknown) are computed over ALL results, while `ScannerWorkspace`'s own filter-chip counts are computed over the Research-filtered "actionable" subset (NEVER_TRADE excluded by default) - these can legitimately show different numbers when NEVER_TRADE-tagged tickers are present. Both use the identical `classifyReadiness` answer (Ticket 6's fix), so this is a display-scope difference, not a classification disagreement. Deliberately not changed now per explicit instruction not to force visually identical numbers across differently-filtered views - a future pass should either scope both to the same set or add a one-line clarification of why they can differ.
- **RESOLVED (micro-ticket after the post-Reporting-Phase polish ticket) - Scanner's numeric score chip previously did not deemphasize itself for an incomplete/NEEDS_DATA candidate.** `scoreChipClass` (`scanner-workspace.tsx`) now takes `result.readiness` as a parameter and returns the same muted gray treatment already used for `scoreLabel === "Verify"` whenever `readiness === "NEEDS_DATA"` - checked first, ahead of every label/score branch, so a stock-screen-only candidate (options never assessed, every known stock-level criterion legitimately passing with a high score) no longer shows a bright, confident-looking chip next to its "STOCK SCREEN ONLY"/"VERIFY" status word. Presentation only: `score`'s numeric value, `classifyReadiness`/`isActionableReadiness`, ranking, filters, and provider calls are all unchanged - confirmed by the full Scanner domain suite passing unmodified. `scoreChipClass` is now exported from `scanner-workspace.tsx` (a plain "use client" component, not a Next.js special file, so no export restriction applies) so it can be unit-tested directly; see `scanner-score-presentation.test.ts`.
- **RESOLVED (Navigation/Communication Cleanup ticket) - Recommendations/Chat/Notifications consolidation.** The read-only recon above (SAFE TO CONSOLIDATE) was acted on - see the "Primary Navigation" section for the implemented design. No Web Push wiring targeted either route (the push provider is a documented no-op), so this carried no push-specific risk, as predicted.

## Where Documentation Lives

| Topic | File |
|---|---|
| Full chronological history, superseded next-steps, detailed subsystem narratives | `docs/PROJECT_HANDOFF_ARCHIVE.md` |
| Earliest project history (Phase 1 foundation through first production deploy) | `docs/HANDOFF.md` |
| Runtime architecture, data flow, testing layers, deployment detail | `docs/ARCHITECTURE.md` |
| Schema/model detail | `docs/DATA_MODEL.md` |
| Recorded architectural decisions | `docs/DECISIONS.md` |
| Domain/strategy detail (LST, rolls, statuses) | `docs/LST_DOMAIN.md` |
| PWA/notifications detail | `docs/PWA_AND_NOTIFICATIONS.md` |
| Scanner rule catalog and selection algorithm | `docs/SCANNER_RULES.md` |
| Schwab integration detail (OAuth, CSV, reconciliation) | `docs/SCHWAB_INTEGRATION.md` |
| Security/privacy detail | `docs/SECURITY.md` |
| Prior long-form roadmap phases (superseded by the roadmap above) | `docs/ROADMAP.md` |

Agent instructions: `AGENTS.md` (coding conventions, commands, gotchas) and `CLAUDE.md` (entry protocol). Both point here first.
