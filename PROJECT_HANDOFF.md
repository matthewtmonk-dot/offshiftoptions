# PROJECT_HANDOFF.md

This is the **canonical single source of truth** for the current state of Off Shift Options. It is not a diary — obsolete information is replaced or removed, not appended to. When repository state and this file disagree, investigate which is correct and fix this file.

---

# AI CONTINUITY RULES

1. Read this entire file before making project changes.
2. When Matt explicitly says **"Read PROJECT_HANDOFF.md"**: read this entire file, read `AGENTS.md` and `CLAUDE.md`, and inspect enough of the actual repository to confirm this file still matches reality.
3. If the project is understandable and no critical blocker requires Matt's input, respond with **only**: `Ready.` Do not summarize this file, do not recap the project, do not list next steps, do not explain what you read.
4. If there is a genuine contradiction, missing critical information, security concern, unresolved merge state, broken repository state, or other issue that makes it unsafe to begin work, do not say `Ready.` — give the shortest possible explanation of the blocker instead.
5. This `Ready.` behavior applies only to the explicit "Read PROJECT_HANDOFF.md" command. It does not mean saying "Ready." every time this file is edited during normal work.
6. Inspect actual repository state before relying on documentation — this file can go stale.
7. **Updating this file is part of the definition of done for every meaningful project change.** Matt should never have to ask. Before finishing a task, check whether it changed: application behavior, architecture, database/schema, authentication, authorization, deployment, environment variable requirements, hosting, domain configuration, external integrations, scanner functionality, market-data behavior, Schwab integration, security, PWA behavior, chat, notifications, production status, known issues, technical debt, test status, important project decisions, project phase, immediate next step, or the Git/deployment workflow. If yes, update this file before finishing. Minor typo-only/cosmetic changes don't require it.
8. Do not turn this file into an append-only diary. Remove stale information and replace it with current truth. Keep only the history that actually matters to a future developer/AI; Git has the rest.
9. Never put secrets, passwords, API keys, OAuth secrets, access/refresh tokens, session secrets, VAPID private keys, database connection strings, or other credentials in this file. Document environment variable **names** and purposes only.
10. Do not silently reverse the architectural decisions recorded below. If a requested change conflicts with one, explain the conflict before making a major reversal.
11. Never run destructive production database operations (`prisma migrate reset`, `db:reset`, the destructive `prisma/seed.ts`, anything using `deleteMany` as a reset) unless Matt explicitly understands and authorizes exactly what data would be lost.
12. **Database schema changes require special care.** Hostinger cannot reliably run `prisma migrate deploy` in production (see Production/Deployment). Never assume pushing a migration to `main` applies it. Do not push application code that depends on a new migration until that migration has been applied to Supabase and verified out-of-band, per the workflow in Production/Deployment.
13. Keep brokerage integration read-only. Never implement automated order submission merely because an API supports it, unless Matt explicitly authorizes changing that requirement.
14. **Do not blindly implement Matt's requested technical approach if a clearly better, safer, more secure, more maintainable, or more conventional solution exists.** Evaluate significant requests for security, data safety, maintainability, production reliability, deployment compatibility, privacy, and standard best practices. If there's a better approach, explain it and recommend it before implementing the weaker one. Default to the safer/better implementation unless Matt explicitly says he understands the tradeoffs and wants the original approach anyway.
15. Proactively protect against: committed secrets, insecure authentication, weak authorization, IDOR/cross-user access, destructive database operations, accidental production data loss, unsafe API credential handling, client-side exposure of private keys/tokens, insecure OAuth redirects/callbacks, weak session handling, missing input validation, unsafe file uploads, insecure dependency use, fragile deployment shortcuts, dev/production environment confusion, hard-coded production URLs/secrets, unnecessary public exposure of services, overcomplicated infrastructure, misleading financial data, and accidental trading/order execution.
16. Security-sensitive changes (authentication, authorization, database access, financial data, OAuth, brokerage APIs, deployment secrets, production infrastructure) require an explicit focused security review of the affected code before the task is done.
17. If a secret is ever found committed to Git, do not just delete it. Report the exposure (file, type, whether it's in history) and recommend proper credential rotation.
18. Never assume hiding a UI element provides authorization — authorization must be enforced server-side.
19. Never claim mock, cached, stale, demo, or manual financial information is live/current market data.

---

## Project Identity

- **Product name:** Off Shift Options
- **Users:** Private two-user application — Matt and Eric. No public signup exists or is planned.
- **Purpose:** A mobile-friendly app for two firefighter friends to run a disciplined, low-stress cash-secured-put options workflow: track trades/positions, scan candidates against personal criteria, share recommendations, chat, and build toward additional family income and long-term financial flexibility (including reduced dependence on firefighting work if health/life circumstances make that desirable).
- **This is NOT:** a brokerage, a public trading platform, a financial advisory service, or an automated trading bot.
- **`PRODUCT_VISION.md`** (repo root) is the long-term north-star document — product areas, principles, and future direction. It is distinct from this file: `PRODUCT_VISION.md` rarely changes and is not a changelog; `PROJECT_HANDOFF.md` is the operational current-state snapshot that changes with every meaningful task.

## Core Product Principles

- Mobile-first, installable PWA; desktop should still work well (especially scanner/table views).
- Simple, low-stress workflow; financial information should be understandable, not unnecessarily technical.
- `PRIVATE` data stays private to its owner; `SHARED` data is visible to both Matt and Eric per the authorization rules in `src/lib/privacy.ts`.
- Never fabricate live financial information. Demo/manual/mock data must always be visibly labeled as such.
- Future brokerage/market integration begins **read-only**. No order submission, no buy/sell, no account-changing brokerage actions, unless Matt explicitly changes this architecture later.
- Strong security and safe defaults are preferred over convenience shortcuts.
- When a better technical solution exists, recommend it rather than blindly following an inferior implementation request.

---

## Product Model: Scan / Research / Track / Performance

OSO has four connected jobs. Every feature belongs to (at most) one of these - if a change starts blurring the boundary between two, stop and reconsider before merging them:

1. **SCAN** - find stocks/options that meet the user's technical strategy criteria quickly. Purely technical: PASS/FAIL/UNKNOWN, setup score, near-match gap. Never influenced by personal opinion.
2. **RESEARCH** - the user's personal judgment of a ticker as a company/trade candidate (like it, want to own it, would never trade it, special personal rules). Purely personal: never changes what Scan computes.
3. **TRACK** - the append-only historical record of what actually happened (`Campaign`/`CampaignEvent`) - every roll, assignment, covered call, and outcome, preserved forever.
4. **PERFORMANCE** - whether the strategy is actually working over time. Account value and trading P/L come from `AccountLedgerEntry`; realized campaign P/L comes only from completed campaigns; current/mark-to-market and projected-OTM campaign P/L are separate, explicitly labeled views. Deposits/withdrawals never become trading profit, and open premium is never presented as realized profit.

The intended workflow: **Scan -> find technically interesting stocks -> Research the underlying -> Watch/Like/Exclude -> take the trade in Schwab/thinkorswim -> Track the campaign -> measure Performance.** Research *feeds* the Scanner (a researched ticker's status can widen the live scan universe, and a personal exclusion hides a candidate from the actionable list) but never *replaces* it — the Scanner must always be able to discover names the user has never researched. See the Scanner and Research sections below for how this is enforced in code.

**The single most important invariant of this model**: a personal Research judgment (Like/Watch/Avoid/Never Trade) must never change a Scan result's technical score, status, or label. Never trading a company you technically-strong-scoring stock doesn't make it a worse setup — it makes it a setup you've chosen not to act on. The UI communicates this by showing the same score both with and without personal filters applied ("Show Excluded" reveals a `94 · Excellent · EXCLUDED BY YOU` row, not a downgraded one) — see `tests/e2e/research.spec.ts`'s "preserves its exact score" test for the automated proof.

---

## Current Production Architecture

Verified from repository state (`package.json`, `Dockerfile`, `next.config.ts`):

| Layer | Current version/tool |
|---|---|
| Framework | Next.js 16.3.3, App Router, Turbopack |
| Language | TypeScript (strict), React 19.2.8 |
| ORM | Prisma 7.10.0 with `@prisma/adapter-pg` (`pg` 8.x driver) |
| Database | PostgreSQL — Supabase in production, `postgres:17-alpine` via Docker Compose locally |
| Styling | Tailwind CSS 4 |
| Package manager | pnpm 11.24.0 (via Corepack) |
| Testing | Vitest 4.1.11 (unit + opt-in DB integration), Playwright 1.62.1 (e2e) |
| Hosting | Hostinger managed Node.js hosting |
| Local containers | Docker Compose (`compose.yaml`) — `db` (Postgres) + `app` (Node 24 Alpine, see `Dockerfile`) |
| GitHub | https://github.com/matthewtmonk-dot/offshiftoptions.git, branch `main` |
| Production domain | https://offshiftoptions.com — connected and live (externally confirmed; see Hosting/Domain section) |

**Hostinger's actual configured Node.js version is externally managed** (Hostinger control panel) and not verifiable from this repository — there is no `.nvmrc`/`engines` field pinning it. The Docker image and local dev use Node 24.

---

## Production / Deployment

> **⚠ DATABASE SCHEMA CHANGES REQUIRE SPECIAL CARE.** Hostinger auto-deploys every push to `main`, but its managed Node environment cannot reliably execute Prisma's schema engine, so it does **not** apply migrations during deployment (see Migration strategy below). **Never push code to `main` that depends on a new migration until that migration has been applied to Supabase and verified**, per the workflow below. Never run `prisma migrate reset`, the destructive `prisma/seed.ts`, or any other destructive reset against production.

**Deployment model:**

```
GitHub main → Hostinger auto-deploy → Next.js app → Supabase PostgreSQL
```

- **Current permanent build command** (`package.json` → `"build"`): `prisma generate && next build` — confirmed as of the latest commit. It does **not** run `prisma migrate deploy`, does **not** seed, and does **not** bootstrap users. Every push to `main` triggers a Hostinger deploy.
- **Health endpoint:** `GET /api/health` → `{ app, database, latencyMs, checkedAt }` (200) or `{ app: "ok", database: "error", checkedAt }` (503). Never returns the raw database error message or connection details to the caller (logged server-side only).
- **Migration strategy (current, permanent):** Migrations are **not** run automatically by the Hostinger build. Root cause: `prisma migrate deploy` fails inside Hostinger's build environment with `schema-engine-debian-openssl-1.1.x EACCES` when it tries to *execute* the schema-engine binary as a subprocess (note: `prisma generate` succeeds — it doesn't need to execute that binary, only reference it). This points to Hostinger's managed environment restricting execution of arbitrary spawned binaries, not a missing `chmod +x` — a chmod fix was deliberately not attempted. Hostinger's build shell also does not support nested `pnpm run ...` calls from inside a package script.

  **Required workflow for any future schema migration:**
  1. Create/update the migration locally as usual (`pnpm db:migrate` against local Docker Postgres).
  2. Before pushing the schema-changing commit to `main` (a push deploys to Hostinger), apply it to Supabase from a machine that can actually execute the schema engine — a local dev machine or a CI runner both work:
     ```bash
     DATABASE_URL="<supabase-connection-string>" pnpm prisma migrate deploy
     ```
     Never hardcode or commit the Supabase `DATABASE_URL`.
  3. Verify it applied (check `_prisma_migrations` in Supabase, or `/api/health` after the next deploy).
  4. Only then push the code that depends on the new schema.

  Automating step 2 in CI is a reasonable future improvement (see Next Tasks), not yet built.

- **Known historical workaround (resolved, no longer present):** During the very first production deployment, the build script was temporarily changed several times (adding `prisma migrate deploy`, then a bootstrap step, then bypassing the migration engine, then calling `tsx prisma/bootstrap-production.ts` directly) to get Matt/Eric created against the brand-new Supabase database. All of that was reverted once the one-time bootstrap succeeded. **The current `build` script has none of this — verified directly against `package.json`.**

---

## Database / Seed / Bootstrap Safety

**This distinction is critical and must never be blurred:**

- **`prisma/seed.ts`** (`pnpm db:seed`) — **DESTRUCTIVE, development/demo only.** Its `resetDatabase()` unconditionally `deleteMany()`s nearly every table (users, sessions, trades, watchlists, chat, notifications, scanner profiles, etc.) before recreating demo data for Matt and Eric. **Must never run against Supabase/production.** There is no safeguard in the script itself preventing this other than discipline — never wire it into any production script or CI path.
- **`prisma/bootstrap-production.ts`** (`pnpm bootstrap:production`, logic in `src/lib/bootstrap.ts`) — **safe, non-destructive, idempotent.** Creates Matt (`matt@lst.local`) and Eric (`eric@lst.local`) only if a user with that email doesn't already exist, using `DEV_SEED_PASSWORD` for the initial password (only set at creation time — never overwrites an existing password), and creates their shared private conversation only if one doesn't already exist. It never deletes, resets, or overwrites existing users or any other data. Proven by DB-integration tests (`src/lib/bootstrap.integration.test.ts`): first run creates both users + conversation; a second run creates nothing new, doesn't touch the password, and doesn't delete unrelated data added in between.
- **Current status:** The initial production bootstrap has already run successfully against Supabase — Matt and Eric accounts exist in production and login has been verified. **`bootstrap:production` is not part of the routine build** and should not run on every deployment; it now exists mainly as a disaster-recovery/reference tool (e.g., manually re-run only if a brand-new empty production database is ever provisioned again).

**Never run against production:** `prisma migrate reset`, `db:reset`, `pnpm db:seed`, or any ad hoc `deleteMany`-based reset logic — unless Matt explicitly understands and authorizes exactly what data would be lost. If any AI is ever asked to run a destructive database command, it must first explain exactly what data would be lost and get explicit confirmation.

**Migration status:** `prisma/migrations/20260828215700_campaign_tracker_foundation/` (adds the Campaign Tracker schema — see Campaign Tracker section) is applied to both local Docker Postgres and **production Supabase** (Matt ran `prisma migrate deploy` against production on 2026-08-29 and confirmed with `prisma migrate status` — "3 migrations found, database schema is up to date").

**`prisma/migrations/20260831211006_account_ledger_and_broker_link/` is applied to both local Docker Postgres and production Supabase** (Matt ran `prisma migrate deploy` + `prisma migrate status` against production on 2026-08-31, confirmed up to date, before the dependent code was pushed). It is purely additive (two new enums `AccountLedgerEntryType`/`AccountSource`, a new `AccountLedgerEntry` table, three new nullable columns on `TradingAccount` — `source`, `brokerConnectionId`, `externalAccountId` — one default-value change on `TradingAccount.visibility` from `SHARED` to `PRIVATE` for new rows only, a new unique index, and two new FKs).

**`prisma/migrations/20260831235050_schwab_credential_routing_broker_records/` is applied to both local Docker Postgres and production Supabase** (shipped in commit `5cf6c0b`, part of the production baseline this project has built on ever since). It is additive: new enums for Schwab developer credential status, broker-record kind/source/status, import export-type, and import batch status; a nullable `BrokerConnection.developerCredentialId`; a new `SchwabDeveloperCredential` table for encrypted per-user developer app credentials; a new `BrokerRecord` table for normalized broker API/CSV records with provenance, deterministic duplicate protection (`fingerprint`), conflict detection (`identityKey`), review status, and optional links to an import batch (`importBatchId`) and a reconciled Campaign (`linkedCampaignId`); and a new `BrokerImportBatch` table tracking CSV import lifecycle/counts. This single migration was regenerated twice during development (each `prisma migrate reset` run only with the user's explicit per-invocation consent, per Prisma's own AI-safety guard against destructive commands) to stay one coherent migration for the whole feature.

---

## Environment Variables / Secrets

Names and purposes only — **never values**.

**PRODUCTION REQUIRED**
- `DATABASE_URL` — Supabase Postgres connection string.
- `LST_SESSION_SECRET` — HMAC key used to hash session tokens before storing/looking them up (name predates the "Off Shift Options" rebrand; not yet renamed — see Known Issues).
- `NEXT_PUBLIC_APP_URL` — used for `metadataBase` (canonical/Open Graph URL resolution). Already set in Hostinger to `https://offshiftoptions.com`, with a redeployment completed afterward (externally confirmed; not independently verifiable from this repo).

**DEVELOPMENT ONLY**
- `DEV_SEED_PASSWORD` — initial password used by `prisma/seed.ts` and (only for user creation) `prisma/bootstrap-production.ts`. Was required once in Hostinger's env for the one-time production bootstrap; not needed for routine deploys now that Matt/Eric already exist.
- `RUN_DB_TESTS` — set to `1` to opt into the Vitest DB-integration suites (skipped otherwise).
- `PLAYWRIGHT_BASE_URL` — overrides the Playwright base URL (defaults to `http://127.0.0.1:3000`).
- `NODE_ENV` — managed by Next.js itself (`next dev` forces development, `next build`/`next start` force production); not something to set manually.

**SCHWAB INTEGRATION** (read-only foundation implemented; Hostinger env vars configured per Matt on 2026-08-31; OAuth connection working for Matt before the current local schema-changing slice)
- `SCHWAB_CLIENT_ID` — server-side Schwab app client ID.
- `SCHWAB_CLIENT_SECRET` — server-side Schwab app client secret.
- `SCHWAB_REDIRECT_URI` — must match the registered production callback exactly: `https://offshiftoptions.com/api/schwab/callback`.
- `SCHWAB_TOKEN_ENCRYPTION_KEY` — server-side 32-byte AES-GCM key for encrypted Schwab access/refresh tokens and per-user Schwab developer app credentials. Recommended format is `base64:<32 random bytes in base64>`; generate locally with `node -e "console.log('base64:'+require('node:crypto').randomBytes(32).toString('base64'))"`.
- These are consumed only by server-side Schwab code (`src/providers/schwab/*`, `/api/schwab/connect`, `/api/schwab/callback`, and related workflows). Do not use `NEXT_PUBLIC_` for Schwab credentials, and never commit or paste values into project documentation. Per-user developer credentials are entered only through the authenticated `/account` form and stored encrypted server-side; the UI never displays stored secrets again.

**ALPHA VANTAGE INTEGRATION** (verification-only diagnostic implemented 2026-09-03; configured in the Hostinger production environment per Matt)
- `ALPHA_VANTAGE_API_KEY` — single shared server-side key for Alpha Vantage's REST API (company OVERVIEW fundamentals). Free tier: 25 requests/day total, shared across Matt, Eric, and Scanner - not per-user. Consumed only by `src/providers/alpha-vantage/*` and `src/lib/alpha-vantage-diagnostic.ts`. Never use `NEXT_PUBLIC_ALPHA_VANTAGE_API_KEY` - the browser must never see this key. See Alpha Vantage API section.
- `OSO_CRON_SECRET` — shared secret protecting `POST /api/internal/alpha-vantage/process` (the scheduled fundamentals-queue trigger, 2026-09-04). Server-side only, never `NEXT_PUBLIC_`. Sent by the caller as `Authorization: Bearer <secret>` (or `X-OSO-Cron-Secret`), compared with `crypto.timingSafeEqual`. Not yet set in Hostinger's production environment as of this slice - required before the scheduled trigger can be used (the manual Account button works regardless). See Alpha Vantage API section.

**FUTURE WEB PUSH** (not yet used anywhere in code)
- `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` — required once real Web Push delivery is implemented (see Known Issues / PWA section). Private key must stay server-only.

**Secrets scan result:** Tracked files plus the current new/untracked slice files were checked for high-confidence secret patterns (private keys, API tokens, real Schwab env assignments, non-local Postgres URLs). No real secrets found. `compose.yaml` and `.env.example` contain only hardcoded **local Docker dev** placeholder credentials (`lst_buddy` / `lst_buddy_dev`, pointing at `db:5432`/`localhost:5432`), which is the intended, safe convention for local development — not a production credential. No `.env` file is tracked (only `.env.example`).

---

## Authentication

- Email/password authentication with database-backed sessions (`src/lib/auth.ts`).
- Passwords hashed with `bcryptjs` (cost 10) — consistently in `prisma/seed.ts`, `prisma/bootstrap-production.ts`, and `src/lib/account.ts`.
- Session cookie: HTTP-only, `SameSite=Lax`, `Secure` when `NODE_ENV === "production"` (true for the deployed Hostinger app). Session tokens are opaque random values in the cookie, HMAC-hashed (`LST_SESSION_SECRET`) before being stored/looked up — the raw token is never persisted.
- A new session token is always minted on sign-in (never reused from an existing cookie) — no session-fixation vector.
- **Password changing exists**: `/account` (`src/lib/account.ts`) lets a signed-in user change their own password — requires the correct current password, enforces minimum length (10) plus letter+number complexity, and deletes every *other* session for that user on success (keeps the current one), so a changed password immediately signs out other devices.
- **No public signup.** Only Matt and Eric exist; this remains a private two-user app.
- All authorization is enforced server-side in `src/lib/privacy.ts` (`assertCanReadRecord`, `assertCanMutateRecord`) and applied through `src/lib/workflows.ts` — the only place server actions mutate data. UI hiding is never relied on for security.
- Dev-only quick-login buttons on `/login` render only when `NODE_ENV !== "production"` — confirmed absent from a real `next build && next start` production run.

---

## Current Features

| Feature | Status | Notes |
|---|---|---|
| Dashboard | WORKING (one-screen cockpit) | `/dashboard` — a compact status line (per-user greeting, scan provenance badge, open count, win rate), a 5-stat account strip (account value, cash, secured CSP capital, open positions, realized trading P/L), a weekly-return/1% target strip (honest `INSUFFICIENT HISTORY` state when there isn't enough closed-campaign history), and three compact panels (Open Positions, Top Setups sorted by honest setup score, deduped Buddy Activity). Rewritten from an earlier tile-based layout that had several contentless/"No data" cards; see Current State for details. |
| Scanner (results) | DEMO + CONTROLLED LIVE SCHWAB PATH | `src/domain/scanner` — real PASS/FAIL/UNKNOWN evaluation engine with setup scores, near-match detection, exclusion diagnostics, sortable/quick-filtered results, and expandable stock/option inspectors. Demo remains deterministic and visibly labeled. A new read-only `LIVE:SCHWAB` action can run a small staged live scan once Schwab env vars and OAuth are configured; it never silently falls back to demo data. Score/label are now gating-aware — see Scanner section. |
| My LST scanner settings ("Scanner Rules") | WORKING (per-user) | `/scanner/settings` (nav label "Scanner Rules") — each user edits their own `ScannerProfile`/`ScannerRule` rows against an explicit **LST Core** baseline, with a "Reset all to LST Core" action and a per-rule "Differs from LST Core" diff badge; proven isolated per-user by DB integration tests. |
| Account Ledger | WORKING | `/account` and Tracker's Accounts/Performance tabs — see Account Ledger section. Distinguishes external cash flows (deposits/withdrawals/adjustments) from trading P/L; never derives performance from `currentBalance − startingBalance`. |
| Research (formerly Watchlist) | WORKING | `/research` - personal LIKE/WATCH/NEUTRAL/AVOID/NEVER_TRADE status, private by default, dense sortable/filterable table, Would-Own/Monthly-Only/Roll-Friendliness/manual-grade fields, Scanner integration (badges, quick actions, Show Excluded, universe union). See Research section. `/watchlist` redirects here. Migration `20260901200601_research_model` is applied to production. Fundamentals remain intentionally unpopulated - see Research section. |
| Recommendations | WORKING | Send/receive between Matt and Eric, reason tags, status lifecycle (`NEW`/`WATCHING`/`PASSED`/`ARCHIVED`), participant-gated comments. |
| Buddy chat | WORKING (polling-live) | Membership-gated conversation; new messages/read-receipts appear without a manual reload via a 4s `router.refresh()` poll while the tab is visible (`src/components/live-refresh.tsx`) — not WebSockets/Realtime. |
| Notifications | WORKING (in-app only) | In-app notification model/UI/provider works. External Web Push is a documented no-op (see PWA section). |
| Tracker (accounts + campaigns) | WORKING (manual/demo + Schwab positions where connected) | `/positions` (nav label "Tracker") — four compact tabs (Open / History / Performance / Accounts) inside the existing page, not new nav entries. Full CSP/wheel campaign lifecycles (open → roll → assign → covered call → close) with SHARED/PRIVATE/INHERIT visibility and Mine/Eric/Both filtering. See Campaign Tracker section. |
| Legacy Trade/TradeLeg positions | DEMO/MANUAL | The original per-trade position model still renders (as "Legacy open CSP snapshots") below the campaign list on `/positions` for continuity; new work should use campaigns, not this model. |
| Cash-secured put calculations | WORKING | `src/domain/finance/calculations.ts` — real, tested per-share vs. per-contract math (premium capture, ROR, annualized ROR, break-even, position health status). Deterministic, not dependent on live data. |
| PWA / installation | WORKING | Manifest, service worker, install prompt, real 192/512 PNG icons (any + maskable) and Apple touch icon. |
| Health endpoint | WORKING | See Deployment section. |
| Authentication | WORKING | See Authentication section. |
| Change password | WORKING | `/account`. |
| Sharing/privacy controls | WORKING | `PRIVATE`/`SHARED` visibility enforced server-side; proven by unit + DB-integration tests. |
| Appearance (Dark/Light/System) | WORKING | Per-user Dark/Light/System theme, persisted on `UserSettings.appearance`; see Appearance System section. Migration `20260901031101_user_appearance_preference` is applied to production. |
| Schwab market/account data + CSV import/reconciliation | WORKING | Encrypted per-user Schwab developer app credentials, user-scoped market-data/personal-broker provider resolvers, provider-level market-data caching, a Schwab CSV import pipeline (positions/transactions/realized gain-loss, verified against real exports) with preview-before-persist and NEW/DUPLICATE/CONFLICT/NEEDS_REVIEW/INVALID classification, and a "Broker Activity Awaiting Review" reconciliation UI that links a broker position to a Campaign so the Dashboard counts it once. Verified end-to-end in a browser (upload -> preview -> confirm -> reconcile -> linked Campaign). Migration `20260831235050_schwab_credential_routing_broker_records` is applied to production. No trading/order-entry functionality exists. |

**Performance Phase A (implemented 2026-09-02):** duplicate private-route RSC prefetching is disabled with `prefetch={false}` on private `Link`s; Tracker tabs keep the same `/positions?scope=...&view=...` URLs but use a small client tab island for immediate selected/pending state; `/positions` now skips Schwab open-position/reconciliation/import/legacy/performance-only reads unless the active tab needs them; Scanner and Research status buttons update optimistically and roll back on server rejection; `setResearchStatusAction` revalidates only `/research` and `/scanner`; the normal Scanner page path no longer performs a duplicate `ScannerProfile.findFirst` before loading page data. No schema or migration change.

**Performance Phase B (implemented and validated 2026-09-03):** private-route navigation now uses an intent-prefetch wrapper instead of blanket automatic `Link` viewport prefetching, with short dwell-based hover/focus/touch prefetch and cross-desktop/mobile dedupe. `next.config.ts` sets App Router stale times (`dynamic: 60`, `static: 300`) so recent private-route returns reuse cached RSC payloads through roughly 30 seconds and revalidate around 60 seconds. The Dashboard renders stored campaigns/account ledger/snapshot data first and streams live broker-derived Schwab position stats/rows behind Suspense, with honest snapshot-age/refreshing/unavailable states and no fake zero broker values. Schwab read calls are cached only short term and only by provider/user/connection key (accounts/account/positions 15s, orders/transactions 30s); Sync Now bypasses and clears the cache, disconnect clears it, and provider errors are never cached as empty data. Broker reconciliation evidence loading was batched to remove an N+1 query while preserving behavior. Final safety checks confirmed normal browser link semantics and Matt-to-Eric private router-cache isolation. No Schwab fundamentals work, order-entry functionality, schema change, or migration was added.

**Performance + Campaign History redesign (implemented locally 2026-09-03, not yet committed/pushed):** Tracker History now shows every campaign state (`OPEN`, `ASSIGNED`, `CLOSED`) instead of hiding open/assigned campaigns behind a closed-only history view. Tracker Performance is now a dense current-performance cockpit: account value is ledger/broker-snapshot authoritative; trading P/L is contribution-adjusted; realized, current/mark-to-market, and projected-OTM campaign P/L are separated; open CSP marks come only from a linked current-user broker position or an exact cached option snapshot; unavailable marks stay unavailable rather than guessed. The 1% goal tracker uses dated capital exposure, so deposits/withdrawals change future target base but never count as returns. A reusable accessible `InfoTip` component was added and applied to specialized Performance/History/campaign labels so newer options users can understand realized/current/projected values, net premium, secured capital, rolls, mark source, and the 1% target without cluttering the dense UI. No Schwab fundamentals work, order-entry functionality, schema change, migration, scanner rule change, Research privacy change, broker import/reconciliation workflow change, or financial formula change was added.

---

## Scanner

- Engine: `src/domain/scanner/scanner.ts` — criterion-level evaluation. Each criterion produces `PASS` / `FAIL` / `UNKNOWN` with an actual value, operator, desired value/range, and explanation. Overall result: `FAIL` if any criterion fails, else `UNKNOWN` if any is unknown, else `PASS`. The engine also calculates a criteria/setup score (not profit probability), labels score quality, identifies near misses, surfaces a primary concern, and summarizes first-rule exclusions for the "why is the list thin?" diagnostic.
- **LST Core baseline** (`SCANNER_RULE_DEFINITIONS` in `src/domain/scanner/profile.ts`): the canonical default rule set for a fresh user — price $10–$50, RSI ≤ 40, BB % ≤ 33%, Put ROR ≥ 1.00%, earnings distance ≥ 10 days, stock volume ≥ 40,000, open interest ≥ 100, option bid ≥ $0.10 (all `defaultEnabled: true`); DTE, absolute delta, annualized ROR, bid/ask spread %, option volume, Do Not Trade filter, and debt/equity exist as available rules but are `defaultEnabled: false` by default (preference/context rules, not part of the confirmed LST Core numbers). `/scanner/settings` (nav label "Scanner Rules") shows a "Reset all to LST Core" action (`resetScannerSettingsToLstCoreForUser`) and a per-rule "Differs from LST Core" diff badge (`diffScannerRulesFromLstCore`) — it never silently overwrites a user's own edits. **Both `ensureMyLstScannerProfileForUser` (on-demand profile creation) and `prisma/seed.ts` must create each `ScannerRule.enabled` from `definition.defaultEnabled`** — a real bug was found and fixed this session where the seed script left the schema's own column default (`true`) in place for every rule, so a freshly-seeded demo user started with every preference rule turned on instead of matching LST Core. Verified by screenshot after the fix: a fresh Eric profile now shows "This profile matches LST Core."
- **Gating vs. preference / honest score semantics** (`GATING_RULE_KEYS` in `profile.ts`; `honestSetupScore`/`honestSetupLabel`/`hasGatingFailure` in `scanner.ts`): price, stock volume, open interest, option bid, bid/ask spread %, delta, earnings distance, and the Do Not Trade filter are **gating** — a FAIL on any of them means the setup is untradeable, not merely a quality miss. A gating FAIL caps the displayed score at `GATING_SCORE_CAP` (49) and forces the label to `"Fails"` regardless of how good the other criteria look — the UI never shows positive language ("Excellent"/"Strong"/"Ready") for a setup that fails a gating rule. An overall `UNKNOWN` result (a critical criterion, most commonly earnings, could not be evaluated) is labeled `"Verify"`, not a positive score label, even if the *known* criteria all pass — e.g. "92/100 known · Verify · Earnings unavailable" is the intended shape, never "Excellent." `/scanner` and `/dashboard` both use `honestSetupScore`/`honestSetupLabel` (never the raw `setupScore`/`setupScoreLabel`) for anything shown to the user, and the dashboard's Top Setups panel sorts by this honest score, never alphabetically.
- **Known limitation — earnings data**: Schwab's Trader API does not expose a reliable earnings-calendar/next-earnings-date endpoint in the read-only surface this app uses, so the `earningsDistance` criterion stays `UNKNOWN` for a live Schwab scan (demo data hand-supplies `earningsDate`/`earningsDistance` for its fixed candidate list only). This is deliberately left honest (`UNKNOWN`, never guessed) rather than bolting on a third-party financial-data API in this slice. A clean provider boundary for a future earnings source is the `earningsDistance` criterion key itself plus the `ScannerRuleDefinition`/`CriterionResult` shape already in place — a future slice can add a real provider without changing the engine.
- Rule catalog: `src/domain/scanner/profile.ts` (`SCANNER_RULE_DEFINITIONS`) — stock price range, RSI, Bollinger %, DTE, absolute delta, put ROR, annualized ROR, option bid, bid/ask spread %, open interest, option volume, earnings distance, Do Not Trade filter (fixed desired value, not user-editable), debt/equity.
- Per-user settings: `/scanner/settings` edits a user's own `ScannerProfile`/`ScannerRule` rows (enable/disable + desired value(s) per rule); `@@unique([profileId, key])` prevents duplicate rule keys per profile. Verified isolated per user (Matt editing his settings never changes Eric's) by `src/lib/workflows.integration.test.ts`.
- **Scanner UX redesigned for compact, high-density scanning (2026-09-01)**: `/scanner` was a scroll-heavy page (large header, action buttons, live-data banner, five stat cards, a permanently-expanded controls panel, and a permanently-expanded diagnostics panel) before the candidate list ever appeared. It's now: a slim title/action row, a one-line compact metrics strip, a single-row toolbar (one-click All/Pass/Near/Watchlist filters with live counts, an advanced-filters dropdown for the rest, a compact Sort `<select>`, a Columns dropdown for optional fields, and a collapsed-by-default "Why showing N?" diagnostics disclosure), then the candidate table immediately. Each candidate is one row (no separate "Inspect" button row); clicking anywhere on a row expands an inline detail panel (Stock/Option/Criteria breakdown, recommend-to-buddy form) without navigation. Default sort is Score descending with no query-string state to configure. The old Score/Filter "mode" toggle was removed — "Pass" and "Near" one-click filters cover the same ground more directly. Missing values render as a compact `—` (with a "Data unavailable" tooltip) instead of repeating the word `UNKNOWN` across a row; the Status column still always uses explicit words (PASS/NEAR/FAIL/VERIFY/the graded label) so nothing is communicated by color alone. All interactivity (filter/sort/columns/expand) is client-side state over the data the server already fetched — no new network calls on any of those interactions. Mobile keeps a trimmed, tap-to-expand card per candidate instead of the full table. Verified via Playwright (`tests/e2e/scanner.spec.ts`) that all 13 demo candidates are visible with no scrolling at 1366×768, and that mobile (390px) has no horizontal overflow. **No scoring/PASS-FAIL/near-match logic changed** — `src/domain/scanner/scanner.ts` and `profile.ts` were not touched; this was a view-layer-only redesign split across `src/app/(app)/scanner/page.tsx` (data fetching, unchanged domain calls) and the new `src/app/(app)/scanner/scanner-workspace.tsx` (client component owning all filter/sort/columns/expand state).
- **Demo data remains available and labeled**: `DEMO_SCAN_CANDIDATES` in `profile.ts` is a fixed 13-candidate universe (including IONQ, HOOD, PLTR, RIVN, AAP, SNAP, F, CORZ, SOFI, AMD, ROKU, T, WBD) with hand-entered stock/option/technical/earnings/liquidity values, not a live feed.
- **Controlled live Schwab scan**: `src/domain/scanner/live-scan.ts` uses the same 13-symbol starter universe for the first production-safe slice. It fetches quotes and daily history first, calculates OSO-owned RSI/Bollinger/volume/price filters, shortlists only candidates that pass the inexpensive stock stage, then calls option chains for at most 8 symbols by default. Successful live runs persist with `ScanRun.source = "LIVE:SCHWAB"`. Failures surface `LIVE DATA UNAVAILABLE` to the user and do not create or substitute demo results.
- **Scanner universe includes the user's own Research (2026-09)**: `rerunLiveSchwabScannerForUser` unions `STARTER_LIVE_SCAN_UNIVERSE` with `getResearchUniverseTickersForUser(userId)` (the user's LIKE/WATCH/NEUTRAL-status tickers; AVOID/NEVER_TRADE are deliberately excluded from the union) before calling `evaluateLiveMarketScan`. This is additive only — discovery of *new* names is unaffected, and previously-researched names that aren't in the fixed starter list now still get scanned live. The **demo** scan universe was deliberately left untouched (still the fixed 13-candidate `DEMO_SCAN_CANDIDATES` list) because synthesizing realistic fake stock/option values for an arbitrary user-added ticker would mean fabricating financial data, which this project never does.
- **Research integration (2026-09)** - see the Research section below for the full model. In the Scanner itself: a compact per-row badge (LIKE/WATCH/AVOID/EXCLUDED) shows the viewer's *own* research status next to the ticker (never a buddy's); expanding a row reveals a "Research" quick-action strip (Like/Watch/Neutral/Avoid/Exclude, plus "Open in Research") ahead of the Stock/Option/Criteria panels; a `Show Excluded` toggle (only rendered when the user has at least one excluded candidate in the current run) hides NEVER_TRADE-status candidates from the actionable view by default, and reveals them with an `EXCLUDED BY YOU` tag - same score, same status word, never altered - when toggled on. The existing "Watchlist" one-click filter now means "has any research status at all" (`researchStatus !== null`), preserving its original test/user expectations.
- **The existing "Do Not Trade filter" scanner criterion (`doNotTrade` in `SCANNER_RULE_DEFINITIONS`, `defaultEnabled: false`) was inspected during the Research slice and left untouched, but is now understood as legacy/conceptually distinct from personal exclusion**: its `doNotTrade` value is a per-*candidate* flag baked into demo data (`DEMO_SCAN_CANDIDATES`) and always hardcoded `false` for a live scan (`buildStockStageCandidate` in `live-scan.ts` never sets it), so it represents an external/global "flagged security" concept, not the user's own opinion - and conflating the two was exactly the anti-pattern this slice's new post-score personal exclusion (Research's `NEVER_TRADE` status) was built to avoid. It was **not removed or changed** (that would be scoring-logic work, out of scope, and it's already inert for live scans) - a future slice could consider deprecating it now that real personal exclusion exists, but that's a scoring-logic decision for Matt, not made here.
- Demo provider names: `src/providers/market-data/mock.ts` exports `DemoMarketDataProvider` plus backward-compatible `MockMarketDataProvider`; the demo market provider reads from the same deterministic scanner demo universe for quotes and option-chain snapshots. `src/providers/broker-read/mock.ts` exports `DemoBrokerReadProvider` plus backward-compatible `MockBrokerReadProvider`. These providers remain read-only test/development boundaries.

---

## Research (formerly "Watchlist", renamed 2026-09)

Research is the user's personal judgment of a ticker as a company/trade candidate - distinct from Scan's technical PASS/FAIL/UNKNOWN, and feeding but never replacing it. See the Product Model section above.

- **Not a new system - the existing `Watchlist`/`WatchlistItem` models were extended, not replaced.** `Watchlist`/`WatchlistItem` (private-by-default now, see below), `StockNote` (Pro/Con/General), and `Comment` are all reused as-is. `/watchlist` is now a one-line redirect to `/research`; the sidebar nav entry was renamed "Watchlist" -> "Research". `WatchlistItemStatus` (`WATCHING`/`RESEARCHING`/`READY`/`DO_NOT_TRADE`) becomes vestigial (same pattern as `UserSettings.darkMode` from the Appearance slice) — superseded by the new `ResearchStatus` enum, backfilled from it (`WATCHING`->`WATCH`, `RESEARCHING`->`NEUTRAL`, `READY`->`LIKE`, `DO_NOT_TRADE`->`NEVER_TRADE`) in migration `20260901200601_research_model`.
- **Research status**: `ResearchStatus` enum - `LIKE | WATCH | NEUTRAL | AVOID | NEVER_TRADE`. Set via `setResearchStatusForUser(userId, ticker, status)` (`src/lib/workflows.ts`), the single one-click path used by both the Scanner's quick-action strip and the Research page's status tabs/buttons. Changing status never touches the detail fields below (a quick Like/Watch/Exclude click can't accidentally wipe a saved research write-up).
- **Personal exclusion (`NEVER_TRADE`) never changes technical truth** - this is the load-bearing guarantee of the whole slice. A `NEVER_TRADE` item is hidden from the Scanner's actionable view by default (`Show Excluded` toggle reveals it, tagged `EXCLUDED BY YOU`) but its score/status/label are computed exactly as before - see the Scanner section and `tests/e2e/research.spec.ts`. Exclusion is fully reversible (set the status to anything else) - never a delete.
- **Research is PRIVATE BY DEFAULT** (changed from the old `WatchlistItem.visibility` default of `SHARED`, same precedent as `TradingAccount`'s default-visibility change) - a brand-new item is always created `PRIVATE` regardless of status, and **no `Activity` is posted** when research status changes (removed from `setResearchStatusForUser`/`createWatchlistItemForUser`) - even the bare fact that a user watched/liked/excluded a ticker is personal sentiment that belongs to them unless they explicitly share the item via the existing `toggleWatchlistItemVisibilityForUser`. Existing rows keep whatever visibility they already had; only the schema default and new-row behavior changed.
- **Sharing preserves disagreement, never merges it**: because each user has their own separate `Watchlist`/`WatchlistItem` rows, Matt and Eric can each independently research the same ticker with opposite conclusions (Matt: LIKE; Eric: NEVER_TRADE) with zero interference - no new schema was needed for this, it's inherent in the existing per-user-watchlist architecture. The Research page's "Buddy Shared Research" panel shows a buddy's own explicitly-`SHARED` item as its own separate row, never merged into the viewer's.
- **Personal-rule fields added to `WatchlistItem`** (all in migration `20260901200601_research_model`, all nullable/optional, edited via `updateResearchDetailsForUser` and one combined form on the Research detail panel): `companyName`, `whatItDoes` (manual "what does this company do"), `wouldOwn` (`YES | NO | CONDITIONAL`) + `wouldOwnMaxPrice` (auto-cleared when `wouldOwn` isn't `CONDITIONAL`, so a stale price never lingers), `monthlyPutsOnly` (boolean - stored/displayed only, see below), `rollFriendliness` (`UNKNOWN | FRIENDLY | DIFFICULT`) + `rollFriendlinessNote`, `exclusionReason`, and manual `manualSchwabGrade`/`manualLsegRating`/`manualLsegScore`/`manualLsegTarget` (plain text - Eric's spreadsheet had no numeric LSEG API to verify against, so these stay freeform).
- **`monthlyPutsOnly` is stored and displayed but does NOT filter option chains yet** - per explicit instruction, guessing at "standard monthly vs. weekly expiration" classification without clear provider support would risk silently wrong contract selection. `src/providers/schwab/market-data.ts`'s `getOptionChain` does not currently expose enough expiration-cycle metadata to classify this safely. **Deferred, not implemented**: a future slice should first verify exactly what Schwab's option chain response exposes for expiration classification (day-of-month, a `expirationType`/`settlementType`-style field, etc.) before ever filtering contracts by it.
- **`rollFriendliness` is a simple manual enum, not a computed score** - per explicit instruction, a future slice can derive a *trustworthy* roll-friendliness signal from actual Campaign/CampaignEvent history (how often a ticker required rolls, how those rolled campaigns performed) once there's enough real data; inventing a quantitative score now would be less trustworthy than the user's own plain observation.
- **Verified fundamentals: P/E, EPS, and Dividend (amount/yield) are now AUTO-populated from a real, production-verified Schwab response (2026-09-04 slice, no schema change, no migration) - PEG, Debt/Equity, Current Ratio, Industry, Sector, Profitability, Schwab Rating, and LSEG fields remain MANUAL, confirmed genuinely absent from Schwab's Trader API.** (Two earlier attempts, 2026-09-01 and 2026-09-02, were blocked purely by having no live Schwab connection anywhere in this environment - see the diagnostic bullet above for how that was finally resolved.) Full detail in the "Auto Schwab fundamentals wired into Research" bullet below.
  - **2026-09-02 documentation research (UNVERIFIED - not a live response, not Schwab's own portal, do not treat as confirmed)**: a community-maintained, apparently OpenAPI-spec-generated Go client for Schwab's Market Data API (`kebroad/schwab-market-data-production-go` on GitHub) documents the `Fundamental` quote schema as containing **only**: `avg10DaysVolume`, `avg1YearVolume`, `declarationDate`, `divAmount`, `divExDate`, `divFreq`, `divPayAmount`, `divPayDate`, `divYield`, `eps`, `fundLeverageFactor`, `fundStrategy` (fund-specific), `nextDivExDate`, `nextDivPayDate`, `peRatio`. **PEG, Debt-to-Equity, and Current Ratio do not appear anywhere in this documented schema** — if accurate, Schwab's quote endpoint simply does not provide them at all, and no amount of correct parsing will produce them. `ReferenceEquity` (the same source) documents `description`, `cusip`, `exchange`/`exchangeName`, borrow-related fields — no additional fundamental ratios there either. This is a **lead to prioritize verification against, not a basis for shipping code** - a community-generated client can be stale, incomplete, or wrong, which is exactly why the task explicitly requires a live response or Schwab's own docs before normalizing anything. No code was changed based on this research.
  - **Exact next step (unchanged in kind, sharpened in detail)**: once a live, OAuth-connected Schwab developer credential exists locally (or Matt can run this himself and share only the sanitized JSON - never tokens/credentials/account numbers), call `getQuote("AAPL")` via `SchwabMarketDataProvider` and log the complete raw `fundamental` object verbatim. Cross-check it against the field list above: if `peRatio`/`eps`/`divYield`/`divAmount` are the only fields present, that confirms the documentation lead and means PEG/D-E/Current-Ratio should be dropped from the "auto" plan entirely (the existing nullable schema columns can simply stay permanently unpopulated - no migration needed to remove them) and sourced manually only, exactly as `manualSchwabGrade`/`manualLsegRating`/etc. already do. If Schwab's real response differs from this lead, normalize only what's actually there, with explicit unit verification for Debt/Equity specifically (`0.42` vs `42` vs `42%` - do not assume).
- **Dense UI, not cards**: `/research` (`src/app/(app)/research/page.tsx` + `research-workspace.tsx`) mirrors the Scanner redesign's architecture almost exactly - a server component fetches data once (including a join against the user's latest `ScanRun` for live Scanner status/score/RSI/BB per researched ticker, and the user's own `Campaign` history grouped by ticker for "what have I done with this stock before"), a client workspace owns all filter/sort/columns/expand state with zero extra network calls. Status tabs (All/Like/Watch/Neutral/Avoid/Excluded, each with a live count) replace giant per-item cards; default columns are Status/Ticker/Company/Scanner/Price/RSI-BB, with Would-Own/Monthly-Only/Roll-Friendliness/Manual-Grades/Trade-History as optional `Columns` toggles. Verified dense and functional with 57 researched tickers in local visual QA (screenshots reviewed, then deleted per policy). Mobile uses the same tap-to-expand `<details>` card pattern as the Scanner's mobile view, confirmed no horizontal overflow at 390px.
- **The Scanner's score-parsing logic (`toDomainSummary`/`toViewResult`) is deliberately duplicated (not extracted/shared) between `scanner/page.tsx` and `research/page.tsx`** - a small (~20-line), clearly-commented duplication chosen specifically to avoid touching the already-approved, already-pushed Scanner redesign in this slice. A future cleanup slice could extract this into a shared `src/domain/scanner/view.ts`-style module; tracked here as a known, intentional tradeoff, not an oversight.
- **Eric's spreadsheet import was not implemented this slice** (explicitly permitted to defer if it didn't fit cleanly once Research itself was done correctly). The real spreadsheet was never provided to or read by this session, and must never be committed to the repo. **Exact next step**: once Matt provides the actual workbook (as a local, gitignored file, never committed), build a `scripts/import-eric-research.ts`-style dry-run-first importer that (1) requires an explicit target user, (2) previews counts before writing anything (e.g. "67 tickers, 57 business descriptions, 34 Schwab ratings, 6 possible NEVER_TRADE, 8 possible MONTHLY_PUTS_ONLY"), (3) never auto-applies an interpretive mapping like `"AVOID"` text -> `NEVER_TRADE` status or `"MONTHLY PUTS ONLY"` text -> `monthlyPutsOnly: true` without an explicit confirmation step, (4) never logs spreadsheet contents, and (5) never runs against production without Matt's explicit approval of that specific run.
- Tests: `src/lib/research.integration.test.ts` (11 DB-backed tests: private-by-default, cross-user read/mutate isolation via `AuthorizationError`, User B liking a ticker User A excluded with zero interference, disagreement preserved as two independent rows, exclusion reversible not destructive, would-own/monthly/roll-friendliness/manual-grade persistence round-trip, stale `wouldOwnMaxPrice` cleared, a status change never wipes saved details, scanner-universe union includes LIKE/WATCH/NEUTRAL and excludes AVOID/NEVER_TRADE, invalid status rejected) plus `tests/e2e/research.spec.ts` (8 Playwright tests covering the dense table, add-ticker default state, exclude/unexclude, mobile overflow, and the Scanner<->Research integration including the score-unchanged-by-exclusion proof).
- **Research customization + per-user column preferences (2026-09-03 slice, migration `20260903201201_research_customization_and_view_preferences`).** The Research table's fixed 5-column default became fully configurable, per user, without any name-based special-casing anywhere in application code:
  - **New fields on `WatchlistItem`** (all manual unless noted): `manualLsegRecommendation` (`BUY | HOLD | SELL | UNKNOWN`, distinct from the existing free-text `manualLsegRating`), `profitability` (`PROFITABLE | MIXED | UNPROFITABLE | UNKNOWN`) + `profitabilityNote` (a structured "profitable over the past several years?" judgment - never derived from a single year's EPS), `paysDividend` (nullable boolean tri-state), and manual-fallback counterparts for every reserved-auto fundamentals column added in the prior slice - `manualPeRatio`, `manualPegRatio`, `manualDebtToEquity`, `manualCurrentRatio`, `manualDividendYield`, `manualDividendAmount` (paired with a new `fundamentalDividendAmount` reserved-auto column, mirroring the existing yield column). The `fundamentalXxx` columns stay untouched/reserved for a real auto source - display always prefers auto over manual via one pure, unit-tested function (`resolveAutoOrManual` in `src/domain/research/columns.ts`), so wiring up a verified provider later can never silently overwrite something a user typed.
  - **Per-user column layout**: `UserSettings.researchColumns` (an ordered `String[]` - order doubles as column order) + `researchSortKey`, upserted only by `userId` (`updateResearchColumnsForUser`/`updateResearchColumnsAction`). An empty array (never customized) falls back to `DEFAULT_RESEARCH_COLUMNS` in `src/domain/research/columns.ts`, which is exactly Matt's pre-existing default (Company/Scanner/Current Price/RSI-BB) - zero behavior change for any user who never opens Columns. `src/domain/research/columns.ts` is the single shared registry (key/label/group) the client Columns menu and the server-side sanitizer both read from, grouped as Core/Technical/Fundamentals/External Research/Personal/History in the UI. Status and Ticker are pinned/sticky, not part of the configurable set.
  - **`RESEARCH_HEAVY_COLUMN_PRESET`** (same file) is a named preset - not tied to any real person in code - listing every fundamentals/external-research/personal field, in the order originally requested for one specific user's initial layout. It's applied only via the one-time script below, never referenced by any request-handling code path.
  - **One-time initialization script**: `scripts/init-research-columns.ts` - fully generic, takes `--email <address>` on the command line (never a hardcoded email or name), dry-run by default, requires `--apply` to write. Refuses to run with a missing email, zero matches, or (defensively, even though `User.email` is unique) more than one match. Never overwrites an existing saved preference - prints "Existing preference found — no changes made" and stops, with no force flag (add one only if a real future need arises). Prints only the target's name/email and the column-key list, never a password/token/account number. Verified locally end-to-end: dry run changes nothing, `--apply` writes once, a repeat `--apply` correctly refuses to overwrite.
  - **Mine/Buddy/Both**: Research reuses the exact generic `TrackerScope` (`"mine" | "buddy" | "both"`) type and URL-param pattern already established by the Tracker, with the buddy label resolved generically (`users[0]?.name`) - no new scope concept, no hardcoded names. Buddy/Both show only the other user's `SHARED` items (same privacy filtering as the pre-existing "Buddy Shared Research" panel); Both never merges two users' opinions on the same ticker into one row - a small inline cross-reference badge shows the other user's independent view alongside your own.
  - Tests: `src/domain/research/columns.test.ts` (11 unit tests - registry integrity, sanitization, auto/manual precedence), `research.integration.test.ts` extended (+10 DB tests - new-field persistence with correct null-vs-0 handling, invalid-enum rejection, per-user column isolation), `tests/e2e/research-customization.spec.ts` (13 Playwright tests - Matt's unchanged default, the research-heavy layout, Columns-menu completeness, cross-user column isolation, logout/login persistence, manual-field save/reload, Mine/Buddy/Both privacy including two explicit private-leak checks, mobile trim, InfoTip accessibility, theme modes).
  - **Not built this slice** (documented as next steps, not oversights): optional Scanner columns for Research Status/Would Own/P/E/D-E/Schwab Rating/LSEG Recommendation (would require expanding the Scanner's own data pipeline - out of scope per this slice's explicit constraints); promoting Company/Industry to AUTO via the existing `getInstrument()` provider method (architecturally possible, but its real response shape has never been verified against a live Schwab connection, so it stays manual rather than guessed).
- **Columns dropdown click-outside/Escape fix + Current Ratio auto-population + one-stop expanded detail (2026-09-04 slice, migration `20260904162540_alpha_vantage_balance_sheet` - see Alpha Vantage API section for the schema/queue/quota detail):**
  - **`ColumnsMenu` rewritten from a native `<details>`/`<summary>` to `useState` + a `useRef` root + a `pointerdown`(capture)/`keydown` effect** - mirroring `InfoTip`'s existing outside-click/Escape pattern exactly rather than adding a dependency. Clicking the Columns button still toggles it (unchanged for existing tests/muscle memory); clicking anywhere else, or Escape, now also closes it; clicking a checkbox/reorder-arrow inside never closes it. `tests/e2e/research-customization.spec.ts` gained a dedicated test for outside-click/Escape/repeated-open-close stability.
  - **Current Ratio gets a verified Alpha Vantage auto-tier** - `currentRatioCell()` (`research-workspace.tsx`) now chains Schwab-auto -> Alpha Vantage `balanceSheetCurrentRatio` -> manual, exactly mirroring the existing `pegRatioCell` three-tier pattern, with the shown title correctly attributing the source. Debt/Equity's cell is untouched (still manual-only) - no verified auto source exists for it yet, see Alpha Vantage API section. The manual-entry form's Current Ratio/PEG inputs now pre-fill with the fully-resolved (auto-if-available) value, same precedent as the pre-existing PEG input.
  - **New "Auto Data" progressive-disclosure section in the expanded Research row** (`ResearchAutoDataSection`, both desktop and mobile expanded views) - a collapsed-by-default `<details>` organized into Company/Financial Health/Valuation/Growth/Analyst groups, reusing the exact same cell-renderer functions the dense table uses (`currentRatioCell`, `pegRatioCell`, `avAnalystTargetCell`, `avAnalystConsensusCell`) so formatting/source-tooltips are never defined twice. Renders nothing at all when there's genuinely no auto data for that ticker - never a wall of dashes.

---

## Campaign Tracker

`/positions` (nav label "Tracker") is a manual account + campaign lifecycle tracker — the direct implementation of the "Tracker" product area in `PRODUCT_VISION.md`. It models a whole trading idea (a "campaign") from the first cash-secured put through rolls, assignment, covered calls, and eventual close as one continuous, inspectable history — not isolated trade rows.

**Schema** (`prisma/schema.prisma`, migration `prisma/migrations/20260828215700_campaign_tracker_foundation/`, **applied to production Supabase as of 2026-08-29** — see Database / Seed / Bootstrap Safety):
- `RecordVisibility` enum: `INHERIT | PRIVATE | SHARED` — used by `Campaign.visibility`.
- `Campaign` model: owner, account, ticker, `CampaignStrategy` (`CASH_SECURED_PUT | WHEEL`), `CampaignStatus` (`OPEN | ASSIGNED | CLOSED`), visibility, opened/closed dates, thesis text, and an `entrySnapshotJson` (the owner's own scanner result at entry time, for context — not a live query).
- `CampaignEvent` model: append-only lifecycle events (`SELL_PUT`, `CLOSE_PUT`, `ROLL_PUT_CLOSE`, `ROLL_PUT_OPEN`, `ASSIGNMENT`, `SELL_COVERED_CALL`, `CLOSE_COVERED_CALL`, `COVERED_CALL_EXPIRED`, `STOCK_SALE`, `NOTE`), each with its own contracts/shares/strike/premium/fees/underlying price. A roll writes a `ROLL_PUT_CLOSE` + `ROLL_PUT_OPEN` pair sharing a `groupKey`; **no workflow ever updates or deletes an existing event** — the full history is always reconstructible.
- `TradingAccount` gained `startingBalance`/`manualBalance` (manual, not synced). Its default `visibility` was briefly `SHARED` during the Campaign Tracker slice; the 2026-08-31 Account Ledger slice changed the default back to `PRIVATE` per explicit product decision (financial account data should not default to shared the way collaborative watchlist/research data does) — see Account Ledger section and Architectural Decisions.

**Visibility model:** a `Campaign.visibility` of `INHERIT` resolves to its parent account's `Visibility` (`resolveInheritedVisibility` in `src/lib/privacy.ts`); `SHARED`/`PRIVATE` on the campaign itself always override the account. Server-side query logic (`getTrackerPageData` in `src/lib/app-data.ts`) filters buddy-visible campaigns with `visibility = SHARED OR (visibility = INHERIT AND account.visibility = SHARED)` — a buddy never receives a row for a private campaign or an inherited campaign under a private account, regardless of the `mine`/`buddy`/`both` scope requested. Proven by `src/lib/workflows.integration.test.ts` (inherited/explicit-shared/explicit-private transitions, including an account later flipped to SHARED while its campaign is explicitly PRIVATE, which must still block) and by a Playwright test that logs in as both users. All campaign mutations (`closeCampaignPutForUser`, `rollCampaignPutForUser`, `assignCampaignPutForUser`, `toggleCampaignVisibilityForUser`, etc., in `src/lib/workflows.ts`) re-read ownership from the database by ID before acting — a tampered form ID cannot mutate another user's campaign.
- One narrow leak was found and fixed during this session: a buddy viewing an explicitly-SHARED campaign whose parent account is PRIVATE previously saw that private account's name on the campaign card. `CampaignCard` in `src/app/(app)/positions/page.tsx` now shows "Private account" instead unless the viewer can actually read the account (owner or account itself SHARED).

**Financial calculations** (`src/domain/finance/campaigns.ts`, `summarizeCampaign()`): sums option credits/debits (including roll legs, at the 100-share multiplier) into `totalPremiumReceived`/`optionDebitsPaid`/`netOptionPremium`, tracks `rollCredits`/`rollDebits`/`netRollPremium` for roll-specific display, and computes `realizedPL` as `premium credits − premium debits − realized stock cost basis + stock sale proceeds − fees`, unconditionally (not branched on share-holding state). A proportional (average-cost) allocation splits assigned-share cost basis between shares already sold and shares still held, so a **partial** stock sale's realized gain/loss is captured immediately rather than silently dropped until the whole position closes — this was a real gap found and fixed this session, with two new regression tests (`campaigns.test.ts`) proving it. `unrealizedPL`/`totalCampaignPL` require a supplied current stock price for any still-held shares and stay `null` (never fabricated) without one. `finalResult` is `GAIN`/`LOSS`/`BREAKEVEN`/`OPEN`/`UNKNOWN` — never dressed up. Verified against the task's own worked roll example (sell +$48, roll −$71/+$102 ⇒ net roll +$31, total option cash flow +$79) both in unit tests and live in the demo UI.

**UI** (`src/app/(app)/positions/page.tsx`): a stat strip (campaign count, known realized P/L, net option premium, visible accounts), a Mine/`<buddy name>`/Both segmented control (`?scope=`), and four compact tabs inside the same page (`?view=open|history|performance|accounts`, default `open`) — deliberately not four new top-level nav entries:
  - **Open** — collapsible "New Campaign"/"New Account" panels (the account panel opens by default and the campaign panel is hidden entirely when the user has no account yet, so the form for an unusable action is never expanded ahead of the actual first step), a read-only "Your Schwab Positions" panel (`SchwabPositionsPanel`, only rendered when Schwab is connected) showing each Schwab position tagged `Possible match` (same underlying ticker as an open campaign) or `Unlinked` — never an automatic hard link, and one `<details>` card per non-`CLOSED` campaign with lifecycle timeline, result breakdown, and (owner-only) action forms (Close Put, Roll, Mark Assigned).
  - **History** — the same campaign card for all visible campaign states (`OPEN`, `ASSIGNED`, and `CLOSED`), so open rolls and assigned wheel campaigns remain visible in the historical record instead of disappearing until final close.
  - **Performance** — always computed from the current user's own accounts and own campaigns only (`ownAccounts`, `ownCompletedCampaigns`, and `ownPerformanceCampaigns` from `getTrackerPageData`, never the scope-filtered `campaigns`/`visibleAccounts` lists), so switching the Mine/Buddy/Both selector can never blend Matt's and Eric's results. Shows account value, contribution-adjusted trading P/L, realized campaign P/L, current/mark-to-market P/L when a real mark source exists, projected-OTM P/L for still-open CSPs, closed-campaign stats, a dated 1% target tracker, and a per-campaign accounting table — see Account Ledger / Performance Methodology section.
  - **Accounts** — the scope-filtered `visibleAccounts`, each showing ledger-derived starting/contributions/current value (not the old ad hoc `manualBalance` field) plus a link to `/account` for logging a deposit/withdrawal/adjustment.

Verified overflow-free on a 390px mobile viewport via Playwright and manual screenshots (`test-results/visual-qa/`).

**Demo data** (`prisma/seed.ts`, local dev only): 3 accounts (Matt IRA — SHARED, Eric IRA — SHARED, Matt's Playground/Paper — PRIVATE) and 8 campaigns covering: a simple profitable closed CSP (BROS), an open CSP (IONQ), a single roll (AAP), multiple rolls closing positive (SOFI), an assigned CSP that continues through covered calls including one expiring worthless (F, `WHEEL` strategy), a losing closed CSP (ROKU), an explicitly-PRIVATE campaign under a private account (WBD, Matt), and a SHARED campaign owned by Eric (HOOD) so both users have visible history.

**Known limitation (intentional, not a bug):** there is no UI action yet to sell/close a covered call or record a stock sale — an `ASSIGNED` campaign shows an honest in-app note ("Covered call and stock-sale events are modeled now; manual buttons for that phase are a clean next slice.") rather than a broken control. The finance engine and schema already fully support these event types (exercised by seed data and tests); only the create-event UI/workflow/action for that phase is missing. This is the recommended next slice — see Next Tasks.

---

## Roll Status (OSO decision-support, 2026-09)

A per-user-configurable "Roll Buffer %" and a resulting GREEN/AMBER/RED "Roll Status" badge for open cash-secured puts on the Dashboard and Tracker. **Explicitly not a confirmed proprietary LST numeric rule** — it is an adjustable OSO decision aid, and Matt and Eric may each set a different threshold. Requested and specified in full (exact color boundaries, label wording, display format, and the timing-vs-risk distinction below) by Matt this slice.

**Schema** (`prisma/schema.prisma`, migration `prisma/migrations/20260904144732_user_roll_buffer_preference/`, purely additive — `ALTER TABLE "UserSettings" ADD COLUMN "rollBufferPercent" DECIMAL(5,2) NOT NULL DEFAULT 3.0`): `UserSettings.rollBufferPercent`, defaulting every existing and new row to 3.0%. **Applied to production Supabase and verified via `prisma migrate status` ("Database schema is up to date!") on 2026-09-04.**

**Domain logic** (`src/domain/finance/rollStatus.ts`, `rollStatus.test.ts`, 15 tests): `computeRollStatus({ currentPrice, strike, rollBufferPercent, now })` returns `null` (never fabricates) for a non-finite/non-positive input, otherwise:
- **GREEN / HOLD** — `distancePct > rollBufferPercent` (strictly outside the buffer).
- **AMBER / NEAR STRIKE** — `0 < distancePct <= rollBufferPercent` (inclusive of the buffer edge).
- **RED** — `distancePct <= 0` (at-or-below strike; never RED merely because a position is short). Label is `ROLL CANDIDATE` before the Friday management checkpoint, `ROLL` at/after it — `isPastFridayManagementCheckpoint()` treats Friday at/after 15:00 ET (via `Intl.DateTimeFormat`/`America/New_York`, the same dependency-free ET pattern as `schwab/csv.ts`) and all of Saturday/Sunday as past-checkpoint. This is display wording only — the color/percentage can update anytime off a live price, but the app never tells the user to execute mid-week merely because the stock crossed the strike; execution stays in Schwab/Thinkorswim regardless.
- Every result carries a `distanceText` (e.g. `"+1.8% above strike"`, `"At strike"`) and a full `reason` sentence (e.g. `"+1.8% above strike · inside your 3% Roll Buffer"`) — color is never the only signal.

**`getCurrentOpenPut(events)`** (`src/domain/finance/campaigns.ts`) — the campaign's currently-open short put (strike/contracts/expiration), or `null` if the most recent trade event isn't `SELL_PUT`/`ROLL_PUT_OPEN`. Promoted this slice from a private, duplicated-risk helper in `positions/page.tsx` into one shared, tested function (6 tests in `campaigns.test.ts`) used by both the Tracker's existing cost-to-close resolution and the new Roll Status badge.

**Settings** (`/scanner/settings`, a new "Decision Support Preferences" panel with its own Save button, deliberately separate from the Criteria form and NOT wired through the `ScannerRule`/criteria evaluation pipeline): a Roll Buffer % number input (`MIN_ROLL_BUFFER_PERCENT = 0.1` / `MAX_ROLL_BUFFER_PERCENT = 25`, `updateRollBufferPercentForUser` in `src/lib/workflows.ts`, upserted per-user) with Matt's exact specified `InfoTip` copy. Per Matt's explicit instruction this lives in Scanner Settings, not Account credentials. 5 DB-integration tests (`src/lib/rollBuffer.integration.test.ts`) prove the 3.0% default, persistence, cross-user isolation (Matt and Eric can hold different values), upsert-for-no-existing-row, and rejection of out-of-range/`NaN` input without mutating the stored value.

**Live price** (`src/lib/live-quotes.ts`, new): `getLiveQuotePricesForUser(userId, tickers)` batches `MarketDataProvider.getQuote` (via `getSchwabMarketDataProviderForUser`, the same provider `live-scan.ts` already uses) across unique tickers with `mapWithConcurrency`. Never throws and never fabricates — no connection, no token, or a failed per-ticker lookup all map to `null`, rendered as an honest "Roll status unavailable" badge (`RollStatusUnavailableBadge`, `src/components/roll-status-badge.tsx`) rather than a guessed color.

**Display**: `RollStatusBadge` (same file) renders the emoji+label `Badge` (reusing the existing `good`/`warn`/`bad` tone system, never a new color language) plus the percentage-distance line and, for AMBER, the buffer note — always alongside the color, matching Matt's "color must not be the only signal" requirement.
- **Dashboard** (`src/app/(app)/dashboard/page.tsx`): the Open Positions panel's row list is now the async `DashboardOpenPositionsWithRollStatus` inside its own `Suspense` boundary (same pattern already used there for `DashboardBrokerStats`/`DashboardBrokerPositions`), batching one quote fetch for all visible campaigns' open-put tickers.
- **Tracker** (`src/app/(app)/positions/page.tsx`): `CampaignCard`'s collapsed header row shows the badge next to the ticker/status/visibility badges, computed only for the Open tab (`view === "open"`) since that's the only tab it's specified for.
- **Whose Roll Buffer applies**: always the *viewer's own* `rollBufferPercent`, never the campaign owner's — load-bearing on the Tracker's Buddy/Both scopes, where the displayed campaign may belong to the other user. `getDashboardData`/`getTrackerPageData` (`src/lib/app-data.ts`) each select the requesting user's own `UserSettings.rollBufferPercent`, mirroring the existing per-page `researchColumns`/`researchSortKey` select rather than adding to the shared cross-page session query.

**Short-position display cleanup (same slice, found while verifying the above):** `describeBrokerPositionForDisplay` (`src/domain/finance/brokerPositions.ts`) previously rendered a short option as a bare signed quantity ("-1 contract") next to its negative market value colored red, as if being short were itself a loss. It now returns neutral `quantityLabel` ("Short 1 put"/"Long 1 put"/"100 sh") plus a `valueLabel`/`value` pair — `"Cost to close"` with the liability shown as a positive number for a short option, `"Market value"` unmodified otherwise — and both Dashboard (`DashboardBrokerPositions`) and Tracker (`SchwabPositionsPanel`) render it with no sign-based red/green styling. Red/green stays reserved for the Roll Status badge and verified realized P/L. 2 new tests in `brokerPositions.test.ts`.

**Not done this slice (proportional scope, no user request for it)**: no Suspense-per-row streaming finer than the existing page-level boundaries; no caching of live quotes beyond the provider's own market-data cache; the Tracker's History tab does not show Roll Status (only Open does, per spec). Verified via `tsc --noEmit`, `eslint`, the full Vitest suite (365 tests, run with `RUN_DB_TESTS=1` against this session's local development database), `prisma validate`/`migrate status` (both local dev and production Supabase confirmed up to date), a clean `next build`, and a live Playwright smoke pass against a production-mode (`next start`) local server: `/scanner/settings`'s Roll Buffer correctly defaults to 3.0%, saves, and survives a reload; Dashboard and Tracker Open both render honest "Roll status unavailable" badges (no live Schwab connection in this local environment, so GREEN/AMBER/RED itself could not be exercised end-to-end - proven instead by the 15 domain-level tests above); Tracker History shows no badge. The short-position wording fix likewise could not be seen live (no synced broker positions in this local session) and is proven at the unit-test level only.

**"Next LST Checkpoint" reminder (2026-09-04, same slice as the layout/Alpha Vantage work below)**: `src/domain/finance/lstCheckpoint.ts` - `getNextLstCheckpointLabel(now)` returns one of five compact, non-prescriptive labels ("Entry review begins around 10:30 AM ET" / "Monday entry window · setups still must qualify" / "Next position check · 3:00 PM ET" / "Friday Roll Call · 3:00 PM ET" / "Next entry review · Monday 10:30 AM ET") per the verified working schedule (Monday entry/setup review ~10:30 AM-12:30 PM ET, a Monday-Thursday position-management check ~3:00 PM ET, Friday's Roll Call ~3:00 PM ET). Same dependency-free `Intl.DateTimeFormat`/`America/New_York` pattern as Roll Status's Friday checkpoint (DST-safe, verified against the real November 2026 EDT->EST fallback boundary in tests). Shown as one compact line in the Dashboard's existing top status bar (no new card). Purely a timing aid - never claims 10:30 is an exact mandatory minute or that 3:30 PM is the Friday checkpoint, and never triggers any order/automation behavior. 7 unit tests (`lstCheckpoint.test.ts`).

---

## Appearance System

Dark (existing look, unchanged) / Light (bright professional financial-dashboard look, OSO's own identity — not a Schwab clone) / System (follows OS `prefers-color-scheme`, reacts live while the app is open).

- **Persistence**: reuses the existing `UserSettings` model — added `UserSettings.appearance: AppearanceMode` (`DARK | LIGHT | SYSTEM`), no new table. Logged-in: the database is always authoritative (`resolveAppearanceForRequest` in `src/lib/appearance.ts`). Logged-out (e.g. viewing `/login`): a non-sensitive `oso-appearance` cookie (not httpOnly, never mixed with session/credential cookies) is read as a fallback, defaulting to `SYSTEM` if neither exists.
- **Existing users stay Dark**: migration `20260901031101_user_appearance_preference` adds the column with `DEFAULT 'DARK'` (backfilling every existing row, including in-flight inserts during the migration window), then a second `ALTER COLUMN ... SET DEFAULT 'SYSTEM'` changes only the default for *future* rows. A naive single-default migration would have silently switched Matt/Eric to `SYSTEM` — deliberately avoided. `prisma/seed.ts` explicitly sets `appearance: "DARK"` for the two demo users so a local reseed matches this same "already-onboarded" behavior instead of falling back to the new-row default.
- **No flash, no client JS required for System**: `src/app/layout.tsx` sets `data-theme="dark"|"light"` on `<html>` server-side for an explicit choice, and omits the attribute entirely for `SYSTEM` — `globals.css`'s plain `@media (prefers-color-scheme)` block then resolves it natively during the browser's own style computation, before first paint. No inline bootstrap script, no hydration mismatch risk.
- **Retheming without rewriting components**: Tailwind v4 compiles every color utility (`bg-zinc-950`, `text-emerald-300`, etc.) to a `var(--color-*)` reference. `globals.css` overrides that same variable set under `:root[data-theme="light"]` (and the System-driven media-query equivalent), which re-themes the entire already-written app without touching component files. A parallel semantic layer (`--background`, `--surface`, `--positive`, `--negative`, `--warning`, `--unknown`, `--border`, etc., aliased onto the same scale) exists for new/shared UI to prefer going forward.
- **`AppearanceControl`** (`src/components/appearance-control.tsx`): a client component rendered both compact (sidebar footer, next to sign-out) and full-size (`/account` → Preferences panel) — never a new top-level nav item. Clicking applies the `data-theme` attribute and cookie optimistically (instant, no flash) via plain DOM mutation, then persists to the database in the background through `updateAppearanceAction` (a Server Action called directly, not via `<form>`).
- **A real bug found and fixed in this slice**: `text-zinc-950` was being used for two incompatible purposes — page background *and* "always-dark text on a bright accent button/badge." Since Light mode remaps the `zinc-950` variable to a near-white value, every primary button would have rendered illegible near-white-on-bright-green text in Light mode. Fixed by replacing the fixed-purpose usage with `text-black` across 10 files (verified by grep that no other usage pattern existed for that exact class). **New rule**: never use a Tailwind color-scale literal for something that must stay a fixed color regardless of theme — use `text-black`/a dedicated non-remapped value instead.
- **A second bug found and fixed**: the login page's outer background was a literal hex-value gradient (`bg-[radial-gradient(...),linear-gradient(135deg,#090d0b,#18181b_58%,#0f1720)]`), not tied to the theme variables, so it stayed black in Light/System-light mode while the card on top of it correctly turned light — a jarring mismatch. Fixed by referencing the same `var(--color-zinc-950)`/`var(--color-zinc-900)` tokens inside the arbitrary-value gradient instead of hardcoded hex, so it re-themes with everything else.
- **Testing gotcha discovered this slice (fixed in `playwright.config.ts`)**: Next.js dev's cross-origin dev-asset guard trusts `localhost` but silently 403s JS chunk requests made via the equivalent `127.0.0.1` — this breaks client hydration (and therefore every `onClick`-driven assertion) without any visible test failure, since whatever the server-rendered default happens to be just stays on screen. `playwright.config.ts`'s default `baseURL` now uses `localhost`. If a future Playwright run against `next dev` shows interactive elements silently not responding, check this first.
- Verified in both themes on desktop and mobile viewports (Dashboard, Tracker Open/Accounts, Scanner, Scanner Rules, Watchlist, Account, Login) via temporary Playwright screenshots (viewed, then deleted per policy — never committed). PASS/FAIL/VERIFY/near-match labels, NEW/DUPLICATE/CONFLICT/NEEDS_REVIEW/INVALID import labels, and SHARED/PRIVATE/MANUAL badges all remain legible and color-distinguishable in Light mode.
- Tests: `src/lib/appearance.test.ts` (pure `dataThemeAttributeFor` mapping), `src/lib/appearance.integration.test.ts` (DB-backed: fresh users default to `SYSTEM`, a chosen appearance persists, changing User A's appearance never touches User B's, upsert works for a user with no settings row yet).

---

## App Shell / Navigation

- **Collapsible desktop sidebar (2026-09-04)**: the persistent left nav (`src/app/(app)/layout.tsx` + the new `src/app/(app)/app-sidebar.tsx`) can collapse to a ~72px icon rail on desktop/tablet (`md:` and up), reclaiming real width for wide content like Research's dense table - not an overlay, the flex layout genuinely redistributes width to `<main>`.
  - **Split for interactivity**: `layout.tsx` stayed an async Server Component (fetches `user`/`unread`/the sidebar cookie); all nav rendering (desktop aside + the existing mobile top bar/icon grid, previously duplicated across the same file) moved into one new `"use client"` component so `usePathname()`-based active-route highlighting (new - the nav had none before) and collapse state could exist. Desktop expanded is `w-64`; collapsed is `w-18` (72px, within the requested ~64-76px range).
  - **Persistence, chosen over localStorage**: a plain non-httpOnly cookie (`oso-sidebar-collapsed`), read server-side in `layout.tsx` and passed down as `initialCollapsed` - the same pattern `AppearanceControl`/`oso-appearance` already uses. This was a deliberate deviation from "prefer localStorage": a naive `useEffect`-read-then-`setState` trips this repo's `react-hooks/set-state-in-effect` ESLint rule, and a `useState(() => localStorage...)` lazy initializer causes a real SSR/client hydration mismatch (server never sees `localStorage`). The cookie approach has zero lint issues, zero hydration mismatch, and a genuinely flash-free first paint (the server already knows the preference) - strictly better on every axis "avoid hydration flash if practical" cared about. No database migration - this is browser-cookie state only, exactly like appearance's cookie is not itself the DB source of truth for logged-in users.
  - **Toggle**: a small circular chevron button anchored to the sidebar's edge (`-right-3 top-6`, `aria-label="Collapse sidebar"`/`"Expand sidebar"`, `aria-expanded`), always visible regardless of collapsed state - never hidden, never requires hunting.
  - **Collapsed-state accessibility**: each nav icon gets `aria-label` (the full label, since the visible text is hidden), `aria-current="page"` when active (now added to both desktop and mobile nav, which had no active-state indicator at all before this slice), and a CSS-only hover/focus-visible tooltip (`group-focus-visible:block`, real keyboard-focus support - not a native `title` attribute, which doesn't reliably show on keyboard focus). Sign-out, account initials, and (when collapsed) a title-tooltip identify the user; the 3-button theme picker (`AppearanceControl compact`) does not fit a 72px rail without redesigning that shared, already-shipped component, so it's shown only when expanded - one click on the always-visible toggle away, a deliberate proportionality call rather than force-fitting it.
  - **Mobile completely unchanged**: the existing `md:hidden` top bar + 4-column icon grid is untouched in structure/behavior (only gained the same new `aria-current` active-state, for free, since both nav renderings now share one `navItems` array and one `isActiveRoute` helper) - no permanently-visible icon rail was introduced on mobile.
  - Tests: `tests/e2e/desktop-sidebar.spec.ts` (12 Playwright tests - starts expanded with no preference, collapse/expand width changes, main content genuinely reclaims width (not an overlay, verified via non-overlapping bounding boxes), preference survives client-side navigation and a full page refresh, collapsed icons remain keyboard-focusable/labeled/navigable, active route indicated in both states, theme/account/sign-out remain reachable after re-expanding, Research renders/scrolls correctly with no page-level horizontal overflow in either state, mobile nav is unaffected and has no collapse toggle). All 12 pass against a genuinely current build - see the Testing section's Docker note below for why this took real diagnostic work to prove.
- **Wide-screen shell width (2026-09-04)**: `layout.tsx`'s outer row was a flat `max-w-7xl` (1280px) regardless of viewport, wasting large gutters on wide monitors (Matt's complaint - screenshots showed unused gray space at 1920px+). Now `max-w-7xl 2xl:max-w-[1800px]` - unchanged (still 1280px) at every viewport Tailwind's `2xl` breakpoint (1536px) doesn't reach, so 1366px/1440px are pixel-identical to before (no regression), while 1920px/2560px get a substantially wider ~1800px shell instead of the old 1280px cap. Deliberately NOT `100vw` and NOT scaling further past 1800px even at 2560px - a bounded ceiling, chosen from the suggested ~1600-1800px range, so forms/text never become absurdly wide. Collapsed sidebar reclaims width within whichever cap is active (already worked; unaffected by this change - verified 1544px → 1728px `<main>` width when collapsing at 1920px). No page-specific overrides were added - Research's dense table (and Dashboard/Scanner/Tracker) automatically benefit from the wider shared shell rather than each page opting into its own width.
- **Sidebar theme-control overflow fix (2026-09-04)**: the expanded account card's name/email column had no `min-w-0`/`truncate`, so a long email could push the 3-button `AppearanceControl` past the card's border. Fixed with `min-w-0` on the flex row and the name/email div, `truncate` on the name and email text, and a new `className` prop on `AppearanceControl` (plus `shrink-0` baked into its own compact-variant root) so the theme picker is never squeezed. Confirmed via Playwright bounding-box checks at 1024/1366/1920px that the control's right edge stays inside the card's right edge at every width.

---

## Account Ledger / Performance Methodology

**Schema** (migration `20260831211006_account_ledger_and_broker_link` — see Database / Seed / Bootstrap Safety for production status): `AccountLedgerEntry` (`src/domain/finance/accountLedger.ts` for the domain logic) records `STARTING_VALUE | DEPOSIT | WITHDRAWAL | MANUAL_ADJUSTMENT | BROKER_SNAPSHOT | NOTE` entries per `TradingAccount`, each with an amount/accountValue/cash and `occurredAt`. `TradingAccount` gained `source` (`MANUAL | SCHWAB`), `brokerConnectionId`, and `externalAccountId` (unique per `(userId, externalAccountId)` so a Schwab sync upserts the same account instead of duplicating it).

**Why a ledger instead of `startingBalance`/`currentBalance`:** the ledger is the only source of truth for trading performance — `currentBalance − startingBalance` would silently count a deposit as profit. Worked example (`accountLedger.test.ts`, `accountLedger.integration.test.ts`): starting $10,000 + a $2,000 deposit + $300 of trading P/L = current $12,300, and the trading profit is reported as **+$300**, not +$2,300. `summarizeAccountLedger()` computes `startingValue`/`netContributions`/`ledgerDerivedValue` from `STARTING_VALUE`/`DEPOSIT`/`WITHDRAWAL`/`MANUAL_ADJUSTMENT` entries only (a `BROKER_SNAPSHOT` never counts as a contribution — it's a fact Schwab reported, not a cash flow the user made); `currentAccountValue()` then adds known realized trading P/L on top, or uses the latest `BROKER_SNAPSHOT` as authoritative when one exists (a live Schwab-synced account already reflects P/L/contributions/everything, so it overrides the ledger-derived math rather than double-counting alongside it).
- `createTradingAccountForUser` now defaults new accounts to `PRIVATE` (previously `SHARED`) and automatically writes a `STARTING_VALUE` ledger entry from the provided starting balance.
- `addAccountLedgerEntryForUser` (Account page mini-form) lets an owner log a `DEPOSIT`/`WITHDRAWAL`/`MANUAL_ADJUSTMENT`; re-checks account ownership server-side (a cross-user attempt throws `ValidationError`, proven by `accountLedger.integration.test.ts`).
- `prisma/seed.ts` was updated to give each of the three demo accounts a matching `STARTING_VALUE` ledger entry, so the Account/Performance/Accounts views don't show "No data" for the seeded demo accounts.

**Win/loss accounting** (`src/domain/finance/performance.ts`, `summarizeWinLoss`): only campaigns with `status: "CLOSED"` are ever fed into this function — `getTrackerPageData`'s `ownCompletedCampaigns` and `getDashboardData`'s `completedCampaigns` both query `status: "CLOSED"` at the database level, so an OPEN campaign's net cash flow (e.g. +$28 sell, −$53 roll close, +$82 roll open = +$57 while still OPEN) can never be counted as a completed win no matter its sign — it only becomes a `GAIN`/`LOSS`/`BREAKEVEN` once a subsequent close event actually closes it (e.g. a final −$11 close brings the example to +$46, a `GAIN`, counted only at that point). This exact worked example is a regression test in `src/lib/accountLedger.integration.test.ts` against a real database. No event is ever double-counted or deleted — every roll leg stays in the append-only `CampaignEvent` history (see Campaign Tracker section).

**Weekly return vs. the 1% target** (`summarizeWeeklyReturns`): buckets each closed campaign's realized P/L by the ISO week it closed in and divides each week's P/L by a single fixed baseline (the account's current ledger-derived value). This is a **simple realized-return-per-week metric, explicitly not a time-weighted or money-weighted rate of return** — it does not adjust for deposits/withdrawals mid-period, and a fixed baseline understates return in early (smaller-account) weeks and overstates it in later (grown-account) weeks. This is a deliberate choice to avoid fabricating a precision the app cannot back up; a proper time-weighted return is future work once there is enough dated history to make one meaningful. When there are no closed campaigns with a known baseline, the Dashboard's realized-weekly strip shows `INSUFFICIENT HISTORY` rather than a fabricated projection.

**Current Performance cockpit helpers** (`summarizeCampaignProgress`, `tradingProfitFromAccountValue`, `summarizeContributionAdjustedGoal`): Tracker Performance keeps account value, realized P/L, current/mark-to-market P/L, and projected-OTM P/L as separate accounting concepts. Account value remains authoritative from `currentAccountValue()` (latest `BROKER_SNAPSHOT` when present, otherwise ledger-derived value plus closed realized P/L); open option premiums or marks are never added on top of that account value. `tradingProfitFromAccountValue()` reports `current account value - starting capital - net contributions`, so deposits, withdrawals, and manual adjustments never become returns. `summarizeContributionAdjustedGoal()` calculates the 1% target from dated capital-week exposure: a deposit or withdrawal changes the target base only from its own date forward.

**Open campaign marks:** current/mark-to-market P/L for an open CSP is shown only when the app has a credible cost-to-close source: either a current user's linked Schwab/CSV `BrokerRecord` position with a market value, or an exact `OptionContractSnapshot` match on underlying, expiration, strike, and PUT. If neither exists, the UI says the current mark is unavailable. Projected-OTM P/L is separate and applies only to open CSPs with an active short put and no assigned shares; assignment and covered-call phases are not treated as "OTM success."

**Privacy invariant:** Performance is deliberately read from an always-"mine"-scoped query, never the Mine/Buddy/Both scope-filtered campaign list — so setting the Tracker scope to "Both" can make a buddy's shared campaigns *visible* in Open/History, but can never blend into *your own* win rate, weekly return, or realized P/L figures. Proven by `src/lib/accountLedger.integration.test.ts`.

---

## Schwab API

- **Current status: the per-user credential-routing/CSV normalization slice (migration `20260831235050_schwab_credential_routing_broker_records`) is live in production**, shipped in commit `5cf6c0b`.
- Enabled products: **Market Data Production** and **Accounts and Trading Production**.
- **Order Limit: 0.** Even though the Schwab app includes the Accounts and Trading product, Off Shift Options remains strictly read-only.
- Registered production callback URL: `https://offshiftoptions.com/api/schwab/callback`.
- Official Schwab docs/API checks were refreshed before implementation. Schwab public docs identify OAuth 2 delegated access with `authorization_code` callback flow and refresh-token renewal; public snippets also state an Individual Developer is limited to one app and that a Schwab brokerage account is required to access Trader APIs. No public official PKCE requirement/support was found, so the implementation uses signed, expiring, per-user OAuth state and does not send PKCE parameters. No public official Schwab documentation found during this pass clearly permits OSO to route one user's market-data usage through another user's personal developer app/OAuth connection, so shared market-data fallback remains disabled (`DISABLED_POLICY_NOT_VERIFIED`) until explicit policy/legal confirmation exists.
- No Schwab credentials, secrets, OAuth tokens, authorization codes, refresh tokens, account hashes, or client IDs exist in this repository. Only env var names and placeholder examples are documented.
- OAuth routes:
  - `GET /api/schwab/connect` requires an authenticated OSO session, resolves the signed-in user's saved Schwab developer app first (falling back to the server-held OSO env app only when the user has no per-user app), creates a 10-minute signed HttpOnly state cookie scoped to the current user and selected developer credential ID, and redirects to Schwab's authorization endpoint.
  - `GET /api/schwab/callback` requires the same current OSO user, verifies returned state and cookie state, exchanges the code server-side with the same resolved Schwab app config, stores encrypted tokens in that user's `BrokerConnection`, associates the connection to the per-user developer credential when applicable, discovers account-number hashes server-side, marks the developer credential validated/invalid, and redirects back to `/account`.
- Token/developer-credential storage and refresh: `src/providers/schwab/crypto.ts` encrypts access/refresh tokens and per-user developer app secrets using AES-256-GCM with `SCHWAB_TOKEN_ENCRYPTION_KEY`; `src/providers/schwab/tokens.ts` refreshes near-expiry access tokens using the connection's original developer credential when present and marks a connection `EXPIRED` if refresh fails. Token, credential, and account-hash errors are sanitized before reaching UI.
- Market data provider: `src/providers/schwab/market-data.ts` implements the existing `MarketDataProvider` interface for quotes, price history, option chains, instruments, and market hours. `src/providers/schwab/normalizers.ts` maps Schwab payloads into OSO's internal quote/candle/option snapshots, including bid/ask/mark/last/open interest/volume/Greeks where present. OSO still calculates Wilder RSI, Bollinger Bands/position, spread %, distance OTM, RoR, annualized RoR, setup score, PASS/FAIL/UNKNOWN, and near-match diagnostics.
- Provider resolution:
  - `resolveMarketDataProviderForUser(userId)` uses only the authenticated user's own connected Schwab OAuth/token and wraps public/reference market-data calls in a provider-keyed cache (`src/providers/market-data/cache.ts`) so repeated quotes/history/chains/instruments/hours are reused where safe. It does not hardcode undocumented Schwab rate limits.
  - Shared OSO market-data fallback is architecturally reserved but currently disabled because current public Schwab documentation did not verify that usage.
  - `resolvePersonalBrokerProviderForUser(userId)` uses only that user's own Schwab broker authorization. It never falls back to another user's OAuth connection for balances, positions, transactions, account history, account hashes, or observed orders.
- Controlled scanner integration: `rerunLiveSchwabScannerForUser()` uses `getSchwabMarketDataProviderForUser(userId)` and persists successful runs with `source = "LIVE:SCHWAB"`. If Schwab env vars/tokens/API calls are unavailable for every ticker, the action returns `LIVE DATA UNAVAILABLE` and does not silently use demo data. A single ticker's fetch failure no longer aborts the whole scan (see below) - it returns as an UNKNOWN candidate instead.
- **Scanner responsiveness (2026-09-03).** `evaluateLiveMarketScan` (`src/domain/scanner/live-scan.ts`) fetches quote/history and, for the shortlisted survivors, option chains via `mapWithConcurrency` (`src/lib/concurrency.ts`) instead of a one-ticker-at-a-time loop. `SCAN_FETCH_CONCURRENCY = 4` is a conservative, documented-in-code assumption (Schwab does not publish a per-connection concurrent-request limit) - adjust it if production use shows throttling or comfortable headroom. Staged fetching is unchanged: option chains are still only requested for the stock-stage shortlist, never the full universe. A per-ticker quote/history or option-chain failure is caught and that ticker is returned as an UNKNOWN candidate with a `scanNote`, rather than failing the entire scan; only a 100%-failed stock stage (e.g. auth outage) still throws `LIVE DATA UNAVAILABLE`. `persistScannerRun`'s per-candidate `ScanResult` inserts also run via `mapWithConcurrency` (limit 6, a local-Postgres-pool bound, unrelated to the Schwab-facing limit). `rerunLiveSchwabScannerForUser` now returns `{ scanned, nearMatches, elapsedMs }` for the UI instead of void.
  - The "Run Live Scan" button (`src/app/(app)/scanner/live-scan-button.tsx`) is a client component that shows a pending state (`Scanning…`, disabled, spinning icon) the instant it's clicked, before the server responds; an `aria-live="polite"` status region announces an elapsed-second counter while pending and `{scanned} scanned · {nearMatches} near matches · {elapsedMs}s` on success (auto-clears after 8s) or a concise error with Retry on failure. The refresh icon only spins under `motion-safe:` (Tailwind), so `prefers-reduced-motion: reduce` gets a static icon with the same text updates. `runLiveSchwabScannerAction` returns `{ ok, ... }` / `{ ok: false, error }` instead of redirecting, matching the Research quick-action pattern.
  - No real per-stage progress (e.g. "8/13 tickers") was built: Server Actions return once at the end of the request, and streaming that back would need SSE/job-queue infrastructure disproportionate to this slice. The elapsed-timer + static status text is the intentional fallback.
  - Scanner already showed the most recent persisted run immediately on navigation with a "Last run: ..." timestamp before this slice (`src/app/(app)/scanner/page.tsx`) - confirmed via a Playwright test that results survive a Scanner -> Research -> Scanner round trip, no code change was needed there.
  - No live Schwab OAuth connection is available in this dev environment, so the ~13s scan duration Matt reported couldn't be reproduced against real Schwab latency. Profiling instead used `scripts/scanner-benchmark.ts` (not part of the app - run with `npx tsx scripts/scanner-benchmark.ts`): real local-Postgres persistence timing, and a demo-data provider wrapped with a clearly-labeled *simulated* 400ms per-call delay (an estimate, not a measured Schwab figure) to isolate the real, connection-independent architectural effect of bounded concurrency. Representative run: fetch stage medians ~10.7s sequential -> ~1.6s at concurrency 4; a same-session warm re-run inside all cache TTLs dropped to ~0ms; persistence medians ~90ms sequential -> ~28ms at concurrency 6 for the 13-ticker starter universe. The fetch stage (quote/history/option-chain calls) dominates total scan time by a wide margin over persistence; whatever the real bottleneck is on Matt's ~13s scans, it is almost certainly network/Schwab-latency-bound rather than app/DB-bound, and the concurrency change should meaningfully cut it without changing scanner results (`live-scan.test.ts` has an equivalence test proving serialized vs. concurrent fetches produce identical candidates).
- Broker-read foundation: `src/providers/schwab/broker-read.ts` implements read-only accounts, account detail/positions, transactions, and historical order observation behind `BrokerReadProvider`.
- Per-user developer credential portal: `/account` -> Brokerage Connections -> Schwab -> Configure developer app lets a signed-in user enter their own app key/client ID, client secret, and callback URL. The browser is only an HTTPS entry form; the server action authenticates the user, validates, encrypts, and stores ciphertext in `SchwabDeveloperCredential`. The UI shows configured state, last validation time, replace, and remove controls only. It never renders stored secrets, stores them in browser storage/client cookies/URLs, uses `NEXT_PUBLIC_`, or logs them.
- **Schwab CSV normalization - verified against real exports (2026-08-31).** Matt provided the actual content of one real Positions export, one real Transactions export, and one real Realized Gain/Loss export directly to this session (never saved to this Git workspace or any repository file - no real account numbers, balances, or specific holdings are recorded here or anywhere in this repo). `src/providers/schwab/csv.ts`'s header sets, preamble/title-row handling, quoting, date formats (`MM/DD/YYYY`, including the `"MM/DD/YYYY as of MM/DD/YYYY"` settlement-date variant on cash rows), currency formatting (`$`, `,`, parenthesized negatives), and the OCC-adjacent option-symbol format all matched the real files exactly with **zero parser changes required** - only the sanitized fixtures (`src/providers/schwab/__fixtures__/*.csv`) were expanded from 2 to 3 positions/7 transactions to match the real files' row counts more closely, using entirely fake tickers/company names. `detectSchwabCsvExportType` distinguishes the three export types by their required header sets alone, never by filename. `parseSchwabPositionsCsvWithDiagnostics`/`parseSchwabTransactionsCsvWithDiagnostics`/`parseSchwabGainLossCsvWithDiagnostics` additionally report which raw rows were unreadable (`InvalidCsvRow`) without crashing the rest of the file. Records get deterministic fingerprints, a **separate `identityKey`** (same real-world "slot" - e.g. same account+date+symbol+action - without the financial fields) for conflict detection, stable API source IDs where available, and source provenance (`SCHWAB_API`, `SCHWAB_TRANSACTIONS_CSV`, `SCHWAB_POSITIONS_CSV`, `SCHWAB_GAINLOSS_CSV`). Positions CSV + API current position can reconcile to one current representation without doubling quantity; transactions CSV + API can merge when confidently identifiable; Gain/Loss CSV is validation/reconciliation metadata only and must not add realized P/L a second time. `src/domain/finance/brokerTransactionActions.ts` classifies each transaction's Action text into a fixed vocabulary (`SELL_TO_OPEN`, `BUY_TO_CLOSE`, `DIVIDEND`, `INTEREST`, `TRANSFER`, etc.) and returns `UNKNOWN` for anything unrecognized rather than guessing.
- **Account sync** (`syncSchwabAccountForUser` in `src/lib/workflows.ts`): pulls the authenticated user's own Schwab accounts/balances only, upserts a `TradingAccount` per Schwab account (`source: "SCHWAB"`, keyed on `(userId, externalAccountId)` so re-syncing never duplicates), and writes a `BROKER_SNAPSHOT` ledger entry each sync — never fabricates a value Schwab didn't return, and a fetch failure is recorded (`recordSchwabAccountSyncResult`) and surfaced honestly rather than silently retried or masked. `/account`'s primary, low-risk action is **"Sync now"**; "Reconnect Schwab" and raw token-expiry/callback-URL details are demoted into a collapsed "Connection details" section; "Disconnect" is visually muted/deliberate, not a primary button.
- **Open broker positions**: `getSchwabOpenPositionsForUser` returns the authenticated user's own Schwab positions (symbol, quantity, market value, account label, plus `assetType`/`putCall`/`strikePrice`/`underlyingSymbol` straight from Schwab's own instrument object when present). The Tracker's Open tab labels each Schwab position `Linked to Campaign` (durably reconciled - see below), `Possible match` (same underlying ticker as an open campaign, not yet reconciled), or `Unlinked`.
- **Import Schwab Data** (`/positions?view=accounts`, available to any authenticated user - never "Eric import"): a CSV upload flow with a strict preview-before-persist lifecycle:
  1. **Preview** (`previewSchwabImportForUser` / `previewSchwabImportAction`) - validates the upload (`.csv` extension, 5 MB cap, 20,000-row cap, rejects binary/NUL-byte content), detects the export type from headers, parses+normalizes it, classifies every row against the user's existing `BrokerRecord`s, and stores a `BrokerImportBatch` row with `status: PENDING_PREVIEW` holding the classified rows (already-normalized field values only - **the raw uploaded bytes are never persisted anywhere**). Selecting/uploading a file never mutates financial history by itself.
  2. **Classification** (`src/lib/broker-import.ts`, `classifyCandidates`): every candidate row becomes `NEW`, `DUPLICATE`, `CONFLICT`, `NEEDS_REVIEW`, or `INVALID` - never guessed:
     - `fingerprint` match (exact same economic fact, or same current-position identity for a `POSITION` row) -> `DUPLICATE` (or, for a `POSITION` row whose new `observedAt` is strictly newer with different values, a **superseding update** classified `NEW` - quantities are never summed across snapshots, an older or equal-time snapshot is always `DUPLICATE`).
     - `identityKey` match with a *different* `fingerprint` (same account+date+symbol+action, but different quantity/price/amount) -> `CONFLICT`. The original row is never overwritten; the conflicting version is stored as a separate row with `BrokerRecord.status = CONFLICT` for manual review.
     - No match at all -> `NEW` (or `NEEDS_REVIEW` if the transaction's Action text isn't in the recognized vocabulary).
     - A row the parser could not read at all -> `INVALID` (reported in the batch counts, never persisted).
  3. **Confirm** (`confirmBrokerImportForUser` / `confirmSchwabImportAction`) - re-runs classification against the *current* database state (closes a preview/confirm race window), persists `NEW`/`CONFLICT`/`NEEDS_REVIEW` rows as `BrokerRecord`s (skipping `DUPLICATE`/`INVALID`), wraps each insert/update in a unique-constraint-conflict catch (a concurrent duplicate insert is swallowed, not a hard failure), then marks the batch `CONFIRMED` and **clears its temporary preview payload** - the persisted `BrokerRecord` rows are the only durable result from this point on.
  4. **Discard** (`discardBrokerImportForUser` / `discardSchwabImportAction`) - marks the batch `DISCARDED` and clears its preview payload; no `BrokerRecord`s are ever created.
  - Database-level uniqueness (`@@unique([userId, provider, kind, fingerprint])`, unchanged from the prior slice) backs the dedupe so concurrent imports/syncs can't race their way into duplicate rows.
  - Filenames are display-only (`safeOriginalFilename`, sanitized) and are never used as economic identity - re-uploading the identical content under a different filename is still fully idempotent.
- **Broker Activity Awaiting Review** (`/positions?view=accounts`, `src/lib/broker-reconciliation.ts`): surfaces the user's own confirmed, currently-open, unlinked `BrokerRecord` `POSITION` rows (only the latest snapshot per account+symbol counts as "current"), each showing broker evidence (symbol, quantity, matching transaction count) and a proposed OSO Campaign pre-filled from the opening "Sell to Open" transaction where one was imported (left blank - never guessed - when no such evidence exists). The user can **Confirm** (creates the Campaign through the existing `createCampaignForUser` workflow - no second P/L engine - then durably sets `BrokerRecord.linkedCampaignId`), edit any proposed field first, or **Skip / Leave unlinked** (`reconciliationDismissedAt`, never creates Campaign history). Verified end-to-end with a real file upload in a browser (Playwright + manual screenshots).
- **Dashboard/Tracker double-count fix**: once a broker position is linked to a Campaign, `splitBrokerPositionsByCampaignLink` (matched on the normalized OCC-style option symbol itself, not ticker-only and not the internal per-source account key) removes it from the Dashboard's raw "Open positions"/"Secured (CSP)" broker-position count, since the linked Campaign already represents that exact position. An **unlinked** broker position still counts additively alongside OSO campaigns, exactly as before - this is the documented, safest-available interim behavior for anything not yet reconciled.
- **OCC option symbol parsing** (`src/domain/finance/occOption.ts`): Schwab renders option positions with a raw OCC symbol (e.g. `RIOT 260904P00017500`); `parseOccOptionSymbol` turns that into `{ underlying, expiration, optionType, strike }` (tested against real padding/case variants and invalid input), and `describeBrokerPositionForDisplay`/`formatOccOptionSymbol` render it as "RIOT · Sep 4, 2026 · $17.50 Put" with "-1 contract" (not "-1 sh") for an option quantity. The original raw symbol is always preserved on the `BrokerPosition` object; only the display layer reformats it.
- **CSP secured capital from broker positions** (`src/domain/finance/brokerPositions.ts`, `summarizeCspSecuredCapital`): counts `strike × 100 × |short contracts|` only for positions confidently identified as a short put — preferring Schwab's own `putCall`/`assetType`/`strikePrice` instrument fields when present, falling back to the OCC symbol parse only when Schwab didn't supply them. Long puts, calls, equity, and any position that can't be confidently classified (`classifyBrokerPosition` returns `"UNKNOWN"`) are excluded and never guessed; `hasUnknown` on the summary tells the Dashboard to show the total as a floor (`$X+`) rather than presenting it as complete. No existing "authoritative collateral" field is captured in the normalized Schwab position data today, so the strike-based calculation is the only source — this is documented, not a placeholder for a field we're ignoring.
- **Dashboard Open Positions / Secured CSP**: the Dashboard fetches `getSchwabOpenPositionsForUser` directly (same pattern as the Tracker), passes the result through `splitBrokerPositionsByCampaignLink` to drop any position already linked to a Campaign (see "Dashboard/Tracker double-count fix" above), and adds the remaining **unlinked** broker positions into both "Open positions" (`computeOpenPositionsCount`) and "Secured (CSP)" (`summarizeCspSecuredCapital`). Only genuinely unreconciled positions are still additive; a real-world trade the user has explicitly confirmed as a Campaign is counted exactly once. A small `LIVE SCHWAB` / `MANUAL` / `MIXED` badge next to the Account Value stat shows whether the displayed account metrics are live-synced, manually entered, or a mix — a live broker value is never allowed to look like manually-entered data.
- Schema for this slice: `SchwabDeveloperCredential`, `BrokerConnection.developerCredentialId`, `BrokerRecord` (now with `status`, `identityKey`, `importBatchId`, `linkedCampaignId`, `reconciliationDismissedAt`), and `BrokerImportBatch` are all in the single migration `20260831235050_schwab_credential_routing_broker_records` (applied to both local Docker and production Supabase). This migration was regenerated/consolidated twice during development (each time via a local-only `prisma migrate reset`, run only with the user's explicit per-invocation consent per Prisma's own AI-safety guard) so the whole feature stayed one coherent migration rather than several partial ones.
- **Architectural rule (do not violate):** read-only. Allowed methods are read-oriented only (`getQuote`, `getPriceHistory`, `getOptionChain`, `getInstrument`, `getMarketHours`, `getAccounts`, `getPositions`, `getTransactions`, `getOrders` for historical observation only). Forbidden: `placeOrder`, `submitOrder`, `replaceOrder`, `cancelOrder`, preview-for-execution endpoints, algorithmic execution, automated trading — do not add these unless Matt explicitly changes this requirement.
- **Brokerage account data is user-scoped:** Matt's Schwab OAuth connection, balances, positions, transactions, imported records, campaigns, account settings, performance, projections, and trading-result achievements belong to Matt. They must never populate Eric's account, affect Eric's settings, appear as Eric's positions, change Eric's account value/history, or automatically become shared. Eric can continue using manual accounts/trades/imports without connecting Schwab; if he later wants automatic Schwab account sync, he authorizes his own Schwab account through the same OSO Schwab developer app.
- **Sharing is separate from ownership:** Matt can share a campaign/account summary with Eric, but the owner remains Matt and the underlying brokerage connection/tokens remain Matt-owned. Shared visibility never makes a campaign count toward Eric's balance, P/L, win rate, projections, or trading-result achievements.
- **Investigated 2026-09-04: Eric being "prompted" for his own Schwab connection when using Scanner is not a bug and not a regression - it is the pre-existing, intentionally-disabled shared-market-data policy guard working as documented.** Matt noticed Eric gets a Schwab-related message in Scanner; traced the exact path: clicking "Run Live Scan" calls `rerunLiveSchwabScannerForUser(userId)` (`src/lib/workflows.ts`), which calls `getSchwabMarketDataProviderForUser(userId)` → `resolveMarketDataProviderForUser` (`src/lib/broker-connections.ts`), which looks up **only that user's own** `provider:SCHWAB, status:CONNECTED` `BrokerConnection` row - there is no code path anywhere that could substitute another user's connection. With none found, it returns `{ provider: null, source: "UNAVAILABLE", sharedFallback: "DISABLED_POLICY_NOT_VERIFIED" }`, and the workflow throws `"LIVE DATA UNAVAILABLE: connect Schwab in Account settings with usable Schwab developer credentials."`, shown as a small inline red message under the "Run Live Scan" button (`live-scan-button.tsx`) - **only after that explicit opt-in click, never on page load, never blocking the rest of Scanner.** The Scanner page itself (demo data, candidate list, filters, columns) works fully for Eric with zero Schwab connection, verified live in a browser.
  - **This is intentional, not a bug**: `docs/SCHWAB_INTEGRATION.md` already documents (from an official-Schwab-docs check dated 2026-08-31) that "no public official Schwab documentation found... clearly permits OSO to route one user's market-data usage through another user's personal developer app/OAuth connection," so the shared-market-data fallback the product would ideally have (any authenticated user without their own connection transparently uses a permitted shared OSO feed) **remains unbuilt pending that policy verification** - this session did not invent permission or build it, per explicit instruction. In this local sandbox specifically, Matt's own seeded account also has no real `provider:SCHWAB/status:CONNECTED` row (only a leftover `MOCK` row from earlier test fixtures) - live-browser-verified that Matt hits the exact same message Eric does, confirming there is no Eric-specific regression at all; both users are equally affected by the absence of a live connection in this environment, which is expected and already documented elsewhere in this file.
  - **No shared fallback was implemented and no UI copy was changed** - changing the "unavailable" message to imply a shared feed exists (or is available to Eric) when none does would misrepresent capability. Personal brokerage data (accounts/positions/transactions/orders/tokens) was reconfirmed to have zero shared-fallback code path at all (`resolvePersonalBrokerProviderForUser`'s own return type has no `sharedFallback` variant, structurally, unlike the market-data resolver) - `docs/SCHWAB_INTEGRATION.md` already states this never falls back to another user's connection.
  - Added tests: `workflows.integration.test.ts` (+1 - `rerunLiveSchwabScannerForUser` throws the honest `LIVE DATA UNAVAILABLE` error for a user with no connection and creates no `LIVE:SCHWAB` scan run as a side effect); the 4 pre-existing tests in `broker-connections.integration.test.ts` (own-provider routing, cross-user market-data isolation with the `DISABLED_POLICY_NOT_VERIFIED` reason, personal-broker cross-user isolation, one user's connection deletion not affecting the other) already fully covered the rest and needed no changes. Confirmed no client component anywhere imports `broker-connections.ts` (which is itself `import "server-only"`) - provider objects/tokens structurally cannot reach the browser.
  - **Next step if this is ever prioritized**: get an explicit, current answer from Schwab (developer support, ToS re-read, or written guidance) on whether one authorized user's OAuth token may serve read-only market-data requests on behalf of other authenticated users of the same app, before writing any shared-provider code. Until then, `resolveMarketDataProviderForUser`'s `UNAVAILABLE`/`DISABLED_POLICY_NOT_VERIFIED` result is the correct, honest behavior for any user without their own connection - including Eric.
- Any Schwab client secret, authorization code, access token, refresh token, account hash, or broker-specific identifier must stay server-side, never in client-side JavaScript, logs, error pages, analytics, screenshots, project docs, or Git. Store Schwab access/refresh tokens encrypted at rest with a dedicated server-side key.
- **TEMPORARY diagnostic: Schwab Fundamental Fields verification (2026-09-03/04 slice, started by another agent that hit its usage limit, finished this session).** `/account/schwab-fundamentals` - a protected, read-only engineering instrumentation page, not a normal product feature. It exists because local dev environments have never had a live Schwab OAuth connection (confirmed repeatedly this engagement), so no agent could verify which `fundamental`/`reference` fields Schwab's real API actually returns; Matt's production account has a working live connection and can run this once to get a real answer.
  - **What it does**: calls the exact same request the app's own `SchwabMarketDataProvider.getQuote()` already makes - `GET /marketdata/v1/quotes?symbols=APLD,RIOT,CORZ&fields=quote,reference,regular,fundamental` (`src/providers/schwab/market-data.ts:23-31` already requests `fundamental`/`reference`; the normalizer only ever reads `quote`/`regular` and silently discards the rest) - and reports, per ticker, an explicit ALLOWLIST of safe fields only (`src/providers/schwab/fundamentals-diagnostic.ts`: `REFERENCE_FIELDS`, `FUNDAMENTAL_FIELDS`, `PRICE_FIELD_DEFINITIONS`). It never enumerates the raw response object - a field not on the allowlist can never reach the page, no matter what Schwab returns.
  - **ABSENT vs PRESENT_NULL vs PRESENT_VALUE is preserved exactly**, never collapsed to 0/false/"" (`observePath` in `fundamentals-diagnostic.ts`). A 4th state, `PRESENT_UNDISPLAYED`, additionally redacts any allowlisted value that happens to look like a token/secret/credential (`BLOCKED_VALUE_PATTERN`) or isn't a primitive - defense in depth beyond what was strictly asked for, kept because it costs nothing and only makes leakage harder.
  - **Auth/scoping**: `src/app/(app)/account/schwab-fundamentals/page.tsx` is a Server Component that calls `requireCurrentUser()` (redundant with the `(app)` layout's own check - matches this codebase's existing per-page convention) and passes only that user's own `user.id` into `runSchwabFundamentalsDiagnosticForUser` (`src/lib/schwab-fundamentals-diagnostic.ts`), which resolves the connection via the existing `findSchwabMarketDataConnectionForUser`/`getValidSchwabAccessTokenForConnection` (never a different user's token, never a shared/developer-app fallback beyond what those existing functions already do). No browser-supplied user id anywhere.
  - **Nothing is ever saved.** Zero `prisma` write calls exist in any of the three new files (grepped and confirmed). Research, Scanner, `WatchlistItem.fundamental*`/`manual*`, `UserSettings`, `BrokerRecord`, `Campaign`, accounts, positions, and transactions are all completely untouched by this page.
  - **Failure is sanitized**: no connection -> "Connect Schwab in Account before running this read-only diagnostic." A 401 -> "reconnect" message; a 429 -> rate-limit message with only the safe `Retry-After` value; anything else -> a generic "failed safely" message. The raw Schwab response, access/refresh tokens, Authorization header, and client secret are never logged, never thrown into an error, and never reach the browser - confirmed by dedicated tests (see below) that assert the token/secret strings never appear anywhere in the returned result.
  - **Entry point**: a small "Verify Schwab Fundamental Fields" link in Account -> Brokerage Connections -> Schwab -> Connection details, shown only when a Schwab connection is actually `connected` (so it stays invisible/unobtrusive for everyone else, including both dev users right now, since neither has a live SCHWAB connection).
  - **Field availability was verified against production 2026-09-04** - Matt ran this after-hours against APLD/RIOT/CORZ on his real, live-connected account. Results (and what was auto-populated from them) are in the "Verified fundamentals" bullet above/below. The diagnostic itself stays deployed and unchanged - re-run it any time to spot-check, or to check other tickers by editing `SCHWAB_FUNDAMENTALS_DIAGNOSTIC_TICKERS`.
  - **How Matt should use it and what to paste back**: after this slice is reviewed and deployed, open Account -> Brokerage Connections -> Schwab -> Connection details -> "Verify Schwab Fundamental Fields" while already connected. Copy the "Allowlisted Fields" table's contents (PRESENT/PRESENT NULL/ABSENT per field per ticker) back verbatim - it contains no tokens/secrets/account data, so it's safe to paste directly. Do this once during regular market hours and once while the market is closed, since the diagnostic also reports which of it ran during. Whichever fields come back PRESENT with a real value for all three tickers are candidates to promote from MANUAL to AUTO in Research in a future slice (with explicit unit verification for anything ratio-like, e.g. Debt/Equity, before trusting the scale); anything ABSENT stays MANUAL indefinitely - it means Schwab's Trader API genuinely does not provide it.
  - Tests: `src/providers/schwab/fundamentals-diagnostic.test.ts` (10 unit tests - allowlist/ABSENT/PRESENT_NULL/PRESENT_VALUE/redaction/price-fallback/`getInstrument()`-fallback correctness, no DB needed), `src/lib/schwab-fundamentals-diagnostic.integration.test.ts` (6 DB-backed tests - no-connection safe message, strict per-user connection scoping, token/secret never present in the OK report, sanitized 401/429 handling, zero writes to Research/Scanner/BrokerRecord/Campaign data), `tests/e2e/schwab-fundamentals-diagnostic.spec.ts` (4 Playwright tests - unauthenticated redirect to login, the Account entry point staying hidden with no connection, the safe no-connection message on the page itself with no raw provider detail in its text, and that visiting the diagnostic never perturbs Research/Scanner).
- **Auto Schwab fundamentals wired into Research (2026-09-04 slice, no schema change, no migration).** Matt's real production diagnostic run confirmed exactly which `fundamental`/`reference` fields Schwab's Trader API returns for equities (APLD/RIOT/CORZ, after-hours): present with real values - `peRatio`, `eps`, `divAmount`, `divYield`, `divFreq`, `description`, `avg10DaysVolume`, `avg1YearVolume`; genuinely absent - `peg`/`pegRatio`, `debtToEquity`, `currentRatio`, every dividend date field, every net-income/margin/profitability field, and every rating/grade/recommendation field (Schwab's own or LSEG's). Only the verified-present fields were wired up:
  - **P/E, EPS, Dividend amount/yield are now persisted onto `WatchlistItem.fundamentalPeRatio`/`fundamentalEps`/`fundamentalDividendAmount`/`fundamentalDividendYield`** (the reserved-auto columns from the prior slice - no new columns). Written by a new `syncVerifiedFundamentalsForUser` (`src/lib/workflows.ts`) called once at the end of `rerunLiveSchwabScannerForUser`, for tickers that are BOTH in the scan AND already in that user's own Research - **zero new Schwab requests**, since it reuses the `fundamental`/`reference` groups the live scan's `getQuote()` call already fetches and previously discarded. Never creates a new Research row just because a ticker appeared in a scan. A field is only ever written when this fetch actually returned a real value (including a real negative or a real 0) - a transient miss on one scan can never blank out a value captured on an earlier successful one. `fundamentalSource`/`fundamentalAsOf` are set to `"Schwab Trader API"` and the scan's timestamp whenever any of the four fields is written.
  - **Company name and dividend frequency have no reserved auto column** (only `companyName`, which is manual-only, and no `divFreq` column at all) - rather than add one, they flow through the exact same ephemeral, non-persisted mechanism Current Price already used: `MarketQuote.companyDescription`/`fundamentals.dividendFrequency` (new optional fields, `src/providers/market-data/types.ts`) are captured into the live scan's per-ticker `values` (`src/domain/scanner/live-scan.ts`), which `persistScannerRun` already serializes into `ScanResult.snapshotJson` with zero code change there, and Research reads them back the same way it already reads Current Price (`ResearchScanSnapshot.companyDescription`/`dividendFrequency` in `research/page.tsx`). Manual `companyName` is preserved untouched and used as the display fallback (`resolveAutoOrManual`) whenever the latest scan didn't include the ticker or returned no description - the manual value is never overwritten or deleted.
  - **Scanner scoring is unaffected by construction, not just by testing**: the new fields live in `LiveScanCandidate.verifiedFundamentals` (a sibling to `values`, never merged into it) and two extra keys in `values` (`companyDescription`, `dividendFrequency`) that no Scanner rule name references - `evaluateCandidate` only ever looks up the specific keys its active rules name, so an unreferenced extra key is structurally inert. Verified by re-running the full existing `scanner.spec.ts`/`live-scan.test.ts` suites unchanged (all still pass) plus a new test proving identical `summary` output with and without fundamentals present.
  - **Negative P/E is now displayed as the real number, not hidden as "N/M"** - Schwab genuinely returns negative P/E for companies with negative earnings (all three test tickers had negative P/E and EPS), and that's meaningful, not noise. Only an exact 0 still shows "N/M" (never a value a real quote should produce). The existing P/E InfoTip (`FundamentalInput` in `research-workspace.tsx`) was updated to explain this. PEG's display is unchanged (still shows "N/M" for zero/negative) since PEG stays fully manual/unverified.
  - **Dividend display now prefers real Schwab data over the manual `paysDividend` judgment when it exists** - a verified `$0.00`/`0%` is a real, confirmed "does not pay a dividend," not a missing answer, so it's shown directly (with dividend frequency alongside) rather than falling back to a `—` just because the user never set the manual yes/no flag. This never writes to or overrides `paysDividend` itself - only changes what's displayed when auto data is present.
  - **EPS is a new optional Research column** (`src/domain/research/columns.ts`, FUNDAMENTALS group) - auto-only (no manual counterpart exists), not added to Matt's or Eric's default layout (available via Columns for anyone who wants it, per the task's explicit "don't bloat the default" instruction).
  - **`numberValue` in `src/providers/schwab/normalizers.ts` had a latent bug fixed**: `Number(null)` is `0` (finite), so an explicit `null` fundamental value would previously have been silently written as `0` instead of treated as absent. Now explicitly guarded. No real Schwab response in this verification run actually exercised this case (absent fields were omitted keys, not explicit nulls), but the fix closes the gap for any future response shape.
  - Tests: `src/providers/schwab/normalizers.test.ts` (+3 tests using Matt's real verified values - negative P/E/EPS and real-zero dividend fields survive normalization exactly, a genuinely-absent field maps to null never 0, no reference/fundamental group at all leaves both unset), `src/domain/scanner/live-scan.test.ts` (+1 test - fundamentals carried as a sibling to values with zero effect on scoring), `src/lib/research.integration.test.ts` (+4 DB tests - real negative/zero values persist exactly, a later ABSENT fetch never overwrites a previously-captured real value, writes are strictly scoped to the calling user's own existing rows with no new-row creation and no cross-user leakage, a ticker with no verified fundamentals is a no-op), `tests/e2e/research-customization.spec.ts` (+2 Playwright tests - negative P/E displays as a real number with the updated InfoTip visible, EPS is Columns-only and a manual dividend yield of exactly 0 displays as "0.00%" not blank). Also fixed a real, pre-existing test-isolation bug found while adding these: an existing test left Matt's P/E column toggled on with no cleanup, leaking into later test runs - fixed, and all Columns-menu toggles in this spec now use idempotent `setChecked`/open-if-not-visible helpers instead of blind clicks.
  - **Company/Industry auto-population from `getInstrument()` remains intentionally NOT wired up** - unrelated to this fundamentals verification (a different Schwab endpoint), and its real response shape has still never been verified against a live connection. Left for a future slice if ever prioritized.

---

## Alpha Vantage API

A second, independent fundamentals data source - added specifically to cover the fields Schwab's Trader API does not expose (PEG, Debt/Equity, Current Ratio, profitability, analyst target/recommendation counts - all confirmed genuinely absent from Schwab in the fundamentals-wiring slice above). Alpha Vantage's free tier is 25 requests/day **total**, shared across both users and the Scanner - every design decision below exists to protect that budget.

- **TEMPORARY diagnostic: Alpha Vantage OVERVIEW fields verification (2026-09-03, paced 2026-09-03/04).** `/account/alpha-vantage-fundamentals` - a protected, read-only engineering instrumentation page, click-to-run rather than automatic-on-load (a page that fetched on every load/navigation could burn the whole daily budget by itself). Verifies `function=OVERVIEW` for APLD/RIOT/CORZ against `ALPHA_VANTAGE_API_KEY`, sequentially, never in parallel, with a mandatory **1300ms pace between calls** (`ALPHA_VANTAGE_REQUEST_DELAY_MS` in `overview-diagnostic.ts`) - a real production run without pacing drew Alpha Vantage's "Please consider spreading out your free API requests more sparingly (1 request per second)" throttle on the 2nd call; the diagnostic's own `RATE_LIMITED` early-stop correctly caught it, but the fix is not to trigger it in the first place. A second button, "Verify remaining tickers (RIOT, CORZ)", re-checks only those two without ever re-spending a call on an already-succeeded ticker.
  - **Field availability VERIFIED against real production Alpha Vantage (2026-09-04, after the pacing fix): APLD, RIOT, and CORZ all returned real OVERVIEW data.** Present-with-real-values across all three: Symbol/Name/Description, Sector, Industry, MarketCapitalization, PERatio, EPS, DividendPerShare, DividendYield, ProfitMargin, OperatingMarginTTM, ReturnOnAssetsTTM, ReturnOnEquityTTM, RevenueTTM, GrossProfitTTM, QuarterlyEarningsGrowthYOY, QuarterlyRevenueGrowthYOY, AnalystTargetPrice, the 5 analyst rating counts, BookValue, PriceToBookRatio, EVToEBITDA, Beta. **PEGRatio is present but not universally available** - APLD returned `1.829`, RIOT and CORZ both returned Alpha Vantage's own `"None"` null sentinel (preserved as NULL, not 0 or a fabricated value) - so PEG has a real per-ticker auto value when Alpha Vantage has one, with the user's manual entry remaining the fallback otherwise. **CurrentRatio, QuickRatio, and DebtToEquity are confirmed genuinely absent from OVERVIEW** (probed directly, not assumed) - these stay MANUAL indefinitely; a future slice could investigate Balance Sheet/SEC EDGAR if ever prioritized, but that is not started.
  - **How Alpha Vantage signals problems (no HTTP status codes for this)**: HTTP 200 even for rate-limiting/errors, signaled by JSON body shape - `"Note"`/`"Information"` string keys mean rate-limited/throttled, `"Error Message"` means a request-level error, and an object with no `Symbol` field means invalid symbol/key/plan restriction. **Alpha Vantage exposes no machine-readable "requests remaining" signal** on the free tier (checked for any rate-limit-shaped response header; none observed) - OSO tracks its own daily counter (see below) and never claims it authoritative if a call happens outside OSO.
  - **ABSENT vs PRESENT_NULL vs PRESENT_VALUE is preserved exactly** everywhere Alpha Vantage data is touched, end to end from the diagnostic through the shared cache to Research display - `"None"`/`"-"`/`""`/missing-key all normalize to real NULL, never 0; a real `"0"` always survives as numeric 0.
  - **Key safety**: `ALPHA_VANTAGE_API_KEY` is read server-side only (`getAlphaVantageApiKey()`), appended to the request URL only inside `fetchAlphaVantageJson`, never logged, and stripped from any provider message or field value that happens to echo it back (`sanitizeMessage`/redaction in both `overview-diagnostic.ts` and the production `overview.ts` normalizer - a real gap where a SUCCESS response's free-text field wasn't redacted was found and fixed by its own test during this slice). No `NEXT_PUBLIC_ALPHA_VANTAGE_API_KEY` exists anywhere.
  - Tests: `src/providers/alpha-vantage/overview-diagnostic.test.ts` (28 tests - allowlist/ABSENT/PRESENT_NULL/PRESENT_VALUE/None-sentinel/redaction/RATE_LIMITED-vs-ERROR_MESSAGE-vs-EMPTY classification/pacing (delay between calls, none before the first, none for a call skipped by an earlier throttle)/ticker-subset support/key-never-leaks).

- **Shared fundamentals cache + OSO-tracked daily budget + priority queue (2026-09-04 slice - committed and pushed as `599d11d`/`48b0940`).** The production system built on top of the verified diagnostic above. Core principle: Alpha Vantage fundamentals are public company data, so there is **one cache row per ticker, never per user** - Matt and Eric researching the same ticker (or it appearing in a scan) costs exactly one Alpha Vantage refresh, not one per user/context.
  - **Schema (migrations `20260904022520_alpha_vantage_fundamentals_cache_and_usage` + `20260904023015_alpha_vantage_run_lock`, both purely additive - two new tables, one added column, zero `ALTER`/`DROP` on any existing table)**:
    - `TickerFundamentals` (`ticker` is the primary key - same shared-by-symbol pattern as the existing `MarketQuoteCache`) stores normalized values only (never the raw Alpha Vantage response): all 28 verified OVERVIEW fields, `source`, `fetchedAt`/`staleAfter` (TTL/backoff), and `lastAttemptAt`/`lastAttemptStatus`/`lastErrorMessage` for observability. **A failed/empty/rate-limited attempt only ever updates the attempt-metadata fields - never the actual fundamental value fields** (`applyFetchResultToCache` in `src/lib/alpha-vantage-fundamentals.ts`), so a bad request can never fabricate or blank out real data. Contains no `userId`, research status, note, or campaign information anywhere - it is exclusively public company data.
    - `AlphaVantageDailyUsage` (`date` primary key, UTC calendar day) tracks `autoCount`/`manualCount` plus a `runningSince` single-flight lock column.
  - **Daily budget (`src/lib/alpha-vantage-budget.ts`): 25 total/day, auto capped independently at 22, manual implicitly guaranteed the remaining 3** - the "reserve 3 for manual" requirement is enforced by the auto side's own hard cap, not an artificial cap on manual (manual is a direct, trusted user action). **Concurrency-safe by construction**: every reservation is a single atomic `UPDATE ... WHERE <cap guard> RETURNING ...` SQL statement (`reserveAlphaVantageCall`), never a JS read-then-write, so Postgres itself serializes two concurrent requests racing for the last slot - proven by DB-integration tests firing 5 concurrent reservations at exactly 1 remaining slot and asserting exactly 1 succeeds. The reservation happens **before** the real HTTP call, never after - "tracked" means "a real attempt was made," not "it succeeded."
  - **Single-flight run lock**: only one caller (the automatic queue OR a manual refresh, from any request) may be mid-flight against the real Alpha Vantage API at once (`tryAcquireAlphaVantageRunLock`/`releaseAlphaVantageRunLock`), enforced the same atomic-UPDATE way - never by holding a long-lived DB transaction across the actual network calls. A stale lock (crashed process) self-heals after 5 minutes rather than deadlocking the feature.
  - **Priority queue (`getFundamentalsQueueCandidates`)**, privacy-safe by construction (every query selects `ticker` only - never `ownerId`/`researchStatus`/notes, so the queue can know a ticker needs public data without ever knowing which user researched it or why): (1) every ticker in any user's Research (any status), (2) PASS candidates from each user's own most recent scan, (3) the canonical Scanner Near tier (`getNearMisses(...).length === 1`, the exact same predicate the live scanner itself uses - see the 2026-09-04 architecture-review bullet below), (4) the remaining starter scanner universe - deduplicated, in that order, then filtered to exclude tickers with fresh (`staleAfter > now`) cached data.
  - **TTL: 7 days** (`ALPHA_VANTAGE_FUNDAMENTALS_TTL_MS`) - fundamentals are slow-changing; opening Research never refreshes anything by itself. A failed/empty attempt backs off 1 day (`ALPHA_VANTAGE_FAILURE_BACKOFF_MS`) so one permanently-invalid ticker can't get retried (and burn budget) on every queue run; a `RATE_LIMITED` outcome does **not** back off the ticker at all, since that's about OSO's own pacing, not the ticker.
  - **Execution triggers: the Account "Process fundamentals queue" button, and (2026-09-04) a protected scheduled-cron endpoint** - `POST /api/internal/alpha-vantage/process` (`src/app/api/internal/alpha-vantage/process/route.ts`) calls the exact same `processAlphaVantageFundamentalsQueue()` with no options (AUTO budget behavior) - not a second queue implementation. Neither trigger is ever called from Scanner, Research page render, navigation/prefetch, or `rerunLiveSchwabScannerForUser`/Run Live Scan. See the dedicated bullet below for the cron endpoint's auth/schedule/Hostinger status.
  - **Research integration reads the shared cache live at render time - never persists anything onto `WatchlistItem`** (`getTickerFundamentalsMap` in `research/page.tsx`, matching the same ephemeral pattern Current Price/Company already use). Precedence is explicit and never silently replaces Schwab: Company/Current Price/P-E/EPS/Dividend stay exactly as the prior Schwab-fundamentals slice left them (Alpha Vantage is not even consulted for those). Alpha Vantage fills only what Schwab doesn't provide:
    - **Industry** (existing "Industry / What" column): Alpha Vantage's `industry` is now the AUTO value, `whatItDoes` (manual) is the fallback - via `resolveAutoOrManual`, no schema change.
    - **PEG** (existing `pegRatio` column): three-tier precedence - Schwab's reserved-auto `fundamentalPegRatio` (always null today, confirmed absent from Schwab) -> Alpha Vantage's cached `pegRatio` -> the user's `manualPegRatio`.
    - **Profitability** (existing manual `PROFITABLE`/`MIXED`/`UNPROFITABLE` chip): unchanged as the primary value; its tooltip is enriched with Alpha Vantage's real profit margin/ROE when cached, never replacing the manual judgment.
    - **Two new, non-default Research columns** (`avAnalystTarget`, `avAnalystConsensus` in `columns.ts`, EXTERNAL group, available via Columns like EPS was, not added to anyone's default layout): "Analyst Target (Alpha Vantage)" and "Analyst Consensus (Alpha Vantage)" - **explicitly and only ever labeled "Alpha Vantage," never "LSEG"** (existing manual `manualLseg*` fields are completely untouched). Consensus is `summarizeAlphaVantageAnalystConsensus()` (`src/domain/research/alphaVantageAnalyst.ts`) - an explicit, tested weighted-average of Alpha Vantage's own Strong Buy/Buy/Hold/Sell/Strong Sell counts (+2/+1/0/-1/-2, bucketed by fixed thresholds), returning `null` (never a fabricated "Hold") when there is no analyst data at all.
  - **Manual refresh** (`refreshSingleTickerFundamentals`): refuses to spend a call on already-fresh data unless explicitly forced; draws from the `MANUAL` budget kind (the 3-call reserve once auto is at 22); protected by the same single-flight lock as the automatic queue.
  - **Account -> "Fundamentals Data Providers" panel** now shows (only when configured, kept compact): tracked-today/25, auto-remaining/22, the 3-call manual reserve, cached-ticker count, stale-or-missing-queued count, an `InfoTip` explaining the count is OSO-tracked and not authoritative, and the "Process fundamentals queue" button (`alpha-vantage-queue-button.tsx`).
  - **A real security gap was found and fixed by its own test during this slice**: the production OVERVIEW normalizer (`overview.ts`) redacted the API key from provider *messages* but not from *field values* (e.g. `Description`) on a SUCCESS response - a test asserting a sentinel key never appears anywhere in the result caught this; fixed by routing string fields through the same redaction path as messages.
  - **A real correctness bug was found and fixed by its own test**: the daily-budget and run-lock SQL originally used Postgres's own `now()` for staleness/timestamp comparisons instead of the function's injected `now` parameter, which silently broke correctness whenever a caller passed a non-real `now` (as every test does) - fixed by binding the JS `Date` parameter directly into the SQL instead of calling `now()`.
  - Tests (all passing, 316/316 full suite): `src/providers/alpha-vantage/overview.test.ts` (normalization + redaction), `src/domain/research/alphaVantageAnalyst.test.ts` (consensus bucketing), `src/lib/alpha-vantage-budget.integration.test.ts` (10 DB tests - auto/total caps, manual reserve guarantee, concurrent-reservation safety at the boundary, run-lock acquire/release/stale-recovery/concurrent-acquire), `src/lib/alpha-vantage-fundamentals.integration.test.ts` (24 DB tests - one row per ticker shared across users, queue dedup across Research+Scanner, privacy-safe queue output, freshness/staleness inclusion, None/zero normalization end-to-end, failed-fetch-never-fabricates, throttle stops the queue safely, budget-exhausted refuses to run at all, already-fresh manual refresh spends no call, canonical Near-tier reuse, stale-lock recovery at the processor level, lock release on provider failure/throttle, request-attempt counting never refunded).
  - **Not built this slice (explicitly out of scope, to be decided/prioritized separately)**: Alpha Vantage-derived fields in the expanded Research detail panel (only the dense-table columns were wired - Company/PEG/Industry/Profitability tooltip/the two new Analyst columns; the per-item expanded detail view still shows only what it already did).
  - **Post-approval architecture review (2026-09-04, same slice, before production migration): four required checks, all confirmed safe or fixed.**
    1. **Stale lock recovery** - already correct by construction (atomic reclaim-after-timeout via `tryAcquireAlphaVantageRunLock`, release always in a `finally` in both `processAlphaVantageFundamentalsQueue` and `refreshSingleTickerFundamentals`, so a thrown provider error or a `finally`-wrapped `return` on any outcome including `RATE_LIMITED` still releases the lock). `ALPHA_VANTAGE_RUN_LOCK_STALE_AFTER_MS` stays 5 minutes - reasonable for this low-volume, two-user queue. Added 4 new processor-level tests proving this concretely (a fresh lock blocks a second processor call; a stale one is reclaimed; the lock is released after a thrown provider error; the lock is released after a throttle response) rather than only testing the low-level lock function in isolation.
    2. **Request counting** - already correct by construction: `reserveAlphaVantageCall`/atomic budget reservation happens strictly BEFORE the outbound Alpha Vantage call in both the queue and manual refresh, and nothing anywhere decrements/refunds a reservation once made, regardless of outcome (SUCCESS/RATE_LIMITED/EMPTY/ERROR_MESSAGE/HTTP_ERROR all keep the reservation). Added 4 new tests proving a RATE_LIMITED, an EMPTY, and an HTTP 500 outcome each leave the tracked count incremented (not refunded), plus a test proving the reservation is visible to the outside world before the outbound fetch call even fires.
    3. **Counter wording/reset** - Account panel and its `InfoTip` reworded to never imply synchronization with Alpha Vantage's real quota: "OSO tracked: N / 25 calls," "Auto budget left (OSO tracked)," and "Resets with OSO's UTC usage day" replace the earlier "Tracked today"/"Auto budget remaining" phrasing; the `InfoTip` now reads verbatim "OSO tracks requests it makes. Alpha Vantage does not expose an authoritative remaining-call count or reset signal, so this may differ from the provider's actual quota."
    4. **Scanner Near definition** - the queue's Near-tier previously used an approximate proxy (any ScanResult failing more than one criterion by raw count). **Replaced with the actual canonical rule**: two new shared helpers, `parseStoredCriterionActualValue`/`parseStoredCriterionDesiredValue` (`src/domain/scanner/scanner.ts`), reconstruct a real `CriterionResult[]` from persisted `ScanCriterionResult` rows, then `getNearMisses(...).length === 1` - the exact same predicate `rerunLiveSchwabScannerForUser` already uses for its own `nearMatches` count. **A real, independently-discovered bug was fixed as part of this reuse**: `research/page.tsx`'s existing scan-snapshot reconstruction assigned `criterion.desiredValue` (a `JSON.stringify`'d string, e.g. `"[10,50]"`) directly into `CriterionResult.desiredValue` without `JSON.parse`, so `criterionGap()`'s `Array.isArray(desired)` check was always false for BETWEEN-operator criteria - meaning a genuine near-miss on any BETWEEN rule (3 of the `SCANNER_RULE_DEFINITIONS` use BETWEEN) silently got zero partial credit in Research's `honestSetupScore` display instead of the intended 0.72-point near-miss bonus. Fixed by routing both Research's snapshot builder and the new queue code through the same shared, correctly-`JSON.parse`-ing helper. Queue prioritization still has zero effect on Scanner's own score/status/label - this only changes fetch-priority ordering and (for Research) makes an existing score calculation honest again. New tests: `scanner.test.ts` (+4, including one reproducing the exact BETWEEN bug scenario end-to-end) and `alpha-vantage-fundamentals.integration.test.ts` (+3, a genuine near-miss is prioritized, a far miss is not, a BETWEEN tuple round-trips correctly).
  - **Scheduled cron trigger for the automatic queue (2026-09-04 slice)**: `POST /api/internal/alpha-vantage/process` (`src/app/api/internal/alpha-vantage/process/route.ts`) - a thin, protected wrapper that calls `processAlphaVantageFundamentalsQueue()` with no options (AUTO budget behavior), reusing every existing protection (22/day auto cap, 3-call manual reserve, single-flight lock with 5-minute stale recovery, 1300ms pacing, deduped priority queue, 7-day freshness, throttle handling) unchanged - not a second queue implementation.
    - **Auth**: a shared secret, `OSO_CRON_SECRET` (new server-only env var, never `NEXT_PUBLIC_`), sent as `Authorization: Bearer <secret>` or `X-OSO-Cron-Secret`, compared via `crypto.timingSafeEqual` (`src/lib/cron-auth.ts`) - never a plain `===`, so a wrong guess can't be timed to learn how many characters matched. A missing/wrong/unconfigured secret returns 401 **before** `processAlphaVantageFundamentalsQueue()` is ever called - zero Alpha Vantage calls, zero DB writes on that path. The secret is read once server-side, never logged, never echoed in any response, and the route strips ticker-level detail from its own response (only aggregate counts - `processed`/`callsUsed`/`reason`/`usage`).
    - **Response shape**: `{ processed, callsUsed, reason, usage: { autoCount, manualCount, totalCount, autoRemaining, totalRemaining } }`, HTTP 200 for every real outcome including an empty queue (`reason: "queue_empty"`) or budget exhaustion (`reason: "budget_exhausted"`) - only a genuinely unexpected failure returns 500, with a sanitized generic message (never a raw provider error or stack trace).
    - **Schedule (not yet configured in Hostinger - needs Matt)**: target **00:10 UTC daily**, shortly after `AlphaVantageDailyUsage`'s UTC-day key resets, e.g.:
      ```bash
      curl -s -X POST -H "Authorization: Bearer $OSO_CRON_SECRET" https://offshiftoptions.com/api/internal/alpha-vantage/process
      ```
      **This repository cannot confirm Hostinger's actual cron/scheduled-task capability for this Node.js hosting plan** - PROJECT_HANDOFF.md has never documented one existing (see the "no confirmed Hostinger persistent-worker or cron capability" note this replaces). If Hostinger's hPanel exposes a generic "Cron Jobs" feature that can run an arbitrary shell command on a schedule (a common feature on many Hostinger plans, but not independently verified here for this specific Node app), the command above is what to enter, once daily at 00:10 UTC. If Hostinger cannot do this cleanly for this hosting plan, an external scheduled-HTTP service (e.g. a free cron-ping service, or a scheduled GitHub Actions workflow in this repo) hitting the same URL with the same header is an equally valid trigger - the endpoint doesn't care who calls it, only that the secret is correct. **No workaround/inline scheduler was invented inside the Next.js app itself** (e.g. no `setInterval`-based scheduler) - Node.js hosting processes are not guaranteed to stay alive or be singular, so an in-process timer is not a safe substitute for a real external cron.
    - Tests: `src/lib/cron-auth.test.ts` (10 tests - constant-time accept/reject, length-mismatch safety, unconfigured-server-never-accepts, header extraction from both supported header names, non-Bearer scheme rejected), `src/app/api/internal/alpha-vantage/process/route.test.ts` (10 tests, `processAlphaVantageFundamentalsQueue` mocked - missing/wrong/unconfigured secret all return 401 with zero calls to the queue processor, correct secret via either header calls it exactly once with no arguments, empty/budget-exhausted/throttled outcomes all return 200 with the right `reason`, the configured secret never appears in any response, ticker-level outcome detail is never returned, an unexpected processor failure is sanitized to a generic 500).

- **BALANCE_SHEET support: Current Ratio shipped, Debt/Equity deliberately deferred (2026-09-04 slice, migration `20260904162540_alpha_vantage_balance_sheet` - implemented and tested against this session's LOCAL database only, NOT applied to production Supabase, NOT pushed - see Current State/Next Tasks).**
  - **Schema additions to `TickerFundamentals`** (8 new nullable columns, purely additive, mirroring OVERVIEW's existing freshness-field pattern under a `balanceSheet` prefix so the two endpoints are tracked independently): `balanceSheetCurrentRatio`, `balanceSheetDebtToEquity` (column exists, **never written to this slice** - see below), `balanceSheetFiscalDateEnding`, `balanceSheetFetchedAt`, `balanceSheetStaleAfter`, `balanceSheetLastAttemptAt`, `balanceSheetLastAttemptStatus`, `balanceSheetLastErrorMessage`, plus a `@@index([balanceSheetStaleAfter])`. **A ticker's OVERVIEW being fresh never implies its BALANCE_SHEET is, and vice versa** - proven by dedicated DB-integration tests (a fresh-OVERVIEW/never-fetched-BALANCE_SHEET row still queues for balance sheet; a stale-OVERVIEW/fresh-BALANCE_SHEET row does not re-queue for balance sheet).
  - **Current Ratio formula (verified, implemented): `totalCurrentAssets / totalCurrentLiabilities` from the latest quarterly report** (`quarterlyReports[0]` of Alpha Vantage's real, documented `BALANCE_SHEET` response shape - `src/providers/alpha-vantage/balance-sheet.ts`). Null whenever either input is missing/a null sentinel (`"None"`/`"-"`/`""`) or the denominator isn't a real positive number - never divides by zero, never substitutes an annual report for a missing quarterly one (prefers blank, per instruction). Fiscal date stored alongside the ratio. Wired into Research's `currentRatioCell` as the new middle auto-tier (Schwab -> Alpha Vantage -> manual) - see Research section.
  - **Debt/Equity: deliberately NOT implemented this slice - "still unverified."** Alpha Vantage's `BALANCE_SHEET` exposes several overlapping/redundant debt fields (`shortTermDebt`, `currentDebt`, `currentLongTermDebt`, `longTermDebt`, `longTermDebtNoncurrent`, `shortLongTermDebtTotal`) whose exact composition/overlap is not verifiable from documentation alone (Alpha Vantage aggregates from multiple filing sources and different tickers populate different subsets of these fields) - inventing a formula risks double-counting debt components, which the task explicitly forbade. Rather than guess, a **TEMPORARY, one-ticker (APLD-only) manual diagnostic** was built instead: `src/providers/alpha-vantage/balance-sheet-diagnostic.ts` + `runAlphaVantageBalanceSheetDiagnostic()` (`src/lib/alpha-vantage-diagnostic.ts`) + a new button on `/account/alpha-vantage-fundamentals` ("Verify Balance Sheet fields (APLD - 1 call)"). Costs at most 1 real Alpha Vantage call, **properly reserves the MANUAL budget slot and the global run lock** (unlike the older, pre-budget-system OVERVIEW diagnostic, which was never retrofitted to track budget), shows only the strict allowlisted Current-Ratio-input and debt-candidate fields with ABSENT/PRESENT_NULL/PRESENT_VALUE presence (never a raw payload dump), and **writes to no database table at all** - purely observational. **Exact next production step once deployed: click "Verify Balance Sheet fields (APLD - 1 call)" on `/account/alpha-vantage-fundamentals` and report back which of the debt candidate fields came back `PRESENT_VALUE` for a real ticker** - that result is what a future slice needs before choosing a verified Debt/Equity formula.
  - **Quota efficiency / unified cross-endpoint orchestration (fixed same-day, after initial review caught a starvation bug)**: the first version of this slice ran `processAlphaVantageFundamentalsQueue()` (OVERVIEW) to completion, then `processAlphaVantageBalanceSheetQueue()` (BALANCE_SHEET) - both drawing from the same 22-call AUTO budget - in the cron route. **That let a broad OVERVIEW pass consume the entire day's AUTO budget before BALANCE_SHEET ever got a single call, even for a Research ticker missing both** - directly conflicting with the "OSO Research should be a one-stop workspace" goal (maximize useful research completeness per call, not just refresh the broad OVERVIEW universe first). **Fixed** with a genuinely unified work-item queue, `getUnifiedAlphaVantageWorkQueue()`/`processAlphaVantageQueues()` (`src/lib/alpha-vantage-fundamentals.ts`), which is what the cron route (`/api/internal/alpha-vantage/process`) now calls instead of the two sequential functions:
    - **Priority order (one interleaved `{ticker, endpoint}` list, not two queues)**: (1) Research tickers missing/stale OVERVIEW, (2) Research tickers missing/stale BALANCE_SHEET, (3) Scanner PASS tickers missing/stale OVERVIEW, (4) Scanner PASS tickers missing/stale BALANCE_SHEET, (5) Scanner Near tickers missing/stale OVERVIEW, (6) the starter scanner universe missing/stale OVERVIEW. BALANCE_SHEET is never queued for the Near tier or the starter universe at all - only Research and Scanner PASS ever get a balance-sheet call, so it's never spent scanning the full Scanner universe. A ticker/endpoint pair is queued at most once, at its highest-priority tier, even if it qualifies for more than one.
    - **Why starvation can't happen anymore**: all Research-tier work (both endpoints) is ordered strictly ahead of all Scanner-tier work in the SAME queue drained under one shared budget loop - a lower-priority Scanner OVERVIEW item can never "already be gone" ahead of a higher-priority Research BALANCE_SHEET item, because they now compete for budget slots in one correctly-ordered list instead of two independently-exhausting ones.
    - **Call economics preserved exactly as specified**: a Research ticker missing both endpoints legitimately costs 2 AUTO reservations (its OVERVIEW item, then later its BALANCE_SHEET item in tier 2) - this is intentional, not a bug, since the goal is complete useful research, not minimum calls. Matt and Eric researching the same ticker still dedupes to one shared `{ticker, endpoint}` item per endpoint (proven by test). Endpoint freshness stays fully independent (a fresh OVERVIEW never suppresses a needed BALANCE_SHEET item and vice versa).
    - **Daily limits, pacing, and lock are completely unchanged** - `getUnifiedAlphaVantageWorkQueue`/`processAlphaVantageQueues` reuse the exact same `reserveAlphaVantageCall("AUTO", ...)`, `tryAcquireAlphaVantageRunLock`/`releaseAlphaVantageRunLock`, and `ALPHA_VANTAGE_REQUEST_DELAY_MS` primitives as before - still 25 total/day, 22 AUTO, 3 MANUAL reserve, 1300ms pacing, one global single-flight lock with 5-minute stale recovery. No schema change was needed for this fix.
    - **Additive, not a rewrite**: `processAlphaVantageFundamentalsQueue()`/`getFundamentalsQueueCandidates()` and `processAlphaVantageBalanceSheetQueue()`/`getBalanceSheetQueueCandidates()` are all still present, unchanged, and still pass their own existing tests - `processAlphaVantageFundamentalsQueue()` is still what the Account "Process fundamentals queue" manual button calls (OVERVIEW only, untouched by this fix, since the starvation problem was specific to the cron path running both queues back-to-back). Only the cron route was rewired to the new unified function; a small shared internal helper, `getTickerTiers()`, was factored out of the duplicated Research/Scanner-PASS/Scanner-Near/starter-universe gathering logic so the unified queue and the two original functions never drift into two different definitions of the same tiers. `refreshSingleTickerBalanceSheet()` (unchanged) still mirrors `refreshSingleTickerFundamentals` for a named ticker, used for deterministic single-ticker test assertions.
    - Tests (12 new DB-integration tests in `alpha-vantage-fundamentals.integration.test.ts`, all comparing the *relative* order of the test's own disposable fixture tickers rather than absolute queue length/position - this local dev database has real Matt/Eric seeded Research/Scanner data that legitimately appears in the same tiers, so an absolute-position assertion would be flaky): Research BALANCE_SHEET cannot be starved by Scanner-only OVERVIEW work; Research endpoint work outranks the starter universe; fresh-OVERVIEW+stale-BALANCE_SHEET queues only BALANCE_SHEET; stale-OVERVIEW+fresh-BALANCE_SHEET queues only OVERVIEW; a ticker missing both queues both; Matt+Eric dedupe to one item per endpoint; no private data leaves the queue; AUTO budget exhaustion stops the unified processor at 0 calls regardless of queue size; the 3-call manual reserve is never touched by the automatic queue; the shared run lock blocks the unified processor exactly like the single-endpoint queues; a real throttle response stops it immediately; reading cached fundamentals for Research display never touches the daily budget. `route.test.ts` updated to mock the single unified function instead of two.
  - **7-day TTL** (`ALPHA_VANTAGE_FUNDAMENTALS_TTL_MS`, same constant as OVERVIEW - balance sheets are quarterly/slow-moving) applied independently to `balanceSheetStaleAfter`. No Alpha Vantage call is ever triggered by rendering Research - `getTickerFundamentalsMap` stays a pure read.
  - **Shared cache stays public-data-only** - no new field added anywhere carries `userId`, notes, would-own, research status, or campaign information; `getBalanceSheetQueueCandidates` selects `ticker` only from Research/Scanner, same privacy-safe-by-construction pattern as the OVERVIEW queue.
  - Tests (all passing): `src/providers/alpha-vantage/balance-sheet.test.ts` (7 unit tests - formula, zero-denominator, None-sentinel, EMPTY, RATE_LIMITED, HTTP_ERROR, key redaction), `balance-sheet-diagnostic.test.ts` (7 unit tests - APLD default, ABSENT/PRESENT_NULL/PRESENT_VALUE preservation, zero-denominator, RATE_LIMITED, EMPTY, key redaction, no-raw-payload-leak), `alpha-vantage-fundamentals.integration.test.ts` (+14 DB tests - exact Current Ratio computation, zero-denominator safety, None-sentinel safety, EMPTY safety, endpoint-independent freshness both directions, Matt+Eric dedup, Research-before-Scanner-PASS priority, starter-universe/Near-tier exclusion, no private data in queue output, shared-budget exhaustion blocks BALANCE_SHEET too, shared-lock contention, throttle-stops-queue-safely), `route.test.ts` extended (+3 - both queues called once each per cron invocation, calls summed correctly, either queue's failure is sanitized to 500).
  - **A real test-determinism bug was found and fixed while writing these tests**: value-correctness assertions run through `processAlphaVantageBalanceSheetQueue` (the priority queue) rather than a direct single-ticker call were flaky against this real local dev database, which has genuine Matt/Eric seeded Scanner-PASS history - the unscoped-by-design candidate queries could occasionally place a real ticker ahead of the test's own fixture ticker even with `maxTickers: 1`, silently writing the test's fake payload to the wrong row. Fixed the same way the pre-existing OVERVIEW tests already do it: added `refreshSingleTickerBalanceSheet()` and pointed value-correctness tests at a named ticker directly, reserving the queue-level tests for queue mechanics (ordering/dedup/budget/lock) which don't care which ticker is processed.

---

## Hosting / Domain

- **Production domain:** https://offshiftoptions.com — connected to the Hostinger Node application and live (externally confirmed: purchased, connected, `NEXT_PUBLIC_APP_URL` updated in Hostinger to this URL, and Hostinger redeployed afterward). Not independently verifiable from this repository.
- **Hosting:** Hostinger managed Node.js hosting, deployed via GitHub auto-deploy from `main`.
- **Database:** Supabase PostgreSQL (production).
- Cookies, the service worker scope, and the PWA manifest's `start_url`/`scope` are all relative (`/`) — no hardcoded domain anywhere in the app, so no further code change was needed for the domain cutover.
- The old temporary Hostinger-provided URL is no longer the primary production URL now that `offshiftoptions.com` is connected and confirmed live.

---

## PWA / Mobile

- Manifest (`public/manifest.webmanifest`): name "Off Shift Options", short name "OSO", standalone display, theme/background colors, icons for both `any` and `maskable` purposes.
- Icons: `public/icon.svg` (scalable), `public/icon-192.png`, `public/icon-512.png`, `public/icon-maskable-192.png`, `public/icon-maskable-512.png`, `public/apple-touch-icon.png` (180x180), rebranded `src/app/favicon.ico`. No remaining placeholder icons.
- Service worker (`public/sw.js`): network-first for page navigations with a cached `/login` fallback (never serves a stale authenticated/financial page offline); cache-first only for a small fixed shell asset list. Cache name is versioned (`oso-shell-v2`) so it invalidates cleanly on updates.
- Mobile navigation, touch targets, and layout verified overflow-free at 390px width across all main authenticated routes via Playwright.
- **Unfinished:** real Web Push delivery. `WebPushNotificationProvider` (`src/lib/notifications.ts`) is a documented no-op. The storage endpoint (`POST /api/push-subscriptions`) exists but nothing on the client calls it yet. HTTPS (the old blocker) is no longer the issue — completing this needs VAPID key generation/config, a `web-push` server dependency, a client subscribe flow, permission-decline handling, and a service worker `push` handler. Tracked as a Phase 2/3 task in `docs/PWA_AND_NOTIFICATIONS.md`, not started.

---

## Chat / Social

- One private conversation per Matt/Eric pair, created by the production bootstrap (or dev seed locally). No self-service "start a conversation" UI exists (not needed — there are only two users).
- Sending/reading requires active `ConversationMember` membership, enforced server-side (`src/lib/workflows.ts`); proven by a DB-integration test that a non-member's send attempt is rejected.
- Read receipts (`ChatMessageRead`) track per-user read state; unread count and "Unread"/"Read by ..." UI reflect it.
- **Live updates:** a client component polls `router.refresh()` every 4 seconds while the chat tab is visible (paused when hidden) — the simplest mechanism compatible with the existing Server Component/Server Action architecture, no WebSockets or Supabase Realtime.
- Recommendations carry their own participant-gated comments and reactions; watchlist items carry owner-only notes plus shared, read-access-gated comments.
- Notification delivery on new messages/recommendations/comments/reactions is server-side (`notifyInApp`), scoped to the correct recipient only.

---

## Security

- Server-side mutation authorization for every action, centralized in `src/lib/workflows.ts` (never trust client state or hidden UI).
- `PRIVATE`/`SHARED` visibility enforced in `src/lib/privacy.ts`; buddies can read shared records but never private ones; mutation always requires ownership except for intentionally social actions (comments/reactions/chat) which use read-access or explicit participant checks instead.
- Sessions: HTTP-only, `Secure` in production, `SameSite=Lax`, HMAC-hashed tokens, new token minted per login (no fixation).
- Password hashing: `bcryptjs`, consistent across all creation/change paths.
- Ownership enforcement covers watchlist items, notes, recommendations, chat membership, notifications, and scanner settings — each scoped to the correct owner/participant, proven by unit + DB-integration tests.
- Schwab market data is resolved user-first: use the signed-in user's own Schwab connection when available. Shared OSO market-data fallback is disabled until official Schwab policy explicitly permits it. Schwab brokerage account data and tokens are strictly user-scoped. Never reuse one user's OAuth connection for another user's account sync, imports, balances, positions, transactions, performance, projections, settings, or trading-result achievements.
- Schwab OAuth state is signed with an HMAC, expires after 10 minutes, is tied to the current OSO user, and is stored in an HttpOnly/SameSite=Lax cookie (`Secure` in production). The callback requires an active OSO session and rejects cross-user, tampered, mismatched, or expired state.
- Schwab access/refresh tokens and per-user Schwab developer app credentials are encrypted at rest with a dedicated server-side AES-256-GCM key. Client-side UI shows only safe connection metadata such as status, linked account count, account-number last4s, developer app configured state, and last validation time; account hashes and stored secrets stay server-side.
- Schwab token access has an explicit `expectedUserId` guard and a DB integration regression test proving Eric cannot read Matt's Schwab broker token.
- No secrets in the client bundle — only `NEXT_PUBLIC_APP_URL` is public, and it contains no sensitive data.
- No trading/order-submission endpoints exist anywhere in the codebase.
- Input validation: server-side ticker format validation (`src/lib/tickers.ts`) backed by database `CHECK` constraints; password strength validation (`src/lib/account.ts`).
- `/api/health` never leaks database error details (see Deployment section).
- Production HTTP security headers (`next.config.ts`, applied to every route): `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, a restrictive `Permissions-Policy`, `Strict-Transport-Security`, and a baseline `Content-Security-Policy` (`default-src 'self'`, `frame-ancestors 'none'`; `script-src`/`style-src` still allow `'unsafe-inline'` because Next's App Router needs it without a nonce setup — a known, accepted tradeoff, not a gap to silently "fix" by weakening the CSP further).
- **No currently known open security issues.** A full audit pass (auth, IDOR, session handling, secrets, headers, health endpoint) was completed and fixes applied — see `docs/HANDOFF.md`'s "Pre-Schwab Production Hardening" entry for the detailed findings/fixes list.

---

## Testing

Last verified (September 3, 2026 for the local Performance + Campaign History redesign slice, against local Docker PostgreSQL and a local `next start` production-mode server on port 3001 - nothing run against Supabase):

| Check | Command | Result |
|---|---|---|
| Prisma validate | `corepack pnpm prisma validate` | passed |
| Prisma generate | via `corepack pnpm typecheck` / `corepack pnpm build` | passed |
| TypeScript | `corepack pnpm typecheck` | passed |
| ESLint | `corepack pnpm lint` | passed, no output |
| Vitest unit tests | `corepack pnpm test` | 127 passed / 57 skipped (DB tests skip without `RUN_DB_TESTS`) |
| Vitest incl. DB integration | `RUN_DB_TESTS=1 DATABASE_URL=<local Docker Postgres URL> corepack pnpm test` | 184/184 passed across 33 files against local Docker Postgres; existing `pg` deprecation warning only |
| Playwright e2e, **production mode** (`next start`, local Docker Postgres, not Supabase) | `corepack pnpm build && corepack pnpm exec next start --hostname 0.0.0.0 --port 3001`, then each affected spec file with `PLAYWRIGHT_BASE_URL=http://localhost:3001` | **performance-history.spec.ts 8/8, performance-phase-a.spec.ts 5/5, performance-phase-b.spec.ts 4/4, phase1b.spec.ts 6/6 - 23/23 passed.** This is the authoritative result; see the dev-mode note below for why dev-mode Playwright runs are not used as the pass/fail signal for this project. |
| Playwright e2e, dev mode (`next dev`) | not rerun for this slice | scanner.spec.ts and research.spec.ts previously pass fully; phase1b.spec.ts intermittently fails 1-3 Sign-Out-dependent tests on the dev-only overlay artifact below — not a real regression, confirmed by the production-mode row above |
| Production build | `corepack pnpm build` | passed |

**Local Docker `app` service has NO bind mount - it runs a baked image, not live source (discovered 2026-09-04).** `compose.yaml`'s `app` service has no `volumes:` entry; `db` is the only service with one (the Postgres data volume). `docker compose start`/`stop`/`restart` on the `app` container reuses whatever image was last built - **source file edits are invisible to it until an explicit rebuild.** This caused real, extended, confusing diagnostic time this slice (a running container served page content and a migrations-folder listing from days earlier - stale nav labels, `4 migrations found` instead of 10 - purely because the image predated all of it) before being root-caused. **To actually exercise current source in the Docker dev container, use `docker compose up -d --build app`** (plain `start`/`restart` is not enough). This is a pre-existing setup gap, not something this slice changed - noted here so a future session doesn't re-spend the time diagnosing it.
- **A second, separate, already-documented gotcha re-encountered while investigating the above**: Next dev's cross-origin dev-asset guard 403s/blocks client hydration when accessed via `127.0.0.1` instead of `localhost` (see the Appearance System section's "Testing gotcha" bullet, 2026-09 - this is not new, just re-triggered by manual `127.0.0.1`-based scripts during this slice's diagnosis). `playwright.config.ts`'s default `baseURL` already correctly uses `localhost`; only ad hoc manual scripts using `127.0.0.1` were affected.
- **Two pre-existing issues, unrelated to this slice's diff (confirmed via `git diff` showing zero changes to the affected files), were discovered incidentally while diagnosing the above and are NOT fixed here** (out of scope - this slice is cron + sidebar only): (1) the Research ticker input's HTML `pattern` attribute (`research-workspace.tsx`) throws a browser console `SyntaxError` under Chrome's newer regex-`v`-flag pattern compilation (`[A-Za-z][A-Za-z0-9.-]{0,9}` needs re-escaping for that mode) - cosmetic in the console, but worth a dedicated fix; (2) `research.spec.ts`'s "adding a ticker" flow (and several `scanner.spec.ts` interactions) are timing-sensitive against a freshly-navigated page in dev mode - reproducible by removing a settle delay after `page.goto`, and disappears with one - this may be specific to Turbopack dev-mode hydration latency rather than a real app bug (unconfirmed against production-mode `next start`, which has its own unrelated known local-sandbox limitation - see below). Both should get their own dedicated look in a future slice.
- **This sandbox's local `next start` (host, not Docker) intermittently fails to serve static JS chunks correctly (separately documented in earlier slices this engagement) - still present, still unrelated to application code**, reproduced again this slice on unmodified `HEAD` before any of today's changes. Not chased further, per established precedent.

**Performance Phase A measured local deltas (production mode, local Docker DB, 2026-09-02):**
- Post-login duplicate private-route RSC prefetch burst: **18 -> 0** (`/positions`, `/scanner`, `/scanner/settings`, `/research`, `/recommendations`, `/chat`, `/notifications`, `/account`, `/install` no longer prefetch in duplicate from the desktop/mobile nav).
- Immediate Dashboard -> Scanner: RSC requests **17 -> 1**, 5-sample median **397ms**.
- Immediate Dashboard -> Research: RSC requests **17 -> 1**, 5-sample median **371ms**.
- Settled Scanner -> Research: RSC requests **1 -> 1**, 5-sample median **133ms**.
- Tracker tabs: still one `/positions` RSC per URL tab switch, but tab selection is immediate and server work is tab-specific. Measured Open -> History **71ms -> 79ms**, History -> Performance **52ms -> 76ms**, Performance -> Accounts **55ms -> 62ms**.
- Research optimistic status click: badge visible in **40ms**, action complete in **79ms**. Baseline action complete was 63ms but had no client-side optimistic badge measurement.
- Local interaction regression probes: Scanner Pass filter **49ms**, Research Watch filter **20ms**, both with **0 network / 0 RSC** requests.
- Scanner page data read-count probe for existing seeded profile: `scannerProfile.findFirst` **2 -> 1** while `user.findMany` and `watchlistItem.findMany` remain one each.

**Playwright login helper note**: `phase1b.spec.ts`, `scanner.spec.ts`, and `research.spec.ts` all log in by filling the real email/password sign-in form (`matt@lst.local`/`eric@lst.local`, `DEV_SEED_PASSWORD` or its `lstbuddy-dev-only` default), not the dev-only "Login as Matt/Eric" shortcut buttons (`login/page.tsx` renders those only when `NODE_ENV !== "production"`). This is what makes the production-mode run above possible at all - the shortcut buttons simply don't exist under `next start`, so a test built around them would hang forever waiting for a button that will never render.

**Note on `PLAYWRIGHT_BASE_URL`**: use `http://localhost:<port>`, not `http://127.0.0.1:<port>`, against `next dev` — see Appearance System section's testing gotcha. `playwright.config.ts`'s default was changed accordingly.

**Note on running `scanner.spec.ts` and `research.spec.ts` together**: both suites mutate the same seeded demo tickers (`research.spec.ts` sets RIVN to LIKE and WBD to NEVER_TRADE while proving Scanner<->Research integration), and Playwright does not necessarily execute multiple spec-file arguments in the order given on the command line. Passing both files to one `playwright test` invocation without a reseed in between can make one suite's mutations leak into the other's "pristine demo data" assumptions (observed: `scanner.spec.ts`'s Watchlist-filter and gating-FAIL tests failing only when combined with `research.spec.ts`, passing 12/12 every time when run alone). **Always reseed (`corepack pnpm exec tsx prisma/seed.ts`) between spec files**, or run one file per `playwright test` invocation — this is what the 31/31 production-mode result above used, and it's fully reproducible.

**Temporary visual QA (not part of the permanent suite, deleted after use, never committed)**: `zz-theme.spec.ts` (Appearance slice, 6/6) and a one-off Research visual-QA run seeding 57 synthetic tickers for Matt via direct Prisma calls (bypassing `server-only` guards that block plain `tsx` scripts from importing `src/lib/workflows.ts`), screenshotted across Dark/Light/mobile/expanded-row states, then the DB was reseeded back to the clean baseline.

**Temporary visual QA for the Performance + Campaign History redesign (not part of the permanent suite):** one-off screenshots of Tracker Performance help in Dark/Light/System plus History help on a 390px mobile viewport were captured under ignored `test-results/visual-qa/` and visually inspected. The pages were readable, aesthetic, tooltip copy was sentence-case and legible, and there was no page-level horizontal overflow; the screenshots are not tracked.

Previous Core Trading Cockpit tests: `src/domain/finance/accountLedger.test.ts` and `accountLedger.integration.test.ts` (starting/deposit/withdrawal/adjustment math, cross-user ledger-entry rejection, the $10,000+$2,000+$300=$12,300 worked example, the +28/−53/+82=+57-OPEN-then−11=+46-WIN worked example against a real database, and proof that a buddy's completed campaign never appears in another user's `ownCompletedCampaigns` even under `scope=both`), `src/domain/finance/performance.test.ts` (win/loss and weekly-return math), `src/domain/scanner/profile.test.ts` (LST Core diffing), gating/`UNKNOWN`-semantics tests in `scanner.test.ts`, and the fixed DB-integration assertion for LST Core price defaults.

**2026-08-31 follow-up fix (Dashboard broker positions)**: `src/domain/finance/occOption.test.ts`, `src/domain/finance/brokerPositions.test.ts`, `src/providers/schwab/broker-read.test.ts`, and a `src/lib/broker-connections.integration.test.ts` case proving Eric gets `null` from Matt's Schwab lookups. No schema change.

**Schwab credential-routing/market-data-caching tests:** `src/providers/market-data/cache.test.ts` (repeated quote reuse, provider-key isolation, simultaneous request coalescing, sanitized provider errors); `src/lib/broker-connections.integration.test.ts` (disposable users, user-owned market-data routing, no cross-user personal broker fallback, no cross-user token read, User B surviving User A connection deletion).

**Schwab CSV import/reconciliation tests (this slice):**
- `src/domain/finance/brokerTransactionActions.test.ts` — real observed Schwab actions classify correctly; unrecognized/missing actions return `UNKNOWN` rather than a guess.
- `src/providers/schwab/csv.test.ts` — parsing against the real-verified fixture shapes, repeated-export dedupe, API+CSV position/transaction reconciliation without doubling quantity, Gain/Loss validation without double-adding P/L.
- `src/lib/broker-import.integration.test.ts` (9 tests, real Postgres) — preview never persists a `BrokerRecord`, confirm does; the exact same file imported twice is idempotent (0 new the second time); the same content under a different filename is still idempotent; discarding a preview never creates records; **a same-identity transaction with a changed amount is flagged `CONFLICT` and the original row is never overwritten**; a newer Positions snapshot supersedes an older one in place (quantities are never summed, exactly one row remains); a malformed row is reported as `INVALID` rather than crashing the batch or being silently dropped; User B cannot view/confirm/discard User A's import batch; User B cannot see User A's persisted broker records.
- `src/lib/broker-reconciliation.integration.test.ts` (5 tests, real Postgres) — a position stays unlinked and shows up as awaiting review until confirmed; confirming creates/links a real Campaign via the existing `createCampaignForUser` workflow and the position then correctly classifies as "linked" for Dashboard dedupe purposes; a position cannot be confirmed twice; skipping never creates a Campaign and stops the item from reappearing; User B cannot see, confirm, or skip User A's broker activity awaiting review.

DB-integration tests always use disposable test users with cleanup in `afterAll` — they never touch Matt/Eric's real seeded credentials. Nothing in this repository's test suite is designed to run against Supabase; do not point it there.

**Roll Buffer % / Roll Status slice (2026-09-04):** `tsc --noEmit`, `eslint src`, `prisma validate`, and `next build` all passed clean. `RUN_DB_TESTS=1 npx vitest run` — 365/365 passed (0 skipped) against this environment's configured database (a local Postgres named `lst_buddy` at `localhost:5432`; same disposable-test-user convention as the rest of the suite). `prisma migrate status` confirmed the new migration applied both to that local database and, separately, to **production Supabase** (Matt ran `prisma migrate deploy` against production and confirmed "Database schema is up to date!"). New/changed: `rollStatus.test.ts` (15), `campaigns.test.ts`'s `getCurrentOpenPut` block (6), `rollBuffer.integration.test.ts` (5), `brokerPositions.test.ts` (+2 for the short-position display cleanup). **Live Playwright smoke pass completed** against a production-mode (`next start`) local server on port 3001, logged in as `matt@lst.local`: confirmed the Roll Buffer input defaults to 3.0%, saves to 5.0%, and survives a page reload before being restored to 3.0%; confirmed Dashboard and Tracker Open render honest "Roll status unavailable" badges (this local session has no live Schwab OAuth connection, so colored GREEN/AMBER/RED and the short-position wording fix could not be exercised end-to-end - both are proven at the unit-test level instead); confirmed Tracker History shows no Roll Status badge; screenshots reviewed manually, layout is clean with no overlap. Temporary spec file used for this and deleted afterward, per the project's established temporary-visual-QA convention - never committed.

**Wide layout / theme overflow / Columns dropdown / LST checkpoint / Alpha Vantage BALANCE_SHEET slice (2026-09-04, NOT pushed - see Current State), plus the same-day unified-queue orchestration fix (see Alpha Vantage API section) that replaced the sequential OVERVIEW-then-BALANCE_SHEET cron wiring with one priority-ordered work queue:** `prisma validate`, `tsc --noEmit`, `eslint src`, and `next build` all passed clean. `RUN_DB_TESTS=1 npx vitest run` — 412/412 passed against this environment's local database (401 from the initial slice + 12 new unified-queue tests, net of a route-test consolidation); migration `20260904162540_alpha_vantage_balance_sheet` applied and verified **locally only, deliberately not run against production Supabase** per this slice's explicit instruction. New/changed test files: `lstCheckpoint.test.ts` (7), `balance-sheet.test.ts` (7), `balance-sheet-diagnostic.test.ts` (7), `alpha-vantage-fundamentals.integration.test.ts` (+14), `process/route.test.ts` (+3), `research-customization.spec.ts` (+1 Playwright test for Columns outside-click/Escape). **Live Playwright smoke pass** against a production-mode local server confirmed: no page-level horizontal overflow at 1366/1440/1920/2560/390px; shell width 1280px unchanged at 1366/1440, 1800px at 1920/2560 (screenshots reviewed); collapsed sidebar genuinely widens `<main>` (1544px → 1728px at 1920px); the appearance control's right edge stays inside the account card's right edge at 1024/1366/1920px (bounding-box check, not just visual); Columns menu closes on outside click and Escape, stays open for inside interaction, stable across repeated open/close; Dashboard shows a compact checkpoint label; the BALANCE_SHEET diagnostic button correctly gates behind the existing "not configured" state (no Alpha Vantage key in this sandbox); Research's Current Ratio column and the new expanded-detail Auto Data section render without crashing (no live Alpha Vantage data available locally, so real GREEN/AMBER-style auto-population itself could not be seen end-to-end - proven at the unit/DB-integration level instead). Also ran the full pre-existing `desktop-sidebar.spec.ts` (12/12) and `research-customization.spec.ts` (16/16, including 2 tests that initially failed for an unrelated reason - see below) as regression checks. **A local-dev-only side effect was found and fixed**: running `prisma migrate dev` to create the new migration reseeds this database, which wiped Eric's one-off research-heavy column preset (applied via `scripts/init-research-columns.ts`, not part of the regular seed) back to empty - re-ran that script for `eric@lst.local` to restore it, and separately reverted a researchColumns/TickerFundamentals side effect my own Playwright test runs made to Matt's account and a handful of real seeded tickers. No production data was ever touched.

---

## Important File Map

- `PROJECT_HANDOFF.md` — this file; canonical current-state document.
- `PRODUCT_VISION.md` — long-term product north-star (areas, principles, non-goals); not a changelog.
- `docs/HANDOFF.md` — older chronological session-by-session log (kept for detailed history); this file is now canonical for *current* state.
- `AGENTS.md` — general agent operating instructions.
- `CLAUDE.md` — Claude-specific entry pointer to this file.
- `README.md` — human/developer-facing docs, local setup, scripts.
- `package.json` — scripts and dependencies.
- `prisma/schema.prisma` — database model.
- `prisma/migrations/` — all migrations through `20260901200601_research_model` are applied to production Supabase: `20260828114500_init`, `20260828132500_phase_1b_hardening`, `20260828215700_campaign_tracker_foundation`, `20260831211006_account_ledger_and_broker_link`, `20260831235050_schwab_credential_routing_broker_records` (`SchwabDeveloperCredential`, `BrokerConnection.developerCredentialId`, `BrokerRecord`, `BrokerImportBatch`), `20260901031101_user_appearance_preference` (`UserSettings.appearance`), and `20260901200601_research_model` (`ResearchStatus`/`WouldOwnStatus`/`RollFriendliness` enums, personal-rule/fundamentals fields on `WatchlistItem`).
- `src/lib/appearance.ts` — appearance resolution/persistence (`resolveAppearanceForRequest`, `dataThemeAttributeFor`, `updateAppearanceForUser`) — see Appearance System section.
- `src/components/appearance-control.tsx` — the Dark/Light/System toggle (compact sidebar + full Account Preferences variants).
- `src/components/info-tip.tsx` — reusable accessible contextual help button/tooltip for dense financial labels; supports hover, focus, tap/click, outside click, and Escape.
- `src/app/(app)/research/page.tsx` + `research-workspace.tsx` — the Research page (server data fetch + client dense-table workspace) — see Research section.
- `src/app/(app)/scanner/scanner-workspace.tsx` — also owns the Scanner's Research quick-actions, badges, and `Show Excluded` toggle (added 2026-09, alongside the original scanner redesign) — see Scanner and Research sections.
- `getResearchUniverseTickersForUser`/`setResearchStatusForUser`/`updateResearchDetailsForUser` in `src/lib/workflows.ts` — the Research workflow layer — see Research section.
- `src/domain/finance/accountLedger.ts` — account ledger summarization (`summarizeAccountLedger`, `currentAccountValue`) — see Account Ledger section.
- `src/domain/finance/performance.ts` — win/loss, weekly-return-vs-target, campaign current/projection, contribution-adjusted target, and account trading P/L math (`summarizeWinLoss`, `summarizeWeeklyReturns`, `summarizeCampaignProgress`, `summarizeContributionAdjustedGoal`, `tradingProfitFromAccountValue`) — see Account Ledger section.
- `src/domain/finance/occOption.ts` — OCC option symbol parsing/formatting for display (`parseOccOptionSymbol`) — see Schwab API section.
- `src/domain/finance/brokerPositions.ts` — broker-position classification, CSP secured-capital math, and open-positions counting (`classifyBrokerPosition`, `summarizeCspSecuredCapital`, `computeOpenPositionsCount`, `describeBrokerPositionForDisplay`) — see Schwab API section.
- `src/domain/finance/brokerTransactionActions.ts` — classifies a Schwab transaction's Action text into a fixed vocabulary; `UNKNOWN` for anything unrecognized.
- `src/lib/broker-import.ts` — the CSV import pipeline: file validation, `classifyCandidates` (NEW/DUPLICATE/CONFLICT/NEEDS_REVIEW/INVALID), preview/confirm/discard against `BrokerImportBatch`/`BrokerRecord` — see Schwab API section.
- `src/lib/broker-reconciliation.ts` — "Broker Activity Awaiting Review," confirm-as-Campaign / skip, and the Dashboard double-count fix (`splitBrokerPositionsByCampaignLink`) — see Schwab API section.
- `src/providers/schwab/csv.ts` — Schwab CSV parsing/normalization/fingerprinting/`identityKey` (conflict detection)/reconciliation for the three real export types; `src/providers/schwab/__fixtures__/*.csv` are the sanitized, real-structure-verified fixtures.
- `prisma/seed.ts` — **destructive** development/demo seed. Never production.
- `prisma/bootstrap-production.ts` — safe production bootstrap CLI.
- `src/lib/bootstrap.ts` — safe bootstrap logic (idempotent, non-destructive).
- `src/lib/auth.ts` — authentication/session logic.
- `src/lib/account.ts` — change-password logic.
- `src/lib/broker-connections.ts` — safe Schwab connection summaries, disconnect, and user-scoped market-data/personal-broker provider resolvers.
- `src/lib/privacy.ts` — `PRIVATE`/`SHARED` authorization helpers.
- `src/lib/workflows.ts` — server-side mutation/authorization logic for every server action.
- `src/domain/scanner/` — scanner engine (`scanner.ts`), rule catalog/demo data (`profile.ts`), and staged live Schwab scanner (`live-scan.ts`).
- `src/domain/finance/calculations.ts` — legacy per-trade financial calculations.
- `src/domain/finance/campaigns.ts` — campaign lifecycle financial summaries (`summarizeCampaign`) and the shared `getCurrentOpenPut` helper — see Roll Status section.
- `src/domain/finance/rollStatus.ts` — GREEN/AMBER/RED Roll Status color model and the Friday ~3pm ET management checkpoint (`computeRollStatus`, `isPastFridayManagementCheckpoint`) — see Roll Status section.
- `src/lib/live-quotes.ts` — batched, never-fabricating live quote lookups for the Roll Status badge (`getLiveQuotePricesForUser`) — see Roll Status section.
- `src/components/roll-status-badge.tsx` — the compact Roll Status display component (`RollStatusBadge`, `RollStatusUnavailableBadge`) — see Roll Status section.
- `src/domain/finance/lstCheckpoint.ts` — the compact "Next LST Checkpoint" Dashboard reminder (`getNextLstCheckpointLabel`) — see Roll Status section.
- `src/providers/alpha-vantage/balance-sheet.ts` — production BALANCE_SHEET fetch/normalize (Current Ratio only; Debt/Equity deliberately unimplemented) — see Alpha Vantage API section.
- `src/providers/alpha-vantage/balance-sheet-diagnostic.ts` — TEMPORARY one-ticker (APLD) manual verification tool for the Debt/Equity fields — see Alpha Vantage API section; delete once Debt/Equity is verified and implemented for real.
- `src/domain/social/` — recommendation reason tags/statuses.
- `src/providers/market-data/` — market-data provider contracts plus demo provider and provider-level cache/in-flight coalescing.
- `src/providers/broker-read/` — broker-read provider contracts plus demo provider.
- `src/providers/schwab/` — read-only Schwab OAuth/token/client/normalizer/market-data/broker-read implementation, encrypted per-user developer credentials, and Schwab CSV normalization/reconciliation helpers.
- `src/components/live-refresh.tsx` — chat's polling live-update mechanism.
- `src/app/(app)/` — authenticated Next.js routes (dashboard, positions, scanner, scanner/settings, watchlist, recommendations, chat, notifications, account, install).
- `src/app/api/health/`, `src/app/api/push-subscriptions/`, `src/app/api/schwab/` — API routes.
- `next.config.ts` — security headers.
- `docs/` — supporting architecture/security/decision documentation (`ARCHITECTURE.md`, `SECURITY.md`, `DECISIONS.md`, `DATA_MODEL.md`, `SCANNER_RULES.md`, `PWA_AND_NOTIFICATIONS.md`, `SCHWAB_INTEGRATION.md`, `ROADMAP.md`, `LST_DOMAIN.md`).

---

## Architectural Decisions

Future AI agents should not casually reverse these:

- Hostinger is production application hosting; Supabase is production PostgreSQL.
- Supabase's Data API is not enabled merely because Supabase offers it — Prisma over a direct Postgres connection is the only production data-access path.
- Prisma is the ORM.
- GitHub `main` drives deployment (every push auto-deploys).
- The destructive development seed (`prisma/seed.ts`) and the safe production bootstrap (`prisma/bootstrap-production.ts`) are deliberately separate and must stay that way.
- The production build never runs the bootstrap or migrations automatically (see Deployment section for why and the required manual migration workflow).
- `PRIVATE`/`SHARED` visibility is intentional, load-bearing architecture — not a cosmetic UI toggle.
- Future Schwab integration begins read-only; no automatic trading/order submission.
- The scanner must never represent mock/demo data as live.
- Mobile-first PWA remains a core requirement.
- Chat updates via simple client-side polling (`router.refresh()`), not WebSockets/Realtime — deliberately the simplest option for a two-user app.
- Watchlist Pro/Con/General notes are owner-only; buddy interaction on shared items happens through comments instead.
- Campaigns model a whole trading idea as an append-only event history (`CampaignEvent`), never overwriting or deleting a prior event — a roll always writes new `ROLL_PUT_CLOSE`/`ROLL_PUT_OPEN` rows, it never edits the original `SELL_PUT`. Do not "simplify" this into a mutable current-state-only model.
- Campaign visibility defaults to `INHERIT` from its account, but an explicit `SHARED`/`PRIVATE` on the campaign itself always overrides the account — this lets one campaign be shared out of an otherwise-private account (or vice versa) without changing the account's own visibility.
- Prefer secure, conventional, maintainable implementation choices; do not blindly implement a technically weaker approach when a clearly better one exists (see AI Continuity Rules above).
- Account performance is always derived from the ledger (`AccountLedgerEntry`), never from `currentBalance − startingBalance` — do not reintroduce a derived-difference shortcut (see Account Ledger section).
- A scanner setup that fails a gating rule (`GATING_RULE_KEYS`) must never receive a positive score label, and an `UNKNOWN` overall result must never be labeled as if it passed — do not relax `honestSetupScore`/`honestSetupLabel` back to the raw score/label.
- Performance/win-rate figures are computed from an always-"mine"-scoped query, never the Mine/Buddy/Both scope selector — do not let a shared/"Both" view accidentally aggregate Matt's and Eric's P/L into one number.
- New `TradingAccount` rows default to `PRIVATE` visibility, not `SHARED` — sharing remains explicit and opt-in.
- CSP secured capital from a Schwab position only ever counts a position confidently identified as a short put (`classifyBrokerPosition`); never infer PUT/CALL or short/long when the data is ambiguous — show the total as a floor (`hasUnknown`) instead of guessing.
- Raw OCC option symbols are never shown to the user as-is; always run them through `src/domain/finance/occOption.ts` for display while preserving the original symbol internally.
- Light/Dark/System theming re-themes the existing app by overriding the Tailwind color-scale CSS variables (`--color-zinc-*`, etc.) under `[data-theme]`/`prefers-color-scheme` selectors in `globals.css` — it does not rewrite component class names or introduce a theming library (e.g. `next-themes`). Do not "simplify" this into a component-by-component rewrite; new/shared UI should prefer the semantic token aliases (`--background`, `--surface`, `--positive`, etc.) instead of raw scale classes going forward, but the raw-scale override remains the mechanism that makes the rest of the app theme-aware for free.
- A user's `UserSettings.appearance` (DB) is always authoritative once logged in; the `oso-appearance` cookie only serves logged-out/pre-auth rendering and is deliberately kept separate from session/credential cookies (not httpOnly, never sensitive).
- **A personal Research judgment must never change a Scan result's technical score, status, or label** — see the Product Model and Research sections. Do not let a future "convenience" change (e.g. "just hide excluded rows from the score average too") quietly violate this; Scan and Research are deliberately independent computations that happen to be joined for display.
- Research (`WatchlistItem`) is **PRIVATE by default** for new rows (changed 2026-09, same precedent as `TradingAccount`'s default-visibility change) — do not revert this default, and do not add automatic `Activity`/social broadcasts when a research status changes; sharing stays explicit and opt-in via the existing visibility toggle.
- The Scanner's live universe additively **unions** in the user's own LIKE/WATCH/NEUTRAL research tickers (`getResearchUniverseTickersForUser`) — it must never be *narrowed* to only researched/watchlisted names; discovery of new candidates the user has never researched is a core Scanner requirement, not a nice-to-have.
- `CampaignEvent` rows are only ever created (`create`/`createMany`), never updated or deleted, by any workflow function — this is proven by both the original roll test and a full open→roll→close lifecycle test in `workflows.integration.test.ts`. Any future campaign workflow must follow this pattern; do not introduce an `update`/`delete` on `CampaignEvent` to "fix" a display bug — fix the display/summarization logic instead.

---

## Known Issues / Technical Debt

- **Web Push is unfinished** — documented no-op, real implementation deferred (see PWA section).
- **`LST_SESSION_SECRET` env var name predates the "Off Shift Options" rebrand.** Cosmetic only (it's an internal config key, not user-visible), but renaming it later requires coordinating the Hostinger env var change with a deploy — not urgent.
- **CSP allows `script-src`/`style-src` `'unsafe-inline'`** because Next's App Router needs it without a nonce-based setup. A stricter nonce-based CSP is a possible future hardening step, not currently required.
- **No CI-automated migration deploy** — Hostinger cannot run `prisma migrate deploy` (see the warning in Production / Deployment). The Supabase migration workflow is currently a manual runbook: apply and verify a migration against Supabase *before* pushing dependent code to `main`. Reasonable to automate in CI later; not done yet, and not to be implemented as a side effect of an unrelated task.
- **Live Schwab scanner is a foundation, not the full production scanner yet.** The current local slice adds a simple provider-level market-data cache/in-flight coalescer, but the live universe is intentionally still the 13-symbol starter list, option-chain calls are capped, and there is no broad universe scanner or full batching/backoff policy yet. A broader curated universe plus documented rate-limit handling is still a future Schwab scanner slice.
- **No UI action yet for covered-call sell/close or stock sale** on an `ASSIGNED` campaign — intentionally deferred, honestly labeled in the UI, and is the recommended next slice (see Next Tasks). The finance/schema layer already supports these event types.
- **Research's verified fundamentals (P/E, PEG, D/E, Current Ratio, EPS, dividend yield) are still not populated (verification attempted twice, both blocked by the same missing live connection)** — see the Research section for the 2026-09-02 documentation-research lead suggesting Schwab's quote endpoint may not expose PEG/D-E/Current-Ratio at all (unverified), and the exact next step once a live connection exists.
- **Eric's spreadsheet research was not imported this slice** — deferred by explicit permission since it wasn't provided this session; see the Research section for the exact dry-run-first importer design to build once the workbook is available.
- **Reference-tool evaluation completed, none implemented this slice** (LST-inspired concepts, explicitly deferred per instruction): a "Power of 1%" compounding **planner** comparing a projected balance against OSO's own actual performance is HIGH VALUE for later (OSO's edge over a generic calculator is having real performance data to compare against - see Performance section's eventual scope); an "Escape/Recovery" calculator for an underwater assigned position is USEFUL LATER but should live *inside* an `ASSIGNED` Campaign (pre-filled with real basis/shares/cash from that campaign) rather than as a disconnected global tool, and must not portray averaging down as automatically correct; a glossary/"Learn the Lingo" is LOW PRIORITY (Scanner Rules already explains its own criteria; prefer contextual tooltips over a standalone education subsystem); a dedicated "Tools" hub page is NOT NEEDED yet - each utility belongs inside the workflow it serves (e.g. the Escape calculator inside a Campaign) rather than a separate destination cloned from another product's information architecture.
- Manual campaigns/accounts remain fully manual entry; Schwab-synced accounts update via "Sync now," and a Schwab position can now be explicitly linked to a Campaign via Tracker -> Accounts -> "Broker Activity Awaiting Review" (see Schwab API section). Still missing: importing/reconciling from the live API path directly (today only CSV-imported `BrokerRecord` positions populate the review queue - a live-synced position only becomes reviewable once it also has a corresponding imported/persisted `BrokerRecord` row), and "Edit" during reconciliation is limited to editing the proposed Campaign fields inline before confirming (no separate edit-after-link flow yet).
- **Next.js dev overlay intercepts clicks in headless testing (pre-existing, unrelated to appearance/scanner work) — confirmed, not just presumed**: the CSP's `script-src` lacks `unsafe-eval` (see the existing CSP bullet above), which makes `next dev` show a persistent floating dev-tools indicator badge with a "1 Issue" warning. That badge sits bottom-left of the viewport, which happens to overlap the sidebar's Sign Out button, and its `<nextjs-portal>` intercepts the actual OS-level click coordinate — so Playwright (and a real developer) cannot click Sign Out there in dev mode; `force: true` doesn't help because the click still lands on the overlay, not the button underneath. **Confirmed dev-mode-only during the scanner redesign slice**: the exact same `phase1b.spec.ts` Sign Out flow that hangs under `next dev` passes cleanly (6/6) under a real `next start` production-mode server — see Testing section. Not fixed (out of scope — CSP is a security control, not a UI slice); a real fix would be `devIndicators` repositioning in `next.config.ts` or resolving the underlying `unsafe-eval` gap, either of which is a separate, deliberate decision for Matt.
- **Dashboard "Open positions" and "Secured (CSP)" are additive except where explicitly linked** — an unreconciled broker position and an unrelated OSO campaign both still count separately by design (no reconciliation identity exists for them), but once a position is linked to a Campaign via reconciliation, it counts exactly once. See the Schwab API section's "Dashboard/Tracker double-count fix."

---

## Current State — Where We Left Off

- Off Shift Options is live on Hostinger, deployed via GitHub `main` auto-deploy.
- Supabase production PostgreSQL is live and reachable; `/api/health` has reported healthy.
- Production Matt/Eric accounts have been bootstrapped via the safe, non-destructive bootstrap script and login has been verified in production.
- `offshiftoptions.com` has been purchased, connected as the production domain, and confirmed live; `NEXT_PUBLIC_APP_URL` has already been updated in Hostinger to `https://offshiftoptions.com` and Hostinger redeployed afterward (externally confirmed). The old temporary Hostinger URL is no longer primary.
- Schwab Trader API app approval/configuration is complete as of 2026-08-31: Market Data Production and Accounts and Trading Production are enabled, Order Limit is 0, and the production callback is `https://offshiftoptions.com/api/schwab/callback`.
- Earlier completed work: a full pre-Schwab production hardening pass (rebrand, PWA icons, Change Password, live chat, security headers, `/api/health` fix), a scanner demo-quality pass (setup scoring, near-match detection, exclusion diagnostics, Score/Filter modes, quick filters, `Demo*Provider` naming), the Campaign Tracker foundation (`Campaign`/`CampaignEvent`/`RecordVisibility` schema, lifecycle math, INHERIT/SHARED/PRIVATE visibility, the original `/positions` UI), and the Schwab OAuth/market-data foundation (env config, signed OAuth state, `/api/schwab/connect`/`callback`, encrypted token storage/refresh, read-only market-data and broker-read providers, the original Account-page connection panel, and the `LIVE • SCHWAB` live scanner action). All of the above is committed, pushed, and live in production.
- **Migration `20260828215700_campaign_tracker_foundation` applied to production Supabase on 2026-08-29** (Matt ran `prisma migrate deploy` + `prisma migrate status`, confirmed up to date). Campaign Tracker code committed and pushed to `origin/main` in the same session — see Recent Relevant Commits.
- No schema migration was created or required for the Schwab OAuth/market-data foundation; it reuses the existing `BrokerConnection` model.
- This continuity system (`PROJECT_HANDOFF.md`, `AGENTS.md` update, `CLAUDE.md`) is in place so any AI can resume by reading this file.
- **The "Core Trading Cockpit + Account/Tracking Usability" slice** — driven by an independent 53-finding UX audit of production (`OFF_SHIFT_OPTIONS_UX_AUDIT.md`, repo root) and taking over an interrupted Codex slice that had partially added the `AccountLedgerEntry` schema and LST Core threshold values — is **committed, pushed, and deployed**. Delivered: the Account Ledger (schema + domain logic + `/account` UI), the LST Core scanner baseline with a "Reset to LST Core" action and diff badges (plus a real seed-data bug fix so fresh profiles actually match LST Core), gating-vs-preference honest score/label semantics on the scanner, a Schwab account-sync workflow with an honest Sync-now-first `/account` UI and a read-only Tracker open-positions panel, a rebuilt one-screen Dashboard cockpit, and the Tracker's Open/History/Performance/Accounts tabs with performance math that is provably never blended across users. Full detail is in the Account Ledger, Scanner, Schwab API, and Campaign Tracker sections above. Commit `a7af62b` (2026-08-31).
- **Migration `20260831211006_account_ledger_and_broker_link` was applied to production Supabase and verified** (Matt ran `prisma migrate deploy` + `prisma migrate status`, confirmed "Database schema is up to date") before the above commit was pushed to `main`.
- **2026-08-31 follow-up fix (same day, post-deploy live smoke test): Dashboard broker-position visibility.** The live smoke test found Matt's 3 real synced Schwab short puts (RIOT/APLD/CORZ) correctly appearing on the Tracker's Open tab, but the Dashboard still showed "Open positions: 0" / "Secured (CSP): $0.00" because it only ever read OSO campaigns, never Schwab positions. Fixed by wiring `getSchwabOpenPositionsForUser` into the Dashboard (same pattern as the Tracker) and adding two new domain modules — `src/domain/finance/occOption.ts` (OCC option symbol parsing/formatting, e.g. "RIOT 260904P00017500" → "RIOT · Sep 4, 2026 · $17.50 Put") and `src/domain/finance/brokerPositions.ts` (short-put classification, CSP secured-capital math, additive open-positions counting) — used by both the Dashboard and the Tracker's Schwab positions panel for consistent display and math. Also fixed two stale copy strings ("Demo/manual data only..." on the Tracker, "Demo/manual Phase 1" in the sidebar) that were no longer accurate once live Schwab data was displayed, and added a `LIVE SCHWAB`/`MANUAL`/`MIXED` badge next to the Dashboard's Account Value stat. **No schema change was required or made for this fix.**
- **Deferred from the UX audit, intentionally not attempted** (engineering judgment — see the audit file for full detail): a full `/t/RIOT`-style Research page, a whole-options-universe scanner, whole-chain strike optimization, third-party earnings-calendar ingestion (the `earningsDistance` criterion stays honestly `UNKNOWN` on live Schwab scans — see Scanner section), achievements/gamification, a projections/forecasting engine, CSV import of historical trades, consolidating Recs/Chat/Alerts into one inbox, and a full navigation redesign (only the one low-risk, directly-tied rename — "Settings" → "Scanner Rules" — was made). These remain good candidates for future slices.
- **Application-wide Appearance System (Dark/Light/System) shipped 2026-09-01** — pushed as commit `c6d7ddd`, migration `20260901031101_user_appearance_preference` applied to production Supabase and verified.
- **Scanner UX redesign for compact/high-density scanning shipped 2026-09-01** — pushed as commit `b9387b1` (includes the Playwright login-helper fix so the suite can run against `next start`). No schema migration — view-layer only.
- **Scanner<->Research slice shipped 2026-09-01** — pushed as commit `2ea2de3`, migration `20260901200601_research_model` applied to production Supabase and verified. See Product Model and Research sections for full detail.
- **Schwab fundamentals verification attempted again 2026-09-02, still blocked** — no live Schwab connection available locally; produced a documentation-sourced (unverified) lead on what Schwab's quote endpoint likely does/doesn't expose. No code changed. See Research section.
- **OSO Performance Optimization Phase A implemented and validated 2026-09-02.** It removes the duplicate private-route RSC prefetch storm, makes Tracker tab selection immediate while keeping URL deep links, skips unrelated Tracker tab server reads, adds compact private-route loading shells, makes Scanner/Research status buttons optimistic with rollback, narrows `setResearchStatusAction` revalidation to `/research` and `/scanner`, and removes the duplicate normal-path Scanner profile read. No Schwab fundamentals work, schema migration, or order-entry functionality was added.
- **OSO Performance Optimization Phase B implemented and validated 2026-09-03.** It adds intent-based private-route prefetching, App Router stale-time reuse, a faster Dashboard shell that does not block on live Schwab position reads, short-term user/connection-scoped Schwab broker-read caching with explicit Sync Now/disconnect invalidation, and batched broker reconciliation evidence loading. Final safety checks confirmed normal browser link semantics and Matt-to-Eric private router-cache isolation. No Schwab fundamentals work, schema migration, or order-entry functionality was added.
- **Performance + Campaign History redesign implemented locally 2026-09-03 and awaiting Matt review/commit/push.** It keeps History as the append-only campaign record for all states, replaces the thin Performance tab with a dense account/current/projection cockpit, adds contribution-adjusted 1% target math, adds exact-source current mark handling for open CSPs, updates local seed/test fixtures for the visible examples, and adds accessible contextual help for beginner-facing explanations. No source of truth changed for account value: Schwab/account snapshots remain authoritative through `AccountLedgerEntry` and `currentAccountValue()`, and campaign premium/marks are never double-added to account value.
- **Multi-user Schwab credential routing + CSV import/reconciliation foundation shipped 2026-08-31 in commit `5cf6c0b`.** Implemented: encrypted per-user Schwab developer credentials with OAuth resolution tied to the selected credential; user-scoped market-data (`resolveMarketDataProviderForUser`) and personal-broker (`resolvePersonalBrokerProviderForUser`) provider resolvers with no cross-user fallback (tested); provider-level market-data cache/in-flight coalescing; a Schwab CSV import pipeline for all three real export types (Positions/Transactions/Realized Gain-Loss - parser verified against real files Matt provided directly, not committed to Git) with a strict preview-before-persist lifecycle, NEW/DUPLICATE/CONFLICT/NEEDS_REVIEW/INVALID classification, and database-backed idempotency; and a "Broker Activity Awaiting Review" reconciliation UI that confirms broker evidence into a real Campaign (via the existing `createCampaignForUser` workflow) and durably links it so the Dashboard counts it once, not twice. Verified end-to-end in a real browser (upload -> preview -> confirm -> reconcile -> linked Campaign, with screenshots). Migration `20260831235050_schwab_credential_routing_broker_records` is applied to production Supabase.

### Git / Deployment State (as of this session)

- The Performance + Campaign History redesign referenced below as "awaiting Matt review" was subsequently reviewed, committed (`0fe4b1b`), and pushed. Since then, Scanner responsiveness/performance (`747f21f`) and Research customization + per-user view preferences (`ce637b1`, migration `20260903201201_research_customization_and_view_preferences` applied to production) have also been committed, pushed, and deployed - see Recent Relevant Commits for the full, current list. `HEAD`/`origin/main` were at `ce637b1` before this session's Schwab Fundamentals diagnostic slice began.
- The "Auto Schwab fundamentals wired into Research" slice was subsequently reviewed, committed (`52f6d7a`), and pushed - `HEAD`/`origin/main` are at `52f6d7a` as of this session's start.
- **This session (2026-09-03) added a verification-only Alpha Vantage OVERVIEW diagnostic** (`/account/alpha-vantage-fundamentals`, see Alpha Vantage API section) - not yet committed/pushed pending review. No schema change, no migration, no product writes, no automatic/scheduled Alpha Vantage usage. Not yet run against the real production key.
- **This session (2026-09-04) implemented the user-configurable Roll Buffer % and Roll Status (GREEN/AMBER/RED) display, plus a short-position display cleanup** — see Roll Status section for full detail. Also fixed a pre-existing, unrelated-to-this-feature issue found while verifying it: the Dashboard's Schwab broker-position rows (`describeBrokerPositionForDisplay` in `src/domain/finance/brokerPositions.ts`) colored a short option's negative market value red as if it were a loss and showed a bare signed quantity ("-1 contract"); now neutral "Short 1 put" / "Cost to close: $X" with no red/green tied to being short (2 new regression tests in `brokerPositions.test.ts`).
  - Migration `20260904144732_user_roll_buffer_preference` (additive-only, single `ADD COLUMN UserSettings.rollBufferPercent DEFAULT 3.0`) was applied and verified against this session's local development database first, then **applied to production Supabase and confirmed via `prisma migrate status` ("Database schema is up to date!") before this slice was committed/pushed** — matching the project's standing manual-migration-before-deploy practice.
  - All other checks pass: `tsc --noEmit`, `eslint src`, `prisma validate`, `next build`, and the full Vitest suite (365/365, run with `RUN_DB_TESTS=1` against the local database). A live browser/Playwright smoke pass (production-mode `next start` on port 3001) confirmed: Roll Buffer defaults to 3.0% and round-trips through save/reload on `/scanner/settings`; Dashboard and Tracker Open both render an honest "Roll status unavailable / No live price" badge for every open cash-secured put (Matt has no live Schwab OAuth connection in this local environment, so no colored GREEN/AMBER/RED classification or short-position wording could be exercised end-to-end this session — both are proven at the unit/DB-integration level instead, 15 + 2 new tests respectively); Tracker History correctly shows no Roll Status badge.
  - **Committed and pushed to `origin/main` this session** (commit message: "feat: add configurable roll-buffer decision support").
- **This session (2026-09-04, continued) implemented: wide-screen shell width, sidebar theme-control overflow fix, Research Columns dropdown outside-click/Escape, a "Next LST Checkpoint" Dashboard reminder, and Alpha Vantage BALANCE_SHEET support (Current Ratio shipped, Debt/Equity deliberately deferred pending a one-ticker verification click)** — see App Shell/Navigation, Research, Roll Status, and Alpha Vantage API sections for full detail.
  - Migration `20260904162540_alpha_vantage_balance_sheet` (additive-only, 8 new nullable `TickerFundamentals` columns + one index) is applied and verified **only against this session's local development database**. **Explicitly NOT applied to production Supabase and NOT pushed this session**, per direct instruction (schema-changing UI/architecture work delivered for review before the usual migrate-then-push sequence).
  - All checks pass: `tsc --noEmit`, `eslint src`, `prisma validate`, `next build`, and the full Vitest suite (401/401, `RUN_DB_TESTS=1` against the local database). Live Playwright smoke-tested (see Testing section) plus the full pre-existing `desktop-sidebar.spec.ts` (12/12) and `research-customization.spec.ts` (16/16) regression suites.
  - **Not committed or pushed this session** - working tree holds exactly this slice's diff, ready to commit once Debt/Equity's one-ticker verification (see Alpha Vantage API section) has been run and, separately, once the production migration has been applied through the normal runbook.

---

## Next Tasks

**NOW**
- **Wide-screen layout / theme overflow / Columns dropdown / LST checkpoint / Alpha Vantage BALANCE_SHEET slice is implemented, tested, and browser-smoke-tested locally but NOT committed/pushed** (see Current State and the relevant feature sections). Two things must happen before it ships: (1) click "Verify Balance Sheet fields (APLD - 1 call)" on `/account/alpha-vantage-fundamentals` once deployed, and report back which debt-candidate fields came back `PRESENT_VALUE`, so a future slice can implement Debt/Equity without guessing; (2) apply migration `20260904162540_alpha_vantage_balance_sheet` to production Supabase via the normal `prisma migrate deploy` runbook (it is additive-only, currently local-dev-only). Neither blocks the other - the diagnostic click can happen any time after deploy, independent of when the migration runs.
- **Roll Buffer % / Roll Status slice (including the short-position display cleanup) shipped 2026-09-04** — migration `20260904144732_user_roll_buffer_preference` applied to production Supabase and verified, committed, and pushed to `origin/main`. See Roll Status section and Current State for full detail. No live-browser verification of colored GREEN/AMBER/RED or the short-position wording exists yet against a real Schwab-connected account (only proven at the unit-test level) — worth a quick manual check once Matt has a live Schwab connection available, low priority.
- Review the local Alpha Vantage OVERVIEW verification diagnostic (see Alpha Vantage API section) - if approved, commit and push to `origin/main`.
- **Once pushed and deployed, Matt should click "Verify Alpha Vantage Fundamental Fields" in Account exactly once** (uses up to 3 of the 25 daily calls) and report back which fields came back `PRESENT_VALUE` for all three tickers, especially Industry/Sector/PEG/profitability/analyst-target/Current-Ratio/Debt-Equity - those are the candidates to design the shared-cache slice around next. Do this once during regular market hours and once after-hours if the daily budget allows, matching the Schwab diagnostic's verification approach.
- Once results are in hand: design and build the shared per-ticker fundamentals cache described in the Alpha Vantage API section (new schema needed - a ticker-keyed table, not user-keyed; STOP and report the migration before creating it), including OSO's own daily-request counter (Alpha Vantage exposes none) and the priority queue (Research tickers first, then Scanner PASS/Near). Do not tie Alpha Vantage calls to "Run Live Scan" directly.
- Recommended next performance slice: profile production with a real OAuth-connected Schwab account, then decide whether to persist live API broker records for reconciliation, further reduce Dashboard non-critical query work, or add broader route/data instrumentation before another optimization pass.
- **Import from the live Schwab API path (not just CSV) is a good next slice**: today only CSV-imported `BrokerRecord` rows populate "Broker Activity Awaiting Review" - a purely live-synced position (never also imported via CSV) has nothing to reconcile against yet. Persisting a `BrokerRecord` from `syncSchwabAccountForUser`/`getSchwabOpenPositionsForUser` would close this gap using the same `normalizeSchwabApiPosition`/`normalizeSchwabApiTransaction` normalizers already built for it.
- Conflict resolution UI: `BrokerRecord.status = CONFLICT` rows are visible in the import preview and stored for review, but there is no dedicated "resolve this conflict" screen yet (accept the new value / keep the old one / edit) - conflicts currently just sit there for a human to notice via the batch counts.
- Once broker-position reconciliation is prioritized: add an explicit `Campaign` <-> broker-position link (schema change) and replace the additive (non-deduplicated) Open Positions/Secured CSP counting with that link, so the same real-world trade is never counted twice (see Schwab API section). This depends on the import/reconciliation UI above existing first.
- Next Schwab scanner slice: broaden the live ticker universe, add batching/rate-limit backoff/caching, add stronger empty-chain diagnostics, and decide whether a real earnings-data provider is worth adding (see Scanner section's earnings-`UNKNOWN` limitation) versus keeping it honestly `UNKNOWN`.
- **Recommended next implementation slice:** finish the wheel lifecycle UI — a "Sell Covered Call" action, a "Close Covered Call"/"Expire Covered Call" action, and a "Record Stock Sale" action for `ASSIGNED` campaigns, mirroring the existing `closeCampaignPutForUser`/`rollCampaignPutForUser` pattern (ownership re-checked server-side, event appended, never mutating prior events). The finance math and schema already support this; only the create-event workflows/actions/forms are missing.
- Consider automating the Supabase migration-deploy step in CI instead of the manual runbook.
- Consider renaming `LST_SESSION_SECRET` to something brand-neutral (coordinate with a Hostinger env var update — not urgent).
- **Verify Schwab quote fundamentals** (see Research section - attempted 2026-09-01 and 2026-09-02, both times with no live connection available): once a live, OAuth-connected Schwab developer credential exists locally, call `getQuote` for a harmless public ticker and log the raw `fundamental` field group's names/values/units (never log tokens/credentials/account numbers). Cross-check against the 2026-09-02 documentation lead (`peRatio`/`eps`/`divYield`/`divAmount`/dividend dates only - no PEG/D-E/Current-Ratio found in that source) before wiring anything into `updateResearchDetailsForUser`'s auto-fetch path, with explicit unit handling for Debt/Equity if it turns out to exist after all.
- **Eric's research spreadsheet import**: build the dry-run-first importer once Matt provides the actual workbook (local/gitignored only, never committed) — see Research section for the exact required design (explicit target user, preview counts before writing, confirmation required for interpretive mappings like `"AVOID"` text -> `NEVER_TRADE`, never logs spreadsheet contents, never runs against production without explicit approval).
- Consider extracting the Scanner's score-parsing logic (`toDomainSummary`/`toViewResult`), currently duplicated between `scanner/page.tsx` and `research/page.tsx`, into a shared `src/domain/scanner/view.ts`-style module — deliberately not done this slice to avoid touching the already-approved Scanner redesign (see Research section).
- Research mobile density: the tap-to-expand card list works and has no horizontal overflow, but could be tightened further for very large (50+) research lists — a good candidate for a future pass once real usage feedback exists.

**DEFERRED FROM THE 2026-08-31 UX AUDIT** (engineering judgment — see `OFF_SHIFT_OPTIONS_UX_AUDIT.md` and Current State for why)
- A dense Research page now exists (2026-09-01, see Research section) — the audit's original "whole `/t/RIOT`-style research page" idea is effectively addressed; only whole-options-universe scanning and whole-chain strike optimization remain deferred from that original finding.
- Third-party earnings-calendar ingestion (stays `UNKNOWN` honestly for now).
- Achievements/gamification; a projections/forecasting engine (would require fabricating confidence the app doesn't have).
- CSV import of historical trades.
- Consolidating Recs/Chat/Alerts into one inbox; a full navigation redesign.

**SCHWAB INTEGRATION**
- Keep OAuth/token/account-hash handling server-only and re-check official Schwab docs before adding new endpoint coverage. If Schwab publishes PKCE support/requirements later, add it deliberately with tests.
- Expand shared live market-data integration for quotes, historical prices, option chains, expirations/strikes, bid/ask, option volume, open interest, and Greeks where Schwab provides them.
- Build Matt's read-only account/position/transaction sync as user-scoped brokerage data with an explicit reconciliation/confirmation UI. Eric remains independent/manual unless he later authorizes his own Schwab connection through the same OSO Schwab app.
- Gradually replace the demo scanner universe with live option-chain data while keeping the same PASS/FAIL/UNKNOWN engine and visible data-source labels.
- Re-verify the "no order execution" boundary after every Schwab expansion.

**LATER**
- Complete real Web Push delivery (VAPID keys, `web-push` dependency, client subscribe flow, service worker push handler).
- Stricter nonce-based CSP.
- Expand automated test coverage for the change-password and live-chat features with a two-context Playwright scenario, if desired.

---

## Recent Relevant Commits

- `ce637b1` — feat: add customizable research workspace and fundamentals (Research customization + per-user view preferences; migration `20260903201201_research_customization_and_view_preferences` applied to production)
- `747f21f` — perf: speed up live scanner and add scan feedback (Scanner responsiveness/performance; immediate Scanning… UX, bounded market-data concurrency, staged flow preserved)
- `0fe4b1b` — feat: redesign performance history and contextual help
- `a7af62b` — feat: add account ledger, LST Core scanner baseline, Schwab account sync, and cockpit dashboard/tracker
- `7972423` — feat: wire live Schwab positions into dashboard metrics
- `868e250` — test: cover campaign visibility, roll history, and lifecycle math
- `648c970` — feat: add campaign tracker foundation (accounts, campaigns, lifecycle, visibility)
- `4cba783` — feat: enhance scanner with setup scoring, quick filters, and diagnostics
- `ff413e8` — docs: clarify production migration and domain state
- `7d847e4` — docs: document pre-Schwab production hardening

Full history is in Git — this list is only enough to orient a new AI, not a complete log.
