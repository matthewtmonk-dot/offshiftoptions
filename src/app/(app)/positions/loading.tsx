import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading title="Loading Tracker" detail="Getting campaigns, accounts, and the selected tab ready." />;
}
