import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading title="Loading Dashboard" detail="Refreshing the latest account and setup summary." />;
}
