import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading title="Loading Account" detail="Refreshing account settings and read-only connections." />;
}
